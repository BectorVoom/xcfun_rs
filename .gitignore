/target
# Vendored C++ reference sources — not tracked; provided out-of-band.
xcfun-master
# Tier-2 raw record dump — regenerated by `cargo run -p validation`.
# At order=3 the file reaches ~1.5 GB; commit `validation/report-summary.json`
# (per-cell aggregate) and `validation/report.html` (human view) instead.
validation/report.jsonl
# Ephemeral GSD executor worktrees — should never be tracked.
.claude/worktrees/
# Phase 6 D-04 — Python sidecar bytecode caches (xtask/mpmath_eval).
__pycache__/
*.pyc
