---
quick_id: 260508-c4abi8
slug: regen-c-abi-expected-json
description: Regenerate xcfun-capi c_abi expected.json after 06-N7 substrate fixes
date: 2026-05-08
mode: quick
must_haves:
  truths:
    - crates/xcfun-capi/tests/fixtures/expected.json regenerated against current master
    - cargo test -p xcfun-capi --test c_abi passes (all 4 fixtures, including bp86 fixture #4)
    - Material drift in the fixture is confined to functionals touched by 06-N7 substrate fixes (P86C, BECKEX, PW91C, SPBEC)
  artifacts:
    - crates/xcfun-capi/tests/fixtures/expected.json
  key_links:
    - crates/xcfun-capi/examples/gen_expected.rs:229
    - crates/xcfun-capi/tests/c_abi.rs
    - .planning/todos/pending/2026-05-08-regenerate-c-abi-expected-json-after-06-n7-substrate-fixes.md
---

# Quick Task 260508-c4abi8: Regenerate xcfun-capi c_abi expected.json

## Context

`cargo test -p xcfun-capi --test c_abi` currently fails on **fixture #4 (`bp86`, order 1, mode 1)** —
6 of 6 output slots out of tolerance. Energy slots drift at rel ≈ 1.6e-9 / 3.0e-9; gradient slots
at rel ≈ 7.4e-6 / 1.2e-5.

This is **not** a regression in computation — `expected.json` was baked at commit `849419d`
(Phase 5, 2026-04-30) and never regenerated after the 06-N7 substrate fixes (`b0e4409`,
`d204c69`, `df57c90`) shifted P86C and BECKEX outputs.

PW91C is **not** part of this failure set; its 1.8k residual lives in the validation harness,
not in `cargo test`. See the captured todo at
`.planning/todos/pending/2026-05-08-regenerate-c-abi-expected-json-after-06-n7-substrate-fixes.md`.

## Tasks

### Task 1: Run the regen example

**Files:** `crates/xcfun-capi/tests/fixtures/expected.json` (overwritten)

**Action:** `cargo run -p xcfun-capi --example gen_expected --release`. The example writes
to `crates/xcfun-capi/tests/fixtures/expected.json` (per `examples/gen_expected.rs:229`).

**Verify:** `git diff -- crates/xcfun-capi/tests/fixtures/expected.json` shows changes; the
material drift is confined to functionals touched by 06-N7 substrate fixes (bp86 = BECKEX +
P86C; any other 06-N7-touched functional). No unrelated functional should shift.

**Done:** Diff inspected; only expected functionals shifted.

### Task 2: Verify the c_abi test now passes

**Files:** none modified

**Action:** `cargo test -p xcfun-capi --test c_abi`.

**Verify:** All 4 fixtures pass. Test runner exits 0.

**Done:** Test green.

### Task 3: Archive the captured todo

**Files:** `.planning/todos/pending/2026-05-08-regenerate-c-abi-expected-json-after-06-n7-substrate-fixes.md`

**Action:** Delete (or move to `.planning/todos/done/`) — match existing convention. The
project currently has no `done/` directory, so deletion is the simplest match.

**Verify:** `ls .planning/todos/pending/` no longer lists the regen todo; the cbrt-expand todo
remains.

**Done:** Pending todo cleaned up.
