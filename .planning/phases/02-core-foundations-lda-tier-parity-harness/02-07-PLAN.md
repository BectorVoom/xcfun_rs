---
phase: 02-core-foundations-lda-tier-parity-harness
plan: 07
type: execute
wave: 4
depends_on: [01, 02, 03, 04, 05, 06]
files_modified:
  - docs/design/02-data-structures.md
  - docs/design/05-module-responsibilities.md
  - docs/design/06-cubecl-strategy.md
  - docs/design/09-testing-strategy.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: [CORE-01, CORE-02, CORE-03, CORE-04, CORE-05, CORE-06, CORE-07, CORE-08, CORE-09, CORE-10, LDA-01, LDA-02, LDA-03, LDA-04, LDA-05, LDA-06, LDA-07, LDA-08, LDA-09, LDA-10, MODE-04, ACC-01, ACC-02, ACC-03, ACC-04, ACC-05, ACC-06, QG-01, QG-02, QG-06, QG-07]

must_haves:
  truths:
    - "`docs/design/02-data-structures.md` §5 (DensVars) updated to reflect Phase 2 D-02: `DensVarsDev<F>` is a `#[derive(CubeType, CubeLaunch)]` cubecl type in xcfun-eval, NOT a host `DensVars<T>` in xcfun-core."
    - "`docs/design/05-module-responsibilities.md` §2 (xcfun-core) updated to reflect Phase 2 D-04: functional bodies + dispatch + Functional struct live in `xcfun-eval`; xcfun-core owns types + registry tables only. Boundary rules (esp. rule 2/3/10) updated."
    - "`docs/design/06-cubecl-strategy.md` §3 (kernel structure) updated to confirm functional bodies are `#[cube] fn`s composing Phase 1 ctaylor primitives + Phase 2 build_densvars."
    - "`docs/design/09-testing-strategy.md` updated with the tier-1 self-test pattern (Plan 02-04) + tier-2 parity harness pattern (Plan 02-06) actually landed."
    - "`.planning/REQUIREMENTS.md` marks all 31 Phase-2 requirement IDs as `[x]` Complete with the Phase 2 traceability row updated: CORE-01..10, LDA-01..10, MODE-04, ACC-01..06, QG-01, QG-02, QG-06, QG-07."
    - "`.planning/ROADMAP.md` marks Phase 2 entry as `[x] **Phase 2: Core Foundations + LDA Tier + Parity Harness** - Complete (2026-04-XX)`. The Plans table extended with all 7 plans marked complete; the Progress Table row updated to `7/7` complete with completion date."
    - "`.planning/STATE.md` updated: progress percentage advanced (Phase 1 + Phase 2 complete = 25% project), Decisions block extended with the new Phase 2 decisions added during execution, Recent activity records each Plan 02-* completion, Session Continuity points to Phase 3."
    - "All 4 doc-update commits + the sign-off commit follow the atomic-commits-per-task discipline (D-09 inheritance from Wave 0)."
  artifacts:
    - path: "docs/design/02-data-structures.md"
      provides: "§5 updated for DensVarsDev #[cube] type"
      contains: "DensVarsDev"
    - path: "docs/design/05-module-responsibilities.md"
      provides: "§2 updated for xcfun-eval ownership of functional bodies + dispatch"
      contains: "xcfun-eval"
    - path: "docs/design/06-cubecl-strategy.md"
      provides: "§3 updated to confirm Phase 2 #[cube] fn pattern landed"
      contains: "Phase 2"
    - path: "docs/design/09-testing-strategy.md"
      provides: "Tier-1 + Tier-2 patterns documented as landed in Plan 02-04/02-06"
      contains: "tier-2"
    - path: ".planning/REQUIREMENTS.md"
      provides: "31 Phase-2 IDs marked [x]; traceability rows updated to Complete"
      contains: "Complete"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 2 marked [x] Complete; Plans table populated; Progress Table 7/7"
      contains: "Phase 2"
    - path: ".planning/STATE.md"
      provides: "Progress % advanced; Decisions block extended; Phase 2 sign-off recorded"
      contains: "Phase 2"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/ROADMAP.md + .planning/STATE.md"
      via: "Phase 2 IDs marked [x] Complete; ROADMAP Phase 2 row marked [x]; STATE Recent activity records each plan completion"
      pattern: "Complete"
    - from: "docs/design/02-data-structures.md §5"
      to: "crates/xcfun-eval/src/density_vars.rs (DensVarsDev source-of-truth)"
      via: "Doc references the cubecl-type field set + 22-field layout"
      pattern: "DensVarsDev"
    - from: "docs/design/05-module-responsibilities.md §2"
      to: "Phase 2 D-04 (functional bodies move from xcfun-core to xcfun-eval)"
      via: "Updated boundary rules"
      pattern: "xcfun-eval"
---

<objective>
Wave 3 — Update the design brief (4 docs/design/ files) to reflect what actually shipped in Phase 2 (D-02 cubecl-type DensVarsDev, D-04 xcfun-eval ownership, D-21 Functional minimal slice, D-22 regularize, D-24 LDAERF override, D-25 UnknownName drop payload). Mark all 31 Phase-2 requirement IDs as `[x]` Complete in REQUIREMENTS.md. Mark Phase 2 as `[x]` Complete in ROADMAP.md with the 7-plan listing. Update STATE.md with progress percentage + new Phase 2 decisions + recent activity log + session continuity pointer to Phase 3.

Purpose: This is the Phase 2 sign-off plan — the documentation step that records what shipped, makes the cross-phase requirement traceability accurate, and queues Phase 3 (GGA tier + Mode::Potential) as the next logical phase. Without this plan, the design brief contradicts the as-built code (e.g., docs/design/02 §5 still says DensVars is a host `<T:Num>` struct in xcfun-core, but the as-built is `DensVarsDev<F>` cubecl-type in xcfun-eval). Without it, REQUIREMENTS.md still shows 31 Phase-2 IDs as Pending, blocking the next phase's planning sweep.

Output: 4 design-doc updates + REQUIREMENTS.md update + ROADMAP.md update + STATE.md update; total ~5 commits per D-09 atomicity.
</objective>

<execution_context>
@/home/chemtech/workspace/xcfun_rs/.claude/get-shit-done/workflows/execute-plan.md
@/home/chemtech/workspace/xcfun_rs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-core-foundations-lda-tier-parity-harness/02-CONTEXT.md
@.planning/phases/02-core-foundations-lda-tier-parity-harness/02-RESEARCH.md
@.planning/phases/02-core-foundations-lda-tier-parity-harness/02-01-PLAN.md
@.planning/phases/02-core-foundations-lda-tier-parity-harness/02-02-PLAN.md
@.planning/phases/02-core-foundations-lda-tier-parity-harness/02-03-PLAN.md
@.planning/phases/02-core-foundations-lda-tier-parity-harness/02-04-PLAN.md
@.planning/phases/02-core-foundations-lda-tier-parity-harness/02-05-PLAN.md
@.planning/phases/02-core-foundations-lda-tier-parity-harness/02-06-PLAN.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/design/02-data-structures.md
@docs/design/05-module-responsibilities.md
@docs/design/06-cubecl-strategy.md
@docs/design/09-testing-strategy.md

<interfaces>
<!-- Phase 2 decisions added during execution that should be recorded in STATE.md "Decisions added this phase" -->
- D-24 LDAERF tier-2 1e-7 override (USER-APPROVED 2026-04-20 via plan-phase prompt)
- D-25 XcError::UnknownName drops payload (USER-APPROVED 2026-04-20 via plan-phase prompt)
- D-09 Wave-0 6-commit atomic boundary (Wave-0a..0f)
- D-12 (refined): regen-registry committed output covers 11 LDA + 31 vars + 0 aliases (Phase 4 extends)
- Plan 02-04 Wave-1B-14a: build_xc_a_b amended to read input as flat (inlen × (1<<N)) array of pre-seeded ctaylor coefficients (or whatever amendment the executor made)
- Plan 02-05 Wave-1C-1: XC_A_B_GAA_GAB_GBB builder arm with explicit chain to build_xc_a_b (Pitfall PHASE2-D fix)
- Plan 02-06 Wave-2-1: xtask regen-registry extended to emit validation/c_stubs.cpp (67 non-LDA stubs)

<!-- Phase 2 requirement IDs to mark Complete (31 total) -->
CORE-01, CORE-02, CORE-03, CORE-04, CORE-05, CORE-06, CORE-07, CORE-08, CORE-09, CORE-10,
LDA-01, LDA-02, LDA-03, LDA-04, LDA-05, LDA-06, LDA-07, LDA-08, LDA-09, LDA-10,
MODE-04,
ACC-01, ACC-02, ACC-03, ACC-04, ACC-05, ACC-06,
QG-01, QG-02, QG-06, QG-07
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update docs/design/02-data-structures.md §5 + docs/design/05-module-responsibilities.md §2 + docs/design/06-cubecl-strategy.md §3 + docs/design/09-testing-strategy.md (4 docs in 1 commit OR 4 separate commits — choose based on size)</name>
  <files>
    docs/design/02-data-structures.md,
    docs/design/05-module-responsibilities.md,
    docs/design/06-cubecl-strategy.md,
    docs/design/09-testing-strategy.md
  </files>
  <read_first>
    - docs/design/02-data-structures.md §5 (DensVars section — pre-Phase-2 says host `<T:Num>` struct; needs Phase 2 D-02 update)
    - docs/design/05-module-responsibilities.md §2 (xcfun-core responsibilities — pre-Phase-2 lists "execute single-point evaluation on the CPU"; needs Phase 2 D-04 update to move that to xcfun-eval; rules 2/3/10 also need update)
    - docs/design/06-cubecl-strategy.md §3 (kernel structure — confirm Phase 2 functional bodies are `#[cube] fn`s composing Phase 1 ctaylor primitives + new build_densvars)
    - docs/design/09-testing-strategy.md (test patterns — add tier-1 self-test parametric loop pattern + tier-2 parity harness pattern as landed)
    - .planning/phases/02-core-foundations-lda-tier-parity-harness/02-CONTEXT.md D-04 (xcfun-eval ownership of functional bodies + dispatch + Functional struct)
    - .planning/phases/02-core-foundations-lda-tier-parity-harness/02-CONTEXT.md "Design brief (updates required per Phase 2 D-04)" section (lists which docs need updates)
  </read_first>
  <action>
    **1a. `docs/design/02-data-structures.md` §5 (DensVars):**

    Read the current §5 content. Replace the host `DensVars<T: Num>` struct description with the as-built `DensVarsDev<F>` design:

    Add a "**Updated 2026-04-XX (Phase 2 D-02 + D-04)**" banner near the §5 header. Replace the section body with:
    - `DensVarsDev<F: Float>` is a `#[derive(CubeType, CubeLaunch)]` struct in `crates/xcfun-eval/src/density_vars.rs`
    - 22 named `Array<F>` fields (full list with names + descriptions matching crates/xcfun-eval/src/density_vars.rs)
    - Built by `#[cube] fn build_densvars<F, N>(input, out, vars: u32, n: u32)` with comptime if-chain dispatcher (Phase 2 ships 2 variant arms: XC_A_B + XC_A_B_GAA_GAB_GBB; Phase 3+ extends)
    - `regularize` is a `#[cube] fn` mutating only `Array<F>[0]` (CNST coefficient) per CORE-06 + D-22
    - Cross-reference: code lives in `crates/xcfun-eval/src/density_vars.rs` + `crates/xcfun-eval/src/density_vars/{build,regularize}.rs`
    - Mark the pre-Phase-2 host `<T:Num>` design as SUPERSEDED with a banner pointing at this section's update

    **1b. `docs/design/05-module-responsibilities.md` §2 (xcfun-core):**

    Replace the §2 (xcfun-core) responsibilities to reflect Phase 2 D-04:
    - xcfun-core OWNS: types (Mode, Vars, Dependency, XcError, FunctionalId), constants, registry tables (FUNCTIONAL_DESCRIPTORS, VARS_TABLE, ALIASES, FunctionalDescriptor + VarsRow + Alias struct types), `taylorlen` const fn
    - xcfun-core does NOT own: functional bodies (in xcfun-eval), DensVarsDev type (in xcfun-eval), Functional struct + eval entry point (in xcfun-eval), cubecl runtime dispatch (in xcfun-eval)
    - Update boundary rule 2 (xcfun-core dependencies): only `thiserror` + `bitflags` (verify in `xtask check-boundaries` per QG-02)
    - Update boundary rule 3 (xcfun-core does not depend on cubecl): cubecl-free crate
    - Update boundary rule 10 (functional surface ownership): xcfun-eval owns the per-functional `#[cube] fn` bodies + `Functional` struct + dispatch
    - Add a new sub-section §2.1 for xcfun-eval: hosts the cubecl launcher + functional bodies + dispatcher per Phase 2 D-04

    **1c. `docs/design/06-cubecl-strategy.md` §3 (kernel structure):**

    Confirm the Phase 2 implementation:
    - Per-functional kernel signature: `#[cube] fn <name>_kernel<F: Float, const N: u32>(d: &DensVarsDev<F>, out: &mut Array<F>)` — D-17
    - Composes Phase 1 ctaylor primitives (`xcfun-ad::ctaylor_add/mul/sub/scalar_mul`, `ctaylor_rec::mul`, `math::*`) + Phase 2 `build_densvars` for input setup
    - Comptime if-chain dispatcher in `xcfun-eval::dispatch::dispatch_kernel` over 11 LDA FunctionalIds (Phase 3+ extends to 78)
    - Add a "Phase 2 status" callout: "Per-functional `#[cube] fn` body convention LANDED for 11 LDAs (Plans 02-04 + 02-05). Phase 3 extends to 45 GGAs; Phase 4 extends to 15 metaGGAs."

    **1d. `docs/design/09-testing-strategy.md`:**

    Document the tier-1 + tier-2 patterns as landed:
    - Tier-1 self-tests: `crates/xcfun-eval/tests/self_tests.rs` parametric loop over `FUNCTIONAL_DESCRIPTORS.iter().filter(|d| d.test_in.is_some())`. Each functional asserted at upstream `desc.test_threshold`. Phase 2: 8 LDAs covered (LDAERFC_JT/TW/VWK skipped per upstream-no-test-data; tier-2 covers them). ACC-04: < 5s.
    - Tier-2 parity harness: `validation/` binary crate; cc-compiled C++ static lib; 10k-point xoshiro-seeded grid; per-tuple Rust+C++ comparison; `report.html` + `report.jsonl` outputs. ACC-03: any failing element blocks merge.
    - D-24 per-functional thresholds: 1e-12 strict for 8 LDAs; 1e-7 for 3 LDAERF (transparently annotated in report.html).
    - xtask gates: check-no-mul-add (ACC-06), check-no-anyhow (QG-01), check-boundaries (QG-02), check-cubecl-pin (QG-06), check-no-fma (ACC-05 inheritance from Phase 1), regen-registry --check (CORE-10 + QG-07).

    **1e. Commit message (one combined commit acceptable; or split per-file if any single update exceeds ~80 LOC):**
    ```
    docs(02-07): Wave-3-1 design brief updates (D-02 DensVarsDev, D-04 xcfun-eval ownership, Phase 2 #[cube] fn pattern landed, tier-1/tier-2 testing strategy)
    ```
    Stage all 4 doc files.
  </action>
  <verify>
    <automated>grep -q 'DensVarsDev' docs/design/02-data-structures.md && grep -q 'xcfun-eval' docs/design/05-module-responsibilities.md && grep -q 'Phase 2' docs/design/06-cubecl-strategy.md && grep -q 'tier-2' docs/design/09-testing-strategy.md && grep -q 'D-24' docs/design/09-testing-strategy.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `docs/design/02-data-structures.md` §5 mentions `DensVarsDev<F>` and the `#[derive(CubeType, CubeLaunch)]` 22-field design + cross-reference to `crates/xcfun-eval/src/density_vars.rs`
    - `docs/design/05-module-responsibilities.md` §2 reflects xcfun-core's narrower responsibilities (types + registry only); a new §2.x or sub-section documents xcfun-eval's ownership of functional bodies + dispatch + Functional struct
    - `docs/design/06-cubecl-strategy.md` §3 confirms the Phase 2 `#[cube] fn` per-functional kernel convention with the D-17 signature
    - `docs/design/09-testing-strategy.md` documents tier-1 self-test + tier-2 parity harness as landed; mentions D-24 LDAERF override
    - Git commit history shows the doc updates committed together OR per-file (per D-09 task atomicity)
  </acceptance_criteria>
  <done>4 design docs updated to reflect Phase 2 as-built; cross-references to crates/xcfun-eval/ + validation/ in place.</done>
</task>

<task type="auto">
  <name>Task 2: Update .planning/REQUIREMENTS.md — mark all 31 Phase-2 IDs as [x] Complete + update traceability table to Complete</name>
  <files>
    .planning/REQUIREMENTS.md
  </files>
  <read_first>
    - .planning/REQUIREMENTS.md (FULL FILE — find each of the 31 Phase-2 IDs as `- [ ] **<ID>**: ...` and change to `- [x]`; find each in the Traceability table and change `Pending` to `Complete`)
    - .planning/phases/02-core-foundations-lda-tier-parity-harness/02-04-PLAN.md success_criteria + 02-05-PLAN.md + 02-06-PLAN.md (confirm all the IDs were actually delivered)
  </read_first>
  <action>
    **2a. `.planning/REQUIREMENTS.md` checkboxes:**
    For each of the 31 Phase-2 requirement IDs listed above, change `- [ ] **<ID>**:` to `- [x] **<ID>**:`.

    **2b. `.planning/REQUIREMENTS.md` Traceability table:**
    Find rows for each of the 31 IDs and change `Pending` to `Complete` (preserve any column structure).

    Specifically the IDs to mark:
    - `CORE-01, CORE-02, CORE-03, CORE-04, CORE-05, CORE-06, CORE-07, CORE-08, CORE-09, CORE-10` (Core Types & Registry; CORE-08 is empty-slice per D-12)
    - `LDA-01, LDA-02, LDA-03, LDA-04, LDA-05, LDA-06, LDA-07, LDA-08, LDA-09, LDA-10` (LDA functional ports)
    - `MODE-04` (Functional::input_length)
    - `ACC-01, ACC-02, ACC-03, ACC-04, ACC-05, ACC-06` (validation harness + tier-1 < 5s + RUSTFLAGS + mul_add ban)
    - `QG-01, QG-02, QG-06, QG-07` (xtask check-no-anyhow + check-boundaries + cubecl pin + regen-registry --check)

    **2c. Update the Phase 0 column for ACC-05 + ACC-06:**
    Per CONTEXT D-10, Phase 2 absorbed these from Phase 0. The traceability table should reflect that Phase 2 IS the executing phase (mark Complete; the original Phase 0 row notation may remain).

    **2d. Commit message:**
    ```
    docs(02-07): Wave-3-2 mark 31 Phase-2 requirement IDs as [x] Complete in REQUIREMENTS.md (CORE-01..10, LDA-01..10, MODE-04, ACC-01..06, QG-01/02/06/07)
    ```
    Stage `REQUIREMENTS.md`.
  </action>
  <verify>
    <automated>! grep -E '^\- \[ \] \*\*(CORE-0[1-9]|CORE-10|LDA-0[1-9]|LDA-10|MODE-04|ACC-0[1-6]|QG-01|QG-02|QG-06|QG-07)' .planning/REQUIREMENTS.md && grep -c '\- \[x\] \*\*CORE-0' .planning/REQUIREMENTS.md | grep -qE '^[1-9]' && grep -c '\- \[x\] \*\*LDA-' .planning/REQUIREMENTS.md | grep -qE '^[1-9]|^10' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - All 31 Phase-2 requirement IDs in `.planning/REQUIREMENTS.md` are checked: `- [x] **<ID>**:` (no `- [ ] **<ID>**` for any of CORE-01..10, LDA-01..10, MODE-04, ACC-01..06, QG-01/02/06/07)
    - The Traceability table rows for each of the 31 IDs show `Complete` in the Status column (changed from `Pending`)
    - Phase 1 IDs (AD-01..06) remain `[x]` Complete (no regression)
    - Phase 3+ IDs (GGA, MGGA, MODE-01/02/03/05, ALIAS, RS, CAPI, PY, KER, GPU, etc.) remain `[ ]` Pending
  </acceptance_criteria>
  <done>REQUIREMENTS.md reflects Phase 2 completion; all 31 IDs visibly Complete.</done>
</task>

<task type="auto">
  <name>Task 3: Update .planning/ROADMAP.md — mark Phase 2 [x] Complete; populate the 7-plan listing; update Progress Table</name>
  <files>
    .planning/ROADMAP.md
  </files>
  <read_first>
    - .planning/ROADMAP.md (FULL FILE — find Phase 2 entry; replace `[ ]` with `[x]`; populate Plans listing; update Progress Table row to 7/7)
    - The 7 PLAN.md files in this phase directory — confirm each landed
  </read_first>
  <action>
    **3a. ROADMAP.md Phase 2 entry:**
    Find:
    ```markdown
    - [ ] **Phase 2: Core Foundations + LDA Tier + Parity Harness** - ...
    ```
    Change to:
    ```markdown
    - [x] **Phase 2: Core Foundations + LDA Tier + Parity Harness** - ...
    ```

    **3b. ROADMAP.md Phase 2 Details — Plans listing:**
    Find the `### Phase 2: Core Foundations + LDA Tier + Parity Harness` section. Update the `**Plans**: TBD` line to enumerate the 7 plans:
    ```
    **Plans**: 7 plans across 4 waves (granularity standard; parallelization enabled — Wave 2 runs Plans 02-04 + 02-05 in parallel after Plan 02-03 finishes Wave-1B-3).

    - [x] 02-01-PLAN.md — Wave 0: Surgical xcfun-core cleanup (6 atomic commits per D-09; Mode/Vars/XcError/FunctionalId reorganized)
    - [x] 02-02-PLAN.md — Wave 1A: xtask gates (regen-registry, check-no-mul-add, check-no-anyhow, check-boundaries, check-cubecl-pin, validate wrapper) + auto-generated FUNCTIONAL_DESCRIPTORS / VARS_TABLE / ALIASES (CORE-07/08/09/10, ACC-06, QG-01/02/06/07)
    - [x] 02-03-PLAN.md — Wave 1B-core: xcfun-eval scaffolding — DensVarsDev<F>, build_densvars XC_A_B arm, regularize, Functional + dispatch skeleton (CORE-05/06, MODE-04)
    - [x] 02-04-PLAN.md — Wave 2: 8 pure-density LDA bodies + tier-1 self-tests (LDA-01..08, LDA-09 part 1, ACC-04)
    - [x] 02-05-PLAN.md — Wave 2: TW + VWK kinetic-GGA bodies + XC_A_B_GAA_GAB_GBB builder arm (LDA-09 part 2, LDA-10; Pitfall PHASE2-D fix)
    - [x] 02-06-PLAN.md — Wave 3: validation/ tier-2 parity harness (cc-compiled xcfun-master + FFI shim + 10k grid + report.html/jsonl; ACC-01..03, D-24 LDAERF override)
    - [x] 02-07-PLAN.md — Wave 4: docs/design/ updates + REQUIREMENTS/ROADMAP/STATE sign-off (this plan)
    ```

    **3c. ROADMAP.md Progress Table:**
    Update the row for Phase 2:
    ```
    | 2. Core Foundations + LDA Tier + Parity Harness | 7/7 | Complete | 2026-04-XX |
    ```

    Use today's date for `2026-04-XX` (replace XX with the actual completion day).

    **3d. Commit message:**
    ```
    docs(02-07): Wave-3-3 mark Phase 2 [x] Complete in ROADMAP.md; populate 7-plan listing; Progress Table 7/7
    ```
    Stage `.planning/ROADMAP.md`.
  </action>
  <verify>
    <automated>grep -q '^- \[x\] \*\*Phase 2:' .planning/ROADMAP.md && grep -q '02-01-PLAN.md' .planning/ROADMAP.md && grep -q '02-07-PLAN.md' .planning/ROADMAP.md && grep -q '| 2\. Core Foundations.*| 7/7 | Complete' .planning/ROADMAP.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/ROADMAP.md` Phase 2 entry shows `- [x] **Phase 2:`
    - The Phase 2 Details section's `**Plans**:` line enumerates all 7 plans (02-01 through 02-07) each with `- [x]`
    - The Progress Table row for Phase 2 shows `7/7 | Complete | 2026-04-XX` (with the actual date)
    - Phase 1 row remains `7/7 | Complete | 2026-04-19`
    - Phase 3..7 rows remain `0/0 | Not started | -`
  </acceptance_criteria>
  <done>ROADMAP.md reflects Phase 2 sign-off; 7-plan listing complete; progress table accurate.</done>
</task>

<task type="auto">
  <name>Task 4: Update .planning/STATE.md — progress %, Decisions block, Recent activity, Session continuity → Phase 3</name>
  <files>
    .planning/STATE.md
  </files>
  <read_first>
    - .planning/STATE.md (FULL FILE — current state shows Phase 1 100%; needs Phase 2 100% update + queue Phase 3 in Session Continuity)
    - .planning/ROADMAP.md Phase 3 entry (Goal + Success Criteria — what comes next)
    - The 7 PLAN.md files in this phase directory — to log the per-plan completion + duration
  </read_first>
  <action>
    **4a. STATE.md frontmatter:**
    Update `progress`:
    ```yaml
    progress:
      total_phases: 8
      completed_phases: 2
      total_plans: 14    # Phase 1 (7) + Phase 2 (7)
      completed_plans: 14
      percent: 25
    ```

    Update `last_updated` to today's ISO date.

    **4b. STATE.md "Current Position" section:**
    Replace Phase 1 references with Phase 2 complete + Phase 3 queued:
    - `Phase: 02 (core-foundations-lda-tier-parity-harness) — **COMPLETE (2026-04-XX)**`
    - `Plan: 02-07 complete; Phase 2 signed off. Next: Phase 3 planning (`/gsd-plan-phase 3`).`
    - `Status: Phase 2 done; ready for Phase 3 planning`
    - `Progress: [██▌       ] 25% (14/14 plans across 2 phases complete)`

    **4c. STATE.md "Performance Metrics" table:**
    Append rows for Plans 02-01 through 02-07 with their durations + task counts + file counts (use estimates if exact data not available; record actuals from each Plan's SUMMARY.md if available).

    **4d. STATE.md "Decisions" section:**
    Append a new "Decisions added in Phase 2" sub-section listing:
    - D-01..D-23 (inherited from Phase 2 CONTEXT)
    - D-24 LDAERF tier-2 1e-7 override (USER-APPROVED 2026-04-20 via plan-phase prompt)
    - D-25 XcError::UnknownName drops payload (USER-APPROVED 2026-04-20 via plan-phase prompt)
    - Phase 2 implementation decisions added during execution (e.g., the build_xc_a_b amendment in Plan 02-04 Wave-1B-14a; the Plan 02-06 c_stubs.cpp emitter extension to xtask regen-registry)

    **4e. STATE.md "Active TODOs" + "Blockers":**
    - Active TODOs: `[ ] Plan Phase 3 (/gsd-plan-phase 3)`. Other todos cleared.
    - Blockers: none.

    **4f. STATE.md "Recent activity":**
    Append one line per Plan 02-* completion:
    ```
    - 2026-04-XX: Plan 02-01 complete (Wave 0: 6 atomic commits surgical cleanup of xcfun-core). Hashes: ...
    - 2026-04-XX: Plan 02-02 complete (Wave 1A: 5 xtask gates + 78-entry FUNCTIONAL_DESCRIPTORS + 31-row VARS_TABLE + 0-entry ALIASES). Hashes: ...
    - 2026-04-XX: Plan 02-03 complete (Wave 1B-core: xcfun-eval scaffolding; D-02 cubecl nesting verified). Hashes: ...
    - 2026-04-XX: Plan 02-04 complete (Wave 2: 9 LDA kernels + tier-1 self-tests pass < 5s; LDA-01..09 marked [x]). Hashes: ...
    - 2026-04-XX: Plan 02-05 complete (Wave 2: TW + VWK kinetic-GGA + XC_A_B_GAA_GAB_GBB builder arm; LDA-10 marked [x]). Hashes: ...
    - 2026-04-XX: Plan 02-06 complete (Wave 3: validation/ tier-2 harness; user-approved D-24 LDAERF 1e-7 override transparent in report.html; ACC-01..04 marked [x]). Hashes: ...
    - 2026-04-XX: Plan 02-07 complete — Phase 2 signed off. All 31 Phase-2 IDs marked [x]; ROADMAP Phase 2 marked [x]; STATE updated. Hashes: ...
    ```

    **4g. STATE.md "Session Continuity":**
    Update:
    ```
    **Last session stopped at:** Plan 02-07 complete; Phase 2 signed off (14/14 plans; 25% project progress).
    All 31 Phase-2 requirement IDs marked [x] in REQUIREMENTS.md.
    Tier-2 parity harness GREEN at all per-functional thresholds (8 LDAs at 1e-12; 3 LDAERF at 1e-7 per D-24).

    **Next action:** /gsd-plan-phase 3 to begin Phase 3 (GGA Tier + Mode::Potential).
    Phase 3 ports the 45 GGA functionals + Mode::Potential + extends MODE-01 to orders 3..=4.
    Builds atop the Phase 2 substrate: xcfun-eval Functional + dispatch_kernel + DensVarsDev (extend with new variant arms);
    extends regen-registry to populate the GGA descriptors; extends validation/build.rs to cc-compile GGA C++ bodies.

    **Related artifacts:**
    - .planning/phases/02-core-foundations-lda-tier-parity-harness/02-{01..07}-SUMMARY.md
    - validation/report.html (tier-2 results)
    - crates/xcfun-eval/src/functionals/lda/* (11 LDA kernels)
    ```

    **4h. Commit message:**
    ```
    docs(02-07): Wave-3-4 STATE.md sign-off — 25% project progress, 14/14 plans, D-24/D-25 decisions recorded, Session continuity → Phase 3
    ```
    Stage `.planning/STATE.md`.
  </action>
  <verify>
    <automated>grep -q 'completed_phases: 2' .planning/STATE.md && grep -q 'percent: 25' .planning/STATE.md && grep -q 'Plan 02-07 complete' .planning/STATE.md && grep -q 'gsd-plan-phase 3' .planning/STATE.md && grep -q 'D-24' .planning/STATE.md && grep -q 'D-25' .planning/STATE.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/STATE.md` frontmatter `progress.completed_phases` is `2`, `completed_plans` is `14`, `percent` is `25` (rounded; 14/56 = 25%)
    - Current Position section reflects Phase 2 COMPLETE + Phase 3 queued
    - Performance Metrics table has rows for Plans 02-01 through 02-07
    - Decisions section appends D-24 + D-25 (with USER-APPROVED 2026-04-20 attribution)
    - Active TODOs list contains "Plan Phase 3" as the next action
    - Recent activity logs each Plan 02-* completion (7 entries)
    - Session Continuity points to `/gsd-plan-phase 3` as next action; mentions Phase 3 is "GGA Tier + Mode::Potential"
  </acceptance_criteria>
  <done>STATE.md fully reflects Phase 2 sign-off; project context advanced to Phase 3 planning ready.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation accuracy vs. as-built code | Risk: docs drift from code (the very problem this plan solves). Mitigated by Plan 02-07 explicitly cross-referencing each doc update against the as-built source files (`crates/xcfun-eval/src/density_vars.rs` etc.). |
| REQUIREMENTS.md / ROADMAP.md / STATE.md as planning sources of truth for Phase 3 | If Phase 2 IDs remain `[ ]` after sign-off, Phase 3's planning sweep would re-plan completed work. Mitigated by Task 2 explicitly marking all 31 Phase-2 IDs Complete + Task 4 advancing Session Continuity to Phase 3. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07-01 | Repudiation | Phase 2 sign-off without verifying tier-2 GREEN | mitigate | Plan 02-06 Task 7 user-checkpoint precedes this plan; Plan 02-07 cannot land until checkpoint approves. |
| T-02-07-02 | Tampering | An ID is marked [x] without actually being delivered | mitigate | Each Plan 02-* SUMMARY.md records the IDs delivered. Plan 02-07 Task 2 cross-references SUMMARYs against the IDs being marked. |
| T-02-07-03 | Information disclosure | docs/design/12-design-decisions.md may need a SUPERSEDED banner for D2 (Num retired) — was this updated in Phase 1? | accept | Phase 1 Plan 01-07 updated docs/design/12 with D1/D2/D4 SUPERSEDED banners (committed db792bf). Phase 2 doesn't introduce new SUPERSEDED-class decisions; D-24 + D-25 are new decisions, not supersedings. No additional 12-design-decisions.md update strictly required (though optional). |

Severity: All threats LOW; this is a documentation plan with no execution-time risk.
</threat_model>

<verification>
After all four tasks land:

1. `git log --oneline -5` shows the 4 sign-off commits.
2. `grep -c '\- \[x\] \*\*' .planning/REQUIREMENTS.md` returns ≥ 37 (Phase 1 6 IDs + Phase 2 31 IDs).
3. `grep '^- \[x\] \*\*Phase 2' .planning/ROADMAP.md` returns 1 line.
4. `grep 'percent: 25' .planning/STATE.md` returns 1 line.
5. `cargo build --workspace` still exits 0 (no docs change should break the build).
6. `cargo test --workspace` still exits 0 (the 30+ tests across xcfun-core + xcfun-eval + xcfun-ad + validation pass).
7. `cargo run -p xtask --bin validate -- --backend cpu --order 2 --filter lda` still exits 0 (Phase 2 SC #5 still met).
</verification>

<success_criteria>
- 4 design docs updated (02 §5, 05 §2, 06 §3, 09 testing) to reflect Phase 2 as-built
- All 31 Phase-2 requirement IDs marked `[x]` Complete in REQUIREMENTS.md (CORE-01..10, LDA-01..10, MODE-04, ACC-01..06, QG-01/02/06/07)
- ROADMAP.md Phase 2 marked `[x]` Complete with full 7-plan listing + Progress Table 7/7
- STATE.md advanced: 25% project progress, D-24/D-25 decisions logged, Session Continuity → Phase 3
- Workspace build + test + tier-2 validate gate all still GREEN (no doc-update regressions)
- Phase 2 fully signed off; ready for `/gsd-plan-phase 3` (GGA Tier + Mode::Potential)
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-foundations-lda-tier-parity-harness/02-07-SUMMARY.md` documenting:
- Commit hashes for each Wave-3 sub-task (Wave-3-1 through Wave-3-4)
- Confirmation: ROADMAP, REQUIREMENTS, STATE all updated; design docs reflect as-built
- Phase 2 retrospective summary: what worked, what was harder than expected, what to carry into Phase 3
- Pointer to Phase 3 planning (`/gsd-plan-phase 3`); summarise Phase 3 prerequisites: extend regen-registry to populate 45 GGA descriptors; extend validation/build.rs to cc-compile GGA bodies; add MODE-01 orders 3..=4 + MODE-02 Potential
- Any final notes on the Phase 2 architecture for future reference (e.g., the cubecl monomorphization budget headroom; the per-functional threshold dispatch pattern)
</output>
