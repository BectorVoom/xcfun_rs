---
context: phase
phase: 04-metagga-tier-mode-contracted-aliases
task: 10
total_tasks: 10
status: paused
last_updated: "2026-04-28T07:41:41+0900"
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure
> in this session or prior sessions. Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: **`gsd-executor` worktree subagents can stall silently for hours when running long-duration shell tasks** — discovered when the 04-07 subagent ran for 6h+ after committing Tasks 7.1+7.2 with no completion signal. Structural mitigation: for any task with a `cargo run` / sweep / build command expected to exceed ~20 min, do NOT run inside an isolated `gsd-executor` worktree. Run inline in the main working tree.

- [ ] CONSTRAINT: **The validation binary's all-or-nothing jsonl buffering destroys data on any non-clean exit** — confirmed twice in this session. SCAN-family C++ tmath_die crash at hour 4 → 0 records. WSL termination at hour 1.5 → 0 records. The buffer-then-flush pattern in `validation/src/main.rs` writes the entire jsonl on `Drop` of the report struct; any panic, abort, signal, or VM kill loses the buffer entirely. **Mitigation required before next sweep attempt: patch the binary to flush per-functional or per-record incrementally.** Without this fix, no order-3 sign-off sweep can complete reliably on this WSL setup.

- [ ] CONSTRAINT: **WSL VM lifetime is not guaranteed for ~5h sweeps on this machine** — confirmed via WSL termination during attempt 2. Even `nohup` does not survive WSL VM shutdown. Plan around shorter scopes (chunked sweeps) or accept that long sweeps need supervision and possibly host-level WSL keepalive.

**Do not proceed until all three constraints are acknowledged.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| `gsd-executor` long-task stall | Worktree subagent's session crashes mid-task, leaving the orchestrator blocked indefinitely with no completion signal. | blocking | For tasks expected to run >20 min, run inline in main working tree with `run_in_background=true` + paired Monitor. Never delegate long shell tasks to worktree subagents. |
| Validation binary's all-or-nothing jsonl flush | `cargo run -p validation` buffers ALL records in memory and only writes `validation/report.jsonl` on clean process exit. A panic, abort, timeout kill, or WSL termination mid-run produces zero usable output. **TWO consecutive sweep failures in this session lost ~6 hours of work this way.** | blocking | Patch `validation/src/main.rs` to add incremental jsonl line writing (per-record or per-functional flush). Without this fix, NO long sweep can complete safely. Filed as a hard prerequisite for re-attempting Plan 04-10 Task 10.1. |
| C++ `tmath::log_expand` aborts the validation process | C++ reference's `tmath.hpp:143` asserts `x0 > 0`; non-positive intermediates abort the *entire* validation binary, not just one functional. Hit on XC_BLOCX in prior session. | advisory | Add the offending functional to `validation/src/driver.rs:475` `excluded_by_upstream_spec` skip-list, re-run. |
| C++ `tmath::sqrt_expand` aborts the validation process | C++ reference's `tmath.hpp:165` asserts `x0 > 0` for sqrt. Hit on XC_SCANC in this session — the SCAN-family shared substrate `xcfun-master/src/functionals/SCAN_like_eps.hpp` has 17 sqrt() call-sites that all 10 SCAN-family functionals inherit. **All 10 SCAN-family entries (SCANx, SCANc, rSCANx, rSCANc, rppSCANx, rppSCANc, r2SCANx, r2SCANc, r4SCANx, r4SCANc) added to skip-list at master HEAD f968c32.** | advisory | Source-level evidence (shared header substrate) justified the pre-emptive batch skip. Mirrors BR/CSC/BLOCX precedent. Phase 6 will land guarded sqrt expansion or custom JP-grid harness. |

<current_state>
**Phase 4 gap closure status:**

| Wave | Plan | Status | Notes |
|------|------|--------|-------|
| 1 | 04-07 driver-extension | ✓ COMPLETE (prior session) | Commits `0576afb`, `a61e572`, `1332d29` |
| 1 | 04-08 erf-divergence | ✓ COMPLETE (prior session) | Commits `74b38fa`, `6b8365c`, merge `39c6630` |
| Wave-1 tracking | — | ✓ COMPLETE (prior session) | Commit `ea0c776` |
| 2 | 04-09 contracted-metaGGA | ✓ **COMPLETE (this session)** | 30 cross-mode tests GREEN at strict 1e-12 (TPSSX/SCANX/M06X orders 0..=3); orders 4 metaGGA `#[ignore]`d with explicit Phase-6 forward citation per Plan 04-05 D-19. Commits `307616c`, `4c0a376`, `26db669`, merge `a34ecb6`, tracking `9cdde47` |
| 3 | 04-10 resignoff | ◆ **PAUSED — sweep failed twice** | See "remaining_work" below |

**Master HEAD: `f968c32`** — `fix(04-10): skip-list SCAN family — C++ sqrt_expand abort on shared substrate`. No worktrees outstanding. Working tree clean.

Wave 3 (Plan 04-10) is **NOT COMPLETE**. The Phase-4 sign-off cannot land until the validation binary is patched to support incremental jsonl flush (or a chunked-sweep strategy is adopted).
</current_state>

<completed_work>

Completed Tasks (this session):

**Wave 2 — Plan 04-09 (gap closure: contracted metaGGA cross-mode):**
- Spawned `gsd-executor` in worktree (isolation="worktree"). 3 commits landed:
  - `307616c` test(04-09): metaGGA cross-mode parity at orders 0..=3 (TPSSX, SCANX, M06X)
  - `4c0a376` docs(04-09): update MODE-03 to reflect Plan 04-09 metaGGA cross-mode coverage
  - `26db669` docs(04-09): complete contracted metaGGA cross-mode plan summary
- Merged worktree onto master with orchestrator-file backup-restore protocol: `a34ecb6 chore: merge executor worktree (worktree-agent-a0f4c3dff8d143730) — Plan 04-09`
- Post-merge test gate GREEN: `cargo test -p xcfun-eval --test contracted_cross_mode --features testing` → **30 passed / 0 failed / 3 ignored** (236s; orders 4 metaGGA `#[ignore]`d as Phase-6 forward).
- ROADMAP/STATE plan-progress update for Plan 04-09: `9cdde47 docs(phase-04): update tracking after wave 2 (gap closure plan 04-09)`

**Pre-flight gates for Plan 04-10 (Task 10.1 step 1) — all GREEN:**
- ✓ `cargo run -p xtask --bin regen-registry --check`: OK (no drift)
- ✓ `cargo run -p xtask --bin check-no-anyhow`: PASS (7 library crates)
- ✓ `cargo run -p xtask --bin check-no-mul-add`: PASS (109 files)
- ✓ `cargo test -p xcfun-eval --test self_tests --features testing`: 1/1 pass (58s)
- ✓ `cargo test -p xcfun-eval --test contracted_cross_mode --features testing`: 30/0/3 (236s)
- ✓ `cargo test -p xcfun-eval --test alias_canary`: 16/16 pass (incl. 4 plan-required canaries)

**Plan 04-10 Task 10.1 step 4 — alias canary tests for sign-off ledger — GREEN:**
- ✓ test_camcompx_negative_weight, test_b3lyp_additive_accumulation, test_exx_parameter_overwrite, test_case_insensitive — all pass.

**Cleanup from prior session:**
- Removed stale worktree `agent-a5d18aae0c0c0966e` and its lock; deleted branch.
- Untracked accidentally-committed worktree submodule pointer; added `.claude/worktrees/` to `.gitignore`. Commit `2355a5d`.

**SCAN-family C++ fault discovery and pre-emptive skip-list extension:**
- Sweep attempt 1 crashed at XC_SCANC with `tmath::sqrt_expand: Assertion x0 > 0 failed` (xcfun-master/external/upstream/taylor/tmath.hpp:165) at the 53/76 mark after ~4h.
- Source-level diagnosis: `xcfun-master/src/functionals/SCAN_like_eps.hpp` contains 17 sqrt() call-sites, inherited by ALL 10 SCAN-family functionals. Pre-emptive batch skip justified.
- Commit `f968c32`: skip-listed all 10 SCAN-family entries in `validation/src/driver.rs`. Mirrors BR/CSC/BLOCX precedent.
</completed_work>

<remaining_work>

**Plan 04-10 (Wave 3 resignoff) — PAUSED:**

Task 10.1 step 2 (full-matrix tier-2 sweep at order 3) failed twice:
- Attempt 1 (~4h): SCANC crash → 0 records (now mitigated by skip-list at master HEAD).
- Attempt 2 (~1.5h): WSL terminated → 0 records (root cause: validation binary buffer-then-flush).

**Hard prerequisite before re-attempting Task 10.1 step 2:**
- Patch `validation/src/main.rs` (or wherever the report writer lives) to support incremental jsonl line writing — flush per-record or per-functional. Without this fix, any crash/kill loses the entire buffer, and the order-3 sweep is structurally too long to complete reliably on this WSL setup.

**Once incremental flush is wired, remaining Plan 04-10 work is:**

Task 10.1:
- Step 2: full-matrix tier-2 order-3 sweep (`cargo run -p validation --release -- --backend cpu --order 3 --filter '.*'`). Expected runtime: ~3-5h with SCAN family pre-skipped (LDAs ~10 min, GGAs ~2.5h, TPSS family ~50 min, BR/CSC/BLOCX/SCAN excluded instant, M05/M06 ~110 min if they don't crash).
- Step 3: contracted spot-checks at orders 5/6 SLATERX + PBEX (~5 min total).
- Step 5: commit `validation/report.html` + capture `/tmp/04-10-failure-ledger.txt`.

**Watch-list during Step 2 sweep:** if M05/M06 family aborts with another C++ tmath_die (pow_expand, log_expand, sqrt_expand, atan_expand), add the offender to `validation/src/driver.rs:475`'s skip-list and continue. M05/M06 .cpp files have no direct log/sqrt/atan/asinh calls; their shared header `m0xy_fun.hpp` uses `pow(rho, frac)` only on positive base — LOW risk but not zero.

Task 10.2 (`checkpoint:human-verify`):
- Open `validation/report.html`, confirm capstone timestamp, ≥76 functionals iterated, every NEW failing row matches the documented D-19 forward list, BR/CSC/BLOCX/SCAN rows display as `EXCLUDED (excluded_by_upstream_spec)`.
- Run `jq -s 'group_by(.functional)...'` queries to confirm top-10 worst max_rel_err matches expected D-19 set.

Task 10.3:
- REQUIREMENTS.md: 12 Phase-4 IDs (MGGA-01..05, MODE-03, ALIAS-01..06) `[ ]` → `[x]` or `[~]`.
- ROADMAP.md: Phase 4 → Complete.
- STATE.md: Accumulated Context append; `progress.completed_phases: 4`.

Task 10.4:
- Rewrite 04-VERIFICATION.md to `signed_off` with `must_haves_verified: 6 / 6`.

**Phase verification gates after Plan 04-10:**
- Code review (Skill `gsd:code-review`)
- Regression gate (prior phases' tests)
- Schema/codebase drift gates
- gsd-verifier on phase goal
- ROADMAP/STATE phase-complete update

**Expected D-19 forward list for Phase 4 (consolidated):**
- 13 inherited Phase-3 forwards: PW86X, APBEX, APBEC, P86C, PW91C, B97C, B97_1C, B97_2C, SPBEC, PBEINTC, PW91K, P86CORRC, BECKESRX
- 3 NEW Phase-4 ERF forwards (Plan 04-08): LDAERFX, LDAERFC, LDAERFC_JT
- 2 NEW Phase-4 LDA-corr forwards (Plan 04-08): VWN_PBEC, PBEC
- 0..N NEW Phase-4 metaGGA forwards (TBD by sweep — TPSS family + M05/M06 if they pass; SCAN family is excluded_by_upstream_spec)
- excluded_by_upstream_spec (5+10=15 functionals): BR×3 + CSC + BLOCX + SCAN×10
- Mode::Contracted orders 5..=6 metaGGA (Plan 04-05 D-19, reinforced by 04-09)
</remaining_work>

<decisions_made>

- **Pre-emptively skip-list all 10 SCAN-family functionals (commit `f968c32`)** — source-level evidence (`SCAN_like_eps.hpp` shared substrate, 17 sqrt() call-sites) justified batch skip after the SCANC crash. Mirrors the BR/CSC/BLOCX precedent for "shared metaGGA C++ substrate causes C++ tmath_die at low-density tail." Phase 6 will land the proper fix.
- **Pause work after second sweep failure rather than attempt a third blind retry** — user-approved decision (CHECKPOINT in this session). The structural fix (incremental jsonl flush in validation binary) must land before another sweep attempt, otherwise any crash/kill loses the entire buffer again.
- **Treat user's `--gaps` as the workflow's `--gaps-only` flag** (inherited from prior session) — unambiguous given the gap-closure plan setup.
- **Wave 2 (Plan 04-09) shipped via worktree-isolated gsd-executor** — `autonomous: true` plan, no long-running shell tasks, safe for worktree spawn. Not subject to the gsd-executor stall anti-pattern.
- **Wave 3 (Plan 04-10) ran inline on main working tree** per the blocking anti-pattern from prior handoff — long sweep tasks must NOT be delegated to worktree subagents.
</decisions_made>

<blockers>

**Blocker 1: Validation binary's all-or-nothing jsonl flush.** Two consecutive failures (~6h total) with 0 recoverable records. Until the binary supports incremental flush, no full order-3 sweep can complete reliably on this WSL setup. Resolution: patch `validation/src/main.rs` to write per-functional or per-record. Estimate: 30 min code change + verification.

**Blocker 2 (potential): WSL VM stability for 5h workloads.** Even with incremental flush, sweeps spanning multiple hours risk WSL termination. Mitigation: chunked --filter strategy (per-family), each ~30-90 min, resilient to host events.
</blockers>

## Required Reading (in order)
1. `.planning/phases/04-metagga-tier-mode-contracted-aliases/04-VERIFICATION.md` — original `gaps_found` verdict.
2. `.planning/phases/04-metagga-tier-mode-contracted-aliases/04-07-driver-extension-SUMMARY.md` — Wave 1's Gap 1 closure + BLOCX skip discovery.
3. `.planning/phases/04-metagga-tier-mode-contracted-aliases/04-08-erf-divergence-SUMMARY.md` — Wave 1's Gap 2 verdict (3 ERF Path B, 2 NEW LDA-corr D-19 forwards).
4. `.planning/phases/04-metagga-tier-mode-contracted-aliases/04-09-contracted-metagga-SUMMARY.md` — Wave 2 outcome (30 metaGGA cross-mode tests GREEN; orders 4 forwarded to Phase 6).
5. `.planning/phases/04-metagga-tier-mode-contracted-aliases/04-10-resignoff-PLAN.md` — Wave 3 — what to do once the binary is patched.
6. `validation/src/driver.rs:444-510` — current C++-abort skip-list including BLOCX + all 10 SCAN family entries.
7. `validation/src/main.rs` — entry point that needs the incremental-flush patch (see Blocker 1).

## Critical Anti-Patterns (do NOT repeat these)

- ANTI-PATTERN: **delegating Task-10.1-style long sweeps to worktree subagents** → run inline in main working tree.
- ANTI-PATTERN: **assuming `cargo run -p validation` writes incrementally** → it doesn't; ANY crash/kill loses the entire buffer. Confirmed twice this session.
- ANTI-PATTERN: **assuming WSL stays up indefinitely for ~5h workloads** → it doesn't on this machine. Confirmed once this session.
- ANTI-PATTERN: **trusting an `exit 0` from `cargo run | tee` pipelines as "the binary completed cleanly"** → tee returns 0 even if the upstream binary was killed. Check `report.jsonl` mtime/size to confirm the binary actually wrote output.
- ANTI-PATTERN: **iterating crash-and-skip sweeps blindly** → each iteration costs hours with no recoverable data. Diagnose at C++ source-level first (e.g. shared header substrate), batch-skip the family, then run.

## Infrastructure State

- **Master HEAD:** `f968c32` (SCAN family skip-list)
- **Worktrees:** none. Working tree clean.
- **`validation/report.jsonl`:** 1.6 GB, mtime Apr 26 23:06 — STALE leftover from a pre-Plan-04-07 order-3 run (does NOT iterate metaGGAs). Gitignored.
- **`validation/report.html`:** also stale, last touched by failed sweep attempts. Both files will be overwritten on the next clean sweep exit.
- **Background processes:** none (WSL termination cleared all PIDs).

## Pre-Execution Critique Required

**Yes — before re-attempting Plan 04-10 Task 10.1 step 2, the following must be true:**

1. The validation binary writes `report.jsonl` incrementally (per-functional or per-record). A targeted unit test should confirm a deliberately-killed run still produces partial valid jsonl. Without this, any sweep attempt is a coin flip on hours of work.

2. Either: (a) the order-3 sweep is decomposed into chunked --filter runs of ≤90 min each, OR (b) WSL stability for ~5h has been improved (e.g., disable Windows Update reboots during the run, increase WSL memory limits, etc.).

<context>
Wave 2 went smoothly via worktree-isolated executor — 30 cross-mode tests passed at strict 1e-12. Wave 3 (the actual sign-off sweep) hit two distinct failures that, in retrospect, share a root cause: the validation binary cannot survive any non-clean exit because it buffers all jsonl records and writes only on clean Drop.

The first crash (SCANC sqrt_expand) was a content-side issue — the C++ harness has its own bug at low-density grid points for SCAN family, mirroring the BLOCX log_expand and BR/CSC inlen-11 issues already known. Pre-emptive batch skip was the right call once we saw the shared substrate evidence in SCAN_like_eps.hpp.

The second termination (WSL kill) was a platform-side issue — the host VM didn't survive a 1.5h workload. Even with infinite patience and a perfect binary, the WSL setup on this machine cannot be assumed stable for 5h sweeps.

Combining the two: until the validation binary is hardened against non-clean exit (incremental flush), the Phase 4 sign-off is structurally blocked. The user wisely chose to pause rather than attempt a third blind retry. The skip-list extension that landed (commit f968c32) is the one durable artifact from the failed sweep attempts.

Resumption guidance:
1. Land an incremental-flush patch on the validation binary (small targeted change to `validation/src/main.rs` or `validation/src/driver.rs::run` to write each ReportRecord as it's produced rather than on Drop). 
2. (Optional but recommended) Add a `--filter` chunk-strategy doc note so future sweeps can be split if WSL stability is a concern.
3. Re-run Plan 04-10 Task 10.1 step 2.
4. Continue with Task 10.2 (human-verify checkpoint), Task 10.3 (ledger updates), Task 10.4 (VERIFICATION.md rewrite).
5. Phase verification gates (code review, regression, drift, gsd-verifier).
</context>

<next_action>
**Option A (recommended): Land an incremental-flush patch first, then re-run Plan 04-10.**

```
# In a fresh session:
/gsd:debug 04-10-incremental-jsonl-flush
# Or directly: edit validation/src/main.rs to flush per-record;
# add a unit test that kill -9 the process mid-run leaves a valid partial jsonl;
# commit; then:
/gsd:execute-phase 4 --gaps-only
```

The orchestrator will:
1. Detect 04-07/08/09 complete (skip via has_summary=true).
2. Filter to 04-10 (gap_closure: true, wave: 3, autonomous: false).
3. Spawn 04-10 inline (per blocking anti-pattern). With incremental flush, even a partial run produces useful data.
4. Continue to verification gates after Task 10.4.

**Option B: Sign off as `signed_off_with_caveats` without re-running the sweep.**

Only valid if the team explicitly accepts that "full-matrix tier-2 sweep at order 3" is a Phase-6 prerequisite (not a Phase-4 deliverable). This requires re-scoping the must_have list. Edit `04-VERIFICATION.md` to reflect 5/6 must_haves verified + 1 deferred to Phase 6. NOT recommended without team alignment — the must_have was explicitly named in 04-06.
</next_action>
