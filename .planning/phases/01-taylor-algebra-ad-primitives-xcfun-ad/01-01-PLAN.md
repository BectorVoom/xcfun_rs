---
phase: 01-taylor-algebra-ad-primitives-xcfun-ad
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - Cargo.toml
  - crates/xcfun-ad/Cargo.toml
  - crates/xcfun-ad/src/lib.rs
  - crates/xcfun-ad/src/index.rs
  - crates/xcfun-ad/src/for_tests/mod.rs
  - crates/xcfun-ad/src/for_tests/cpu_client.rs
  - crates/xcfun-ad/src/for_tests/raw_eval_scalar.rs
  - crates/xcfun-ad/tests/cubecl_spike.rs
  - xtask/Cargo.toml
  - xtask/src/main.rs
  - xtask/src/bin/regen_ad_fixtures.rs
  - xtask/assets/regen_ad_fixtures/driver.cpp
  - .planning/phases/01-taylor-algebra-ad-primitives-xcfun-ad/01-01-SUMMARY.md
  - .planning/phases/01-taylor-algebra-ad-primitives-xcfun-ad/01-RESEARCH.md
  - .planning/phases/01-taylor-algebra-ad-primitives-xcfun-ad/01-VALIDATION.md
  - .planning/phases/01-taylor-algebra-ad-primitives-xcfun-ad/01-02-PLAN.md
  - .planning/phases/01-taylor-algebra-ad-primitives-xcfun-ad/01-03-PLAN.md
  - .planning/phases/01-taylor-algebra-ad-primitives-xcfun-ad/01-05-PLAN.md
  - .planning/phases/01-taylor-algebra-ad-primitives-xcfun-ad/01-07-PLAN.md
files_removed:
  # Reverted by Task 1 `git revert` chain (pre-pivot hand-Rust work; D-21/D-22).
  # These paths cease to exist after the revert; they are NOT "modified" — they are deleted.
  - crates/xcfun-ad/src/ctaylor.rs
  - crates/xcfun-ad/src/valid_n.rs
  - crates/xcfun-ad/src/for_tests.rs
  - crates/xcfun-ad/src/expand/mod.rs
  - crates/xcfun-ad/src/expand/inv.rs
  - crates/xcfun-ad/src/expand/exp.rs
  - crates/xcfun-ad/src/expand/log.rs
  - crates/xcfun-ad/src/expand/pow.rs
  - crates/xcfun-ad/src/expand/sqrt.rs
autonomous: true
requirements: [AD-01]

must_haves:
  truths:
    - "Pre-pivot hand-Rust code in crates/xcfun-ad/src/ is removed via git revert, leaving a clean slate for the cubecl port."
    - "Workspace Cargo.toml declares cubecl =0.10.0-pre.3 and cubecl-cpu =0.10.0-pre.3 as pinned dependencies."
    - "xcfun-ad crate has cubecl + cubecl-cpu declared, feature `cpu` activates cubecl-cpu, feature `testing` gates the for_tests module."
    - "A cubecl-cpu smoke test compiles and runs: a #[cube] fn adds two numbers on CpuRuntime and returns the expected result."
    - "for_tests::cpu_client() returns a &'static CpuClient via OnceLock, and raw_eval_scalar launches a 1-thread kernel."
    - "xtask workspace member exists with a regen-ad-fixtures binary skeleton and a C++ driver stub that compiles."
    - "01-01-SUMMARY.md in phases/01-taylor-algebra-ad-primitives-xcfun-ad/ is marked 'SUPERSEDED BY CUBECL PIVOT' in its header per D-22."
    - "01-RESEARCH.md has a SUPERSEDED banner (per D-27 continuation) and every downstream PLAN.md has its `@01-RESEARCH.md` reference removed so executors do not consume pre-pivot guidance."
    - "01-VALIDATION.md frontmatter has `nyquist_compliant: true` and the Validation Sign-Off checkbox is ticked — every Phase 1 task ships with an `<automated>` verify."
  artifacts:
    - path: "Cargo.toml"
      provides: "workspace adds cubecl and cubecl-cpu pins; workspace members include xtask"
      contains: "cubecl = \"=0.10.0-pre.3\""
    - path: "crates/xcfun-ad/Cargo.toml"
      provides: "declares cubecl, cubecl-cpu deps, features cpu/testing"
      contains: "cubecl = { workspace = true }"
    - path: "crates/xcfun-ad/src/lib.rs"
      provides: "crate root post-revert with #![forbid(unsafe_code)] and module declarations (stubs only)"
      contains: "forbid(unsafe_code)"
    - path: "crates/xcfun-ad/src/for_tests/cpu_client.rs"
      provides: "OnceLock<CpuClient> singleton for cubecl-cpu"
      contains: "OnceLock"
    - path: "crates/xcfun-ad/src/for_tests/raw_eval_scalar.rs"
      provides: "helper that launches a 1-thread cubecl-cpu kernel and reads back the result buffer"
      contains: "launch_unchecked"
    - path: "crates/xcfun-ad/tests/cubecl_spike.rs"
      provides: "first working #[cube] fn + CpuRuntime kernel launch proving the substrate works"
      contains: "#[cube"
    - path: "xtask/Cargo.toml"
      provides: "xtask workspace member with cc + anyhow app-boundary deps"
      contains: "cc = { workspace = true"
    - path: "xtask/src/bin/regen_ad_fixtures.rs"
      provides: "xtask subcommand skeleton that will drive the C++ fixture emitter in Plan 05"
      contains: "regen_ad_fixtures"
    - path: "xtask/assets/regen_ad_fixtures/driver.cpp"
      provides: "C++ driver stub that compiles against xcfun-master/external/upstream/taylor/"
      contains: "#include"
    - path: ".planning/phases/01-taylor-algebra-ad-primitives-xcfun-ad/01-RESEARCH.md"
      provides: "SUPERSEDED banner at top of file marking document as historical"
      contains: "SUPERSEDED 2026-04-19"
    - path: ".planning/phases/01-taylor-algebra-ad-primitives-xcfun-ad/01-VALIDATION.md"
      provides: "nyquist_compliant flag flipped to true + sign-off checkbox ticked"
      contains: "nyquist_compliant: true"
  key_links:
    - from: "crates/xcfun-ad/tests/cubecl_spike.rs"
      to: "crates/xcfun-ad/src/for_tests/cpu_client.rs::cpu_client"
      via: "xcfun_ad::for_tests::cpu_client()"
      pattern: "for_tests::cpu_client"
    - from: "Cargo.toml [workspace.dependencies]"
      to: "cubecl / cubecl-cpu crates.io pins"
      via: "version = \"=0.10.0-pre.3\""
      pattern: "=0\\.10\\.0-pre\\.3"
    - from: "01-01-PLAN.md through 01-07-PLAN.md"
      to: "01-RESEARCH.md (historical)"
      via: "SUPERSEDED banner + removed @01-RESEARCH.md context references"
      pattern: "SUPERSEDED 2026-04-19"
---

<objective>
Wave 0 scaffolding for the cubecl-native `xcfun-ad` rewrite. Revert the pre-pivot hand-Rust commits (per D-21/D-22), add cubecl 0.10-pre.3 dependencies to the workspace and crate manifests, prove cubecl-cpu works end-to-end with a minimal `#[cube] fn` smoke test, and lay down the `for_tests` harness (`OnceLock<CpuClient>` + `raw_eval_scalar`) plus the `xtask` workspace member that Plan 05 will use to generate golden fixtures. Also perform the **planning-artifact hygiene pass** mandated by the checker review: flag `01-RESEARCH.md` as SUPERSEDED, remove stale `@01-RESEARCH.md` references from every plan's `<context>` block, and flip `01-VALIDATION.md` `nyquist_compliant` to `true`.

Purpose: Phase 1 is the **first `#[cube]` code in the repo** (PATTERNS.md §"No Analog Found"). Before any `ctaylor_*` port is written, we must (1) clean out the pre-pivot code that would conflict, (2) prove the cubecl-cpu substrate actually lowers a kernel to an MLIR-JITed function on this machine, (3) publish the shared test-harness helpers every downstream plan will consume, AND (4) neutralise the pre-pivot 01-RESEARCH.md document so executors downstream do not receive contradictory guidance.

Output: Clean `crates/xcfun-ad/src/` (only root `lib.rs` + stubs + `for_tests/` + `index.rs`), a green `cargo test -p xcfun-ad --test cubecl_spike --features cpu,testing`, a scaffolded `xtask` crate whose `regen-ad-fixtures` binary is wired (C++ compile succeeds, emits no records yet), 01-RESEARCH.md with a SUPERSEDED banner, every `*-PLAN.md` free of `@01-RESEARCH.md` references, and 01-VALIDATION.md frontmatter flag flipped.
</objective>

<execution_context>
@/home/chemtech/workspace/xcfun_rs/.claude/get-shit-done/workflows/execute-plan.md
@/home/chemtech/workspace/xcfun_rs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-taylor-algebra-ad-primitives-xcfun-ad/01-CONTEXT.md
@.planning/phases/01-taylor-algebra-ad-primitives-xcfun-ad/01-PATTERNS.md
@.planning/phases/01-taylor-algebra-ad-primitives-xcfun-ad/01-VALIDATION.md
@CLAUDE.md
@.cargo/config.toml

<interfaces>
<!-- cubecl 0.10-pre.3 public surface the executor needs. Do not go codebase-exploring for these - use them directly. -->

From `cubecl::prelude::*`:
```rust
// Kernel attribute macros
#[cube]                      // Marks a fn as cubecl kernel body
#[cube(launch_unchecked)]    // Generates a launch_unchecked::<R>(...) wrapper

// Trait bounds for kernels
pub trait Float: /* cubecl-internal bounds */ { ... }   // F::new(literal), F::exp, F::log, F::sqrt, F::powf, F::erf, F::cast_from
pub trait Runtime { type Device; ... }
pub trait CubeType { ... }

// Kernel-scope types
pub struct Array<F> { /* indexable a[i]; len() */ }

// Host-side launch primitives
pub struct CubeCount::Static(x: u32, y: u32, z: u32);
pub struct CubeDim { ... }   // CubeDim::new(x, y, z)
pub struct ArrayArg<R: Runtime> { ... }
impl<R: Runtime> ArrayArg<R> { pub fn from_raw_parts::<F>(handle, len, vectorization) -> Self; }

// Runtime client (R::client(&device) -> client)
// client.empty(bytes) -> handle
// client.create(bytes_slice) -> handle
// client.read_one(handle.binding()) -> Vec<u8>
// Bytes <-> typed conversion: F::as_bytes(&slice), F::from_bytes(&bytes)

// Comptime attribute on function parameters
#[comptime] n: u32           // treated as compile-time constant inside the kernel body
```

From `cubecl_cpu` (crate name `cubecl-cpu`):
```rust
pub struct CpuRuntime;
impl Runtime for CpuRuntime {
    type Device = cubecl_cpu::CpuDevice;
    // ...
}
pub struct CpuDevice; // Default::default() selects the default CPU device
// pub type CpuClient = <CpuRuntime as Runtime>::Client;  // exact alias TBD at impl time per D-17
```

From `std::sync`:
```rust
pub struct OnceLock<T> { /* ... */ }
impl<T> OnceLock<T> {
    pub const fn new() -> Self;
    pub fn get_or_init<F: FnOnce() -> T>(&self, f: F) -> &T;
}
```

Example launch pattern (from cubecl README, adapted):
```rust
let client = CpuRuntime::client(&CpuDevice::default());
let input = [1.0_f64, 2.0];
let in_handle = client.create(f64::as_bytes(&input));
let out_handle = client.empty(core::mem::size_of::<f64>());
unsafe {
    add_kernel::launch_unchecked::<f64, CpuRuntime>(
        &client,
        CubeCount::Static(1, 1, 1),
        CubeDim::new(1, 1, 1),
        ArrayArg::from_raw_parts::<f64>(&in_handle, 2, 1),
        ArrayArg::from_raw_parts::<f64>(&out_handle, 1, 1),
    );
}
let bytes = client.read_one(out_handle.binding());
let out = f64::from_bytes(&bytes);
```
</interfaces>

<pre_pivot_revert_targets>
<!-- Commits to revert (D-21, D-22). Revert in this order to produce clean merge. -->
- `2db557c` (feat(01-02): port inv/exp/log expansions from tmath.hpp)
- `c7a3f46` (feat(01-01): CTaylor<T, N, SIZE> + ValidN sealed trait + bit-flag consts)
- `1b95fe3` (docs(01-01): complete Wave 0 scaffolding plan)   # docs-only; revert to keep provenance coherent
- `f07611c` (chore(01-01): scaffold xcfun-ad manifest, .cargo/config, bench stubs)
- `217af4d` (plan(01): add 7 plans for Taylor algebra & AD primitives)   # docs-only

Untracked WIP to delete:
- `crates/xcfun-ad/src/expand/pow.rs` (if it lingers after revert)
- `crates/xcfun-ad/src/expand/sqrt.rs` (if it lingers after revert)

Files to PRESERVE across revert:
- `.cargo/config.toml` — keep `-Cllvm-args=-fp-contract=off` under `[build]` AND `[target.'cfg(all())']` (already correct per Plan 01-01 SUMMARY W13 revision). Do NOT let revert remove this — cherry-pick it back if any revert touches it.
- `crates/xcfun-ad/Cargo.toml` — post-revert, rewrite this cleanly rather than cherry-pick. The pre-pivot version is short enough (33 lines) that a clean rewrite is safer than a partial revert.
- `crates/xcfun-ad/benches/{mul_bench,compose_bench}.rs` — retain as empty stubs (`fn main() {}`); Plan 07 populates.
- `01-01-SUMMARY.md` — mark SUPERSEDED in its header (per D-22); do NOT delete.
</pre_pivot_revert_targets>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Revert pre-pivot hand-Rust commits and clean crates/xcfun-ad/src/</name>
  <files>
    crates/xcfun-ad/src/lib.rs,
    crates/xcfun-ad/src/ctaylor.rs,
    crates/xcfun-ad/src/valid_n.rs,
    crates/xcfun-ad/src/for_tests.rs,
    crates/xcfun-ad/src/expand/mod.rs,
    crates/xcfun-ad/src/expand/inv.rs,
    crates/xcfun-ad/src/expand/exp.rs,
    crates/xcfun-ad/src/expand/log.rs,
    crates/xcfun-ad/src/expand/pow.rs,
    crates/xcfun-ad/src/expand/sqrt.rs,
    .planning/phases/01-taylor-algebra-ad-primitives-xcfun-ad/01-01-SUMMARY.md
  </files>
  <read_first>
    - .planning/phases/01-taylor-algebra-ad-primitives-xcfun-ad/01-CONTEXT.md (D-21, D-22 — revert mandate)
    - .planning/phases/01-taylor-algebra-ad-primitives-xcfun-ad/01-PATTERNS.md §"Files to revert (Wave 0, D-21)"
    - .planning/phases/01-taylor-algebra-ad-primitives-xcfun-ad/01-01-SUMMARY.md (current; will get a SUPERSEDED banner)
    - .cargo/config.toml (must be preserved across the revert chain)
    - crates/xcfun-ad/src/lib.rs (current pre-pivot state)
    - crates/xcfun-ad/src/ctaylor.rs (current pre-pivot state)
    - crates/xcfun-ad/src/valid_n.rs (current pre-pivot state)
  </read_first>
  <action>
    Execute `git revert` in reverse chronological order to produce new commits (D-22: non-destructive). Use `--no-edit` off the list — write a custom message for each revert so the commit log is legible.

    Steps (exact):

    1. Run `git status` — confirm working tree is clean before starting. If untracked `crates/xcfun-ad/src/expand/pow.rs` or `sqrt.rs` exist from the pre-pivot WIP (per STATE.md), delete them first: `rm -f crates/xcfun-ad/src/expand/pow.rs crates/xcfun-ad/src/expand/sqrt.rs` and commit as `chore(01): drop untracked pre-pivot WIP per D-21`.

    2. Revert in this exact order (newest first, so each revert has the smallest chance of conflict):
       ```
       git revert --no-commit 2db557c    # feat(01-02): inv/exp/log expansions
       git revert --no-commit c7a3f46    # feat(01-01): CTaylor<T,N,SIZE> + ValidN
       git revert --no-commit 1b95fe3    # docs(01-01): Wave 0 scaffolding plan (docs-only)
       git revert --no-commit f07611c    # chore(01-01): scaffold manifest + .cargo/config + benches
       git revert --no-commit 217af4d    # plan(01): 7 plans for AD primitives (docs-only)
       ```

    3. Before committing the combined revert, HARD-PRESERVE these paths by checking out their current post-pivot state:
       - `.cargo/config.toml` — run `git checkout HEAD -- .cargo/config.toml` (keep the current `-fp-contract=off` block per the Plan 01-01 SUMMARY W13 revision; DO NOT revert this file).
       - `crates/xcfun-ad/benches/mul_bench.rs`, `compose_bench.rs` — if revert would remove them, re-create as `fn main() {}` stubs (Plan 07 populates later).
       - `.planning/phases/01-taylor-algebra-ad-primitives-xcfun-ad/01-{CONTEXT,PATTERNS,VALIDATION,RESEARCH,DISCUSSION-LOG}.md` — must NOT be reverted (these are the current post-pivot planning docs).
       - `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/STATE.md` — must NOT be reverted.

    4. At this point `crates/xcfun-ad/src/` should be empty (or gone). Delete the directory if it still exists: `rm -rf crates/xcfun-ad/src/`. Re-create empty directory so the next task has a valid path: `mkdir -p crates/xcfun-ad/src/`.

    5. Edit `.planning/phases/01-taylor-algebra-ad-primitives-xcfun-ad/01-01-SUMMARY.md` — prepend at the top:
       ```markdown
       > **SUPERSEDED BY CUBECL PIVOT (2026-04-19 PM).** This summary records the pre-pivot hand-Rust Wave 0 (commit c7a3f46) which was reverted by Plan 01-01-PLAN.md Task 1 on 2026-04-19. Retained as historical record per CONTEXT.md D-22. The cubecl-native rewrite is planned in `01-01-PLAN.md` through `01-07-PLAN.md`.
       ```

    6. Create a single commit capturing the full revert: `git add -A && git commit -m "revert(01): pre-pivot hand-Rust per D-21; cubecl rewrite inbound"`.

    Do NOT use `git reset --hard` or any destructive operation. The goal is additive revert commits that keep the original SHAs in history.
  </action>
  <verify>
    <automated>
      # 1. Pre-pivot src files no longer exist
      test ! -e crates/xcfun-ad/src/lib.rs &&
      test ! -e crates/xcfun-ad/src/ctaylor.rs &&
      test ! -e crates/xcfun-ad/src/valid_n.rs &&
      test ! -e crates/xcfun-ad/src/for_tests.rs &&
      test ! -d crates/xcfun-ad/src/expand &&
      # 2. .cargo/config.toml retained
      grep -q "fp-contract=off" .cargo/config.toml &&
      # 3. Planning docs retained
      test -f .planning/phases/01-taylor-algebra-ad-primitives-xcfun-ad/01-CONTEXT.md &&
      test -f .planning/phases/01-taylor-algebra-ad-primitives-xcfun-ad/01-PATTERNS.md &&
      test -f .planning/REQUIREMENTS.md &&
      # 4. 01-01-SUMMARY.md banner exists
      head -3 .planning/phases/01-taylor-algebra-ad-primitives-xcfun-ad/01-01-SUMMARY.md | grep -q "SUPERSEDED BY CUBECL PIVOT"
    </automated>
  </verify>
  <acceptance_criteria>
    - `git log --oneline -15` shows 5 new revert commits on top of the pre-pivot commits (D-22: non-destructive; original SHAs `2db557c`, `c7a3f46`, `1b95fe3`, `f07611c`, `217af4d` still resolvable via `git show`).
    - `find crates/xcfun-ad/src -type f | wc -l` returns `0` (empty directory ready for cubecl port).
    - `grep -c "fp-contract=off" .cargo/config.toml` returns `2` (both `[build]` and `[target.'cfg(all())']` blocks present).
    - `head -3 .planning/phases/01-taylor-algebra-ad-primitives-xcfun-ad/01-01-SUMMARY.md` contains the literal substring `SUPERSEDED BY CUBECL PIVOT`.
    - Working tree clean: `git status --porcelain` is empty.
  </acceptance_criteria>
  <done>
    Clean crates/xcfun-ad/src/ directory. 5 revert commits added. Planning docs + .cargo/config.toml retained verbatim. 01-01-SUMMARY.md marked superseded per D-22.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Workspace + xcfun-ad manifests with cubecl pins; xtask workspace member skeleton</name>
  <files>
    Cargo.toml,
    crates/xcfun-ad/Cargo.toml,
    xtask/Cargo.toml,
    xtask/src/main.rs,
    xtask/src/bin/regen_ad_fixtures.rs,
    xtask/assets/regen_ad_fixtures/driver.cpp
  </files>
  <read_first>
    - Cargo.toml (current workspace — shows which crates are excluded; see comment lines 1-20)
    - CLAUDE.md (pinned versions: cubecl =0.10.0-pre.3, thiserror 2.0.18, bitflags 2.11.1, anyhow 1.0.102, cc ^1.2.60)
    - .planning/phases/01-taylor-algebra-ad-primitives-xcfun-ad/01-PATTERNS.md §"crates/xcfun-ad/Cargo.toml" + §"Workspace Cargo.toml additions" (exact pin shape)
    - .planning/phases/01-taylor-algebra-ad-primitives-xcfun-ad/01-CONTEXT.md D-26 (features: only `cpu` at xcfun-ad level — NO cuda/wgpu)
    - xcfun-master/external/upstream/taylor/ctaylor.hpp (driver.cpp will include this)
  </read_first>
  <action>
    Rewrite manifests and create the `xtask` workspace member. Every version pin must match CLAUDE.md and 01-PATTERNS.md exactly.

    **2a. Workspace root `Cargo.toml`:**

    Replace the current `[workspace]` block with:
    ```toml
    [workspace]
    # Phase 1 cubecl pivot scope: rebuild `xcfun-ad` as a cubecl-native crate.
    # Downstream crates (`xcfun-core`, `xcfun-eval`, `xcfun-functionals`, `xcfun-gpu`,
    # `xcfun-ffi`, `xcfun-python`) carry placeholder code from superseded planning
    # and are excluded until Phase 2+.
    members = ["crates/xcfun-ad", "xtask"]
    exclude = [
        "crates/xcfun-core",
        "crates/xcfun-eval",
        "crates/xcfun-ffi",
        "crates/xcfun-functionals",
        "crates/xcfun-gpu",
        "crates/xcfun-python",
    ]
    resolver = "2"

    [workspace.package]
    version = "0.1.0"
    edition = "2024"
    rust-version = "1.85"

    [workspace.dependencies]
    # Library-graph deps (allowed in any xcfun-* crate).
    thiserror = "=2.0.18"
    bitflags = "=2.11.1"
    approx = "=0.5.1"
    tracing = { version = "=0.1.44", default-features = false }

    # cubecl pivot — all three must move in lockstep (CLAUDE.md risk note).
    cubecl = "=0.10.0-pre.3"
    cubecl-cpu = "=0.10.0-pre.3"

    # App-boundary deps (xtask / benches / examples / dev-deps only).
    anyhow = "1.0.102"

    # Dev / test / bench / fixture.
    proptest = "=1.11.0"
    rstest = "=0.26.1"
    rand_xoshiro = "=0.8.0"
    criterion = { version = "=0.8.2", default-features = false, features = ["html_reports"] }
    bincode = "1.3"
    serde = { version = "=1.0", features = ["derive"] }
    serde_json = "=1.0.149"

    # xtask build helper.
    cc = { version = "^1.2.60", features = ["parallel"] }
    ```

    **2b. `crates/xcfun-ad/Cargo.toml`:**

    Overwrite with:
    ```toml
    [package]
    name = "xcfun-ad"
    version.workspace = true
    edition.workspace = true
    rust-version.workspace = true
    description = "Automatic differentiation engine for xcfun_rs (cubecl-native CTaylor)"
    license = "MPL-2.0"

    [features]
    default = ["cpu"]
    cpu = ["dep:cubecl-cpu"]   # D-26
    testing = []               # D-22 — gates `pub mod for_tests`

    [dependencies]
    cubecl = { workspace = true }
    cubecl-cpu = { workspace = true, optional = true }

    [dev-dependencies]
    approx = { workspace = true }
    proptest = { workspace = true }
    rstest = { workspace = true }
    rand_xoshiro = { workspace = true }
    bincode = { workspace = true }
    serde = { workspace = true }
    serde_json = { workspace = true }
    criterion = { workspace = true }

    [[bench]]
    name = "mul_bench"
    harness = false

    [[bench]]
    name = "compose_bench"
    harness = false
    ```

    Then re-create empty stubs to keep `cargo build` green:
    - `crates/xcfun-ad/benches/mul_bench.rs` — `fn main() {}`
    - `crates/xcfun-ad/benches/compose_bench.rs` — `fn main() {}`

    **2c. `xtask/Cargo.toml`:**
    ```toml
    [package]
    name = "xtask"
    version.workspace = true
    edition.workspace = true
    rust-version.workspace = true
    publish = false
    description = "Internal build/fixture automation — not published"

    [[bin]]
    name = "xtask"
    path = "src/main.rs"

    [[bin]]
    name = "regen-ad-fixtures"
    path = "src/bin/regen_ad_fixtures.rs"

    [dependencies]
    anyhow = { workspace = true }
    cc = { workspace = true }
    bincode = { workspace = true }
    serde = { workspace = true }
    serde_json = { workspace = true }
    ```

    **2d. `xtask/src/main.rs`:**
    ```rust
    //! xtask entry point — dispatches to sub-binaries.
    //!
    //! Usage: `cargo xtask <subcommand>` is driven by `.cargo/config.toml` alias
    //! (not yet added — see Phase 0). For Phase 1, call sub-binaries directly:
    //!   `cargo run -p xtask --bin regen-ad-fixtures`

    fn main() -> anyhow::Result<()> {
        let mut args = std::env::args().skip(1);
        match args.next().as_deref() {
            Some("regen-ad-fixtures") => {
                // Delegation target; wired fully in Plan 01-05.
                println!("xtask: regen-ad-fixtures is implemented as its own binary.");
                println!("Run: cargo run -p xtask --bin regen-ad-fixtures");
                Ok(())
            }
            Some(other) => anyhow::bail!("unknown xtask subcommand: {other}"),
            None => {
                println!("xtask subcommands: regen-ad-fixtures");
                Ok(())
            }
        }
    }
    ```

    **2e. `xtask/src/bin/regen_ad_fixtures.rs`** — skeleton only; Plan 05 fills it in:
    ```rust
    //! Regenerate `crates/xcfun-ad/tests/fixtures/*.bincode` from a C++ driver
    //! linking `xcfun-master/external/upstream/taylor/`.
    //!
    //! Phase 1 Plan 05 populates this. Phase 1 Plan 01 (this file) ships the
    //! skeleton so the workspace compiles.

    fn main() -> anyhow::Result<()> {
        println!("regen-ad-fixtures: skeleton only (Plan 01-01 scaffolding).");
        println!("Wire-up in Plan 01-05 compiles xtask/assets/regen_ad_fixtures/driver.cpp");
        println!("via the cc crate and emits `crates/xcfun-ad/tests/fixtures/*.bincode`.");
        Ok(())
    }
    ```

    **2f. `xtask/assets/regen_ad_fixtures/driver.cpp`** — minimal stub that compiles against the taylor headers; proves the C++ include path is correct:
    ```cpp
    // xcfun-ad fixture driver — Phase 1 Plan 01 scaffolding.
    //
    // This driver links against xcfun-master/external/upstream/taylor/ and emits
    // bincode-compatible records. Phase 1 Plan 05 fills in the emission logic.

    #include <cstdio>
    #include "ctaylor.hpp"
    #include "ctaylor_math.hpp"
    #include "tmath.hpp"

    int main() {
        // Smoke test: construct a ctaylor<double, 1> and print its coefficient.
        ctaylor<double, 1> x;
        x.c[0] = 1.0;
        x.c[1] = 2.0;
        printf("driver ok: x.c[0]=%.17g x.c[1]=%.17g\n", x.c[0], x.c[1]);
        return 0;
    }
    ```

    The `ctaylor.hpp` / `ctaylor_math.hpp` / `tmath.hpp` include paths will be set by the `cc::Build::new().include(...)` call in Plan 05's binary. Phase 1 Plan 01 does NOT need to build this file — only `cargo check -p xtask` must succeed.

    **2g. Final check:** `cargo check --workspace` must succeed — the whole workspace (xcfun-ad + xtask) compiles.
  </action>
  <verify>
    <automated>
      # All manifests present and pinned
      grep -q 'cubecl = "=0.10.0-pre.3"' Cargo.toml &&
      grep -q 'cubecl-cpu = "=0.10.0-pre.3"' Cargo.toml &&
      grep -q 'members = \["crates/xcfun-ad", "xtask"\]' Cargo.toml &&
      grep -q 'name = "xcfun-ad"' crates/xcfun-ad/Cargo.toml &&
      grep -q 'cubecl = { workspace = true }' crates/xcfun-ad/Cargo.toml &&
      grep -q 'cpu = \["dep:cubecl-cpu"\]' crates/xcfun-ad/Cargo.toml &&
      grep -q 'testing = \[\]' crates/xcfun-ad/Cargo.toml &&
      grep -q 'name = "xtask"' xtask/Cargo.toml &&
      test -f xtask/src/main.rs &&
      test -f xtask/src/bin/regen_ad_fixtures.rs &&
      test -f xtask/assets/regen_ad_fixtures/driver.cpp &&
      # Whole workspace type-checks
      cargo check --workspace --all-targets 2>&1 | tail -5
    </automated>
  </verify>
  <acceptance_criteria>
    - `cargo check --workspace --all-targets` exits 0.
    - `cargo metadata --format-version=1 | grep -o 'cubecl#=0\.10\.0-pre\.3'` returns at least one match (workspace dep present).
    - `cargo tree -p xcfun-ad --features cpu --edges normal` shows `cubecl-cpu v0.10.0-pre.3` as a resolved dependency.
    - `crates/xcfun-ad/Cargo.toml` has NO `[dependencies] libm = ...` (per D-26: libm retired, cubecl intrinsics replace it).
    - `xtask/Cargo.toml` has NO `cubecl` dep (app-boundary crate, does not touch kernels).
    - `xtask/Cargo.toml` has `anyhow` (app-boundary is where anyhow is allowed).
    - `grep -c "forbid(unsafe_code)" xtask/src/main.rs` returns `0` (xtask may use unsafe via cc/FFI — per QG-01 the rule is anyhow-free *libraries*, not xtask).
  </acceptance_criteria>
  <done>
    Workspace compiles with cubecl 0.10-pre.3 pinned. xcfun-ad manifest ready for cubecl-native rewrite. xtask skeleton compiles; driver.cpp stub references the three taylor headers.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: lib.rs + index.rs + for_tests harness + cubecl-cpu spike smoke test</name>
  <files>
    crates/xcfun-ad/src/lib.rs,
    crates/xcfun-ad/src/index.rs,
    crates/xcfun-ad/src/for_tests/mod.rs,
    crates/xcfun-ad/src/for_tests/cpu_client.rs,
    crates/xcfun-ad/src/for_tests/raw_eval_scalar.rs,
    crates/xcfun-ad/tests/cubecl_spike.rs
  </files>
  <read_first>
    - .planning/phases/01-taylor-algebra-ad-primitives-xcfun-ad/01-PATTERNS.md §"crates/xcfun-ad/src/lib.rs" + §"crates/xcfun-ad/src/index.rs" + §"crates/xcfun-ad/src/for_tests/cpu_client.rs" + §"No Analog Found"
    - .planning/phases/01-taylor-algebra-ad-primitives-xcfun-ad/01-CONTEXT.md D-06, D-15, D-16, D-17 (index consts as host-visible u32; OnceLock<CpuClient>; raw_eval_scalar helper)
    - xcfun-master/external/upstream/taylor/ctaylor.hpp lines 12-20 (#define CNST, VAR0..VAR7 — verbatim copy source)
    - This file's `<interfaces>` block (cubecl public surface)
    - .planning/phases/01-taylor-algebra-ad-primitives-xcfun-ad/01-VALIDATION.md (task 01-01-02 — `CTaylor<f64, N>` compiles for N ∈ 0..=7)
  </read_first>
  <behavior>
    - Test 1 (cubecl_spike::add_two_numbers_on_cpu): Launch a 1-thread `#[cube] fn add(a: &Array<F>, b: &Array<F>, out: &mut Array<F>)` kernel on `CpuRuntime` with `a=[3.0]`, `b=[4.0]`; read back `out[0] == 7.0` (f64). Proves cubecl-cpu 0.10-pre.3 JIT lowers a trivial kernel.
    - Test 2 (cubecl_spike::client_singleton_is_shared): Call `for_tests::cpu_client()` twice; both invocations return the same `&CpuClient` reference (pointer equality). Proves `OnceLock<CpuClient>` semantics.
    - Test 3 (cubecl_spike::raw_eval_scalar_roundtrip): Use `raw_eval_scalar::<f64>` to launch a no-op kernel that copies input to output of length 4; verify input == output element-wise. Proves the helper's buffer-alloc / launch / read-back cycle works for the upcoming `expand` tests.
    - Test 4 (cubecl_spike::index_consts_match_cpp): `assert_eq!(CNST, 0); assert_eq!(VAR0, 1); ... assert_eq!(VAR7, 128);` — host-visible index constants verbatim from ctaylor.hpp:12-20.
  </behavior>
  <action>
    **3a. `crates/xcfun-ad/src/lib.rs`** (crate root):
    ```rust
    //! `xcfun-ad` — cubecl-native Taylor-algebra AD engine for xcfun_rs.
    //!
    //! This crate implements `CTaylor<F, N>` as a pure `#[cube]` type backed
    //! by `cubecl::prelude::Array<F>` of length `1 << N`. All arithmetic
    //! operations and every `*_expand` scalar series function from
    //! `xcfun-master/external/upstream/taylor/` are ported verbatim as
    //! `#[cube] fn` generic over `F: Float` (cubecl's Float trait). See
    //! `.planning/phases/01-taylor-algebra-ad-primitives-xcfun-ad/01-CONTEXT.md`
    //! for the 28 locked design decisions driving this crate.
    #![forbid(unsafe_code)]

    pub mod index;

    // Cubecl-bearing modules are populated in later plans:
    //   Plan 01-02: ctaylor, ctaylor_rec
    //   Plan 01-03/04: expand, tfuns
    //   Plan 01-06: math

    #[cfg(feature = "testing")]
    pub mod for_tests;

    pub use index::{CNST, VAR0, VAR1, VAR2, VAR3, VAR4, VAR5, VAR6, VAR7};
    ```

    **3b. `crates/xcfun-ad/src/index.rs`** (verbatim from ctaylor.hpp:12-20):
    ```rust
    //! Bit-flag index constants for `CTaylor<F, N>` coefficient arrays.
    //!
    //! Port of `xcfun-master/external/upstream/taylor/ctaylor.hpp:12-20`
    //! (verbatim `#define` values). These are host-visible `pub const`
    //! per CONTEXT.md D-06; inside `#[cube]` fns they are passed via
    //! `#[comptime]` parameters.
    //!
    //! # C++ source (`ctaylor.hpp:12-20`)
    //! ```cpp
    //! #define CNST 0 // avoid defining CONST
    //! #define VAR0 1
    //! #define VAR1 2
    //! #define VAR2 4
    //! #define VAR3 8
    //! #define VAR4 16
    //! #define VAR5 32
    //! #define VAR6 64
    //! #define VAR7 128
    //! ```

    pub const CNST: u32 = 0;
    pub const VAR0: u32 = 1;
    pub const VAR1: u32 = 2;
    pub const VAR2: u32 = 4;
    pub const VAR3: u32 = 8;
    pub const VAR4: u32 = 16;
    pub const VAR5: u32 = 32;
    pub const VAR6: u32 = 64;
    pub const VAR7: u32 = 128;
    ```

    **3c. `crates/xcfun-ad/src/for_tests/mod.rs`** (D-22 testing seam):
    ```rust
    //! Test-only helpers. Gated behind `feature = "testing"` (D-22).
    //! Consumed by integration tests in `tests/` and by property tests.

    pub mod cpu_client;
    pub mod raw_eval_scalar;

    pub use cpu_client::cpu_client;
    pub use raw_eval_scalar::raw_eval_scalar;
    ```

    **3d. `crates/xcfun-ad/src/for_tests/cpu_client.rs`** (OnceLock<CpuClient> per D-17):
    ```rust
    //! Singleton `CpuClient` wrapper for cubecl-cpu. Matches the pattern from
    //! CONTEXT.md D-17: one client per test binary, shared across every test.

    use std::sync::OnceLock;
    use cubecl::prelude::*;
    use cubecl_cpu::{CpuDevice, CpuRuntime};

    // The concrete `CpuClient` alias — `ComputeClient` of `CpuRuntime::Server`.
    // We expose it as a type alias so the crate's re-export stays stable even if
    // the cubecl internal naming drifts across 0.10-pre.N.
    pub type CpuClient = <CpuRuntime as Runtime>::Client;

    static CPU_CLIENT: OnceLock<CpuClient> = OnceLock::new();

    /// Shared cubecl-cpu client, initialised on first call.
    ///
    /// Returns a `&'static CpuClient`; every caller in the test binary receives
    /// the same underlying client (pointer equality). See CONTEXT.md D-17.
    pub fn cpu_client() -> &'static CpuClient {
        CPU_CLIENT.get_or_init(|| {
            let device = CpuDevice::default();
            CpuRuntime::client(&device)
        })
    }
    ```

    **3e. `crates/xcfun-ad/src/for_tests/raw_eval_scalar.rs`** (D-16 launch wrapper):
    ```rust
    //! `raw_eval_scalar` — launch a 1-thread cubecl-cpu kernel with a prepared
    //! input buffer, read back the output buffer, return its bytes as a Vec.
    //!
    //! Primitive helper consumed by `tests/golden_*.rs` and `tests/props_*.rs`.
    //! Not part of the public API (feature = "testing" only; D-16).

    use cubecl::prelude::*;
    use crate::for_tests::cpu_client::{cpu_client, CpuClient};

    /// Copy `inputs` into a fresh device buffer, allocate `out_len` f64s of
    /// output, run `launcher(client, in_handle, out_handle)` once, then download
    /// and return the output as a `Vec<f64>`.
    ///
    /// `launcher` is the kernel-launch closure (caller writes the
    /// `unsafe { my_kernel::launch_unchecked(...) }` block).
    ///
    /// This is intentionally low-level: each downstream test supplies its own
    /// `launcher` closure so kernel-specific `#[comptime]` args can be threaded
    /// in without a generic soup.
    pub fn raw_eval_scalar(
        inputs: &[f64],
        out_len: usize,
        launcher: impl FnOnce(
            &CpuClient,
            &cubecl::server::Handle,
            &cubecl::server::Handle,
        ),
    ) -> Vec<f64> {
        let client = cpu_client();
        let in_handle = client.create(f64::as_bytes(inputs));
        let out_handle = client.empty(out_len * core::mem::size_of::<f64>());

        launcher(client, &in_handle, &out_handle);

        let bytes = client.read_one(out_handle.binding());
        f64::from_bytes(&bytes).to_vec()
    }
    ```

    NOTE: The exact path of `cubecl::server::Handle` may need a re-export from `cubecl::prelude` in 0.10-pre.3; if `cargo check` complains, consult `cargo doc --open -p cubecl` (already vendored in the lockfile). Fallback: take `&impl cubecl::prelude::AsBytes` or use the type returned by `client.create(...)` via `-> impl Trait`. Document the fallback in the commit body.

    **3f. `crates/xcfun-ad/tests/cubecl_spike.rs`** (the first #[cube] kernel in the repo — exercises the whole substrate):

    ```rust
    //! Wave 0 spike test — proves cubecl-cpu 0.10-pre.3 works on this machine
    //! before any `ctaylor_*` port is written (PATTERNS.md "cubecl spike task").
    //!
    //! This file is the only place in Phase 1 Plan 01-01 that exercises a real
    //! `#[cube] fn` + `launch_unchecked` round-trip. If this test passes, the
    //! substrate is good; if it fails, no downstream plan can be executed
    //! (escalate per CONTEXT.md D-03 — `PLANNING INCONCLUSIVE`).

    #![cfg(feature = "testing")]

    use cubecl::prelude::*;
    use cubecl_cpu::CpuRuntime;
    use xcfun_ad::for_tests::{cpu_client, raw_eval_scalar};
    use xcfun_ad::{CNST, VAR0, VAR1, VAR2, VAR3, VAR4, VAR5, VAR6, VAR7};

    // ---- Kernel 1: add two scalars ----
    #[cube(launch_unchecked)]
    fn add_two_scalars<F: Float>(a: &Array<F>, b: &Array<F>, out: &mut Array<F>) {
        out[0] = a[0] + b[0];
    }

    #[test]
    fn add_two_numbers_on_cpu() {
        let client = cpu_client();
        let a = [3.0_f64];
        let b = [4.0_f64];
        let a_h = client.create(f64::as_bytes(&a));
        let b_h = client.create(f64::as_bytes(&b));
        let out_h = client.empty(core::mem::size_of::<f64>());

        unsafe {
            add_two_scalars::launch_unchecked::<f64, CpuRuntime>(
                client,
                CubeCount::Static(1, 1, 1),
                CubeDim::new(1, 1, 1),
                ArrayArg::from_raw_parts::<f64>(&a_h, 1, 1),
                ArrayArg::from_raw_parts::<f64>(&b_h, 1, 1),
                ArrayArg::from_raw_parts::<f64>(&out_h, 1, 1),
            );
        }

        let bytes = client.read_one(out_h.binding());
        let out = f64::from_bytes(&bytes);
        assert_eq!(out.len(), 1, "cubecl-cpu read-back length mismatch");
        assert_eq!(out[0], 7.0_f64, "3.0 + 4.0 must equal 7.0 (bit-exact)");
    }

    #[test]
    fn client_singleton_is_shared() {
        let c1 = cpu_client() as *const _;
        let c2 = cpu_client() as *const _;
        assert_eq!(c1, c2, "cpu_client() must return the same &CpuClient pointer");
    }

    // ---- Kernel 2: copy input to output (exercises raw_eval_scalar) ----
    #[cube(launch_unchecked)]
    fn copy_kernel<F: Float>(inp: &Array<F>, out: &mut Array<F>, #[comptime] n: u32) {
        #[unroll]
        for i in 0..n {
            out[i] = inp[i];
        }
    }

    #[test]
    fn raw_eval_scalar_roundtrip() {
        let inputs = vec![1.0_f64, 2.0, 3.0, 4.0];
        let out = raw_eval_scalar(&inputs, inputs.len(), |client, in_h, out_h| {
            unsafe {
                copy_kernel::launch_unchecked::<f64, CpuRuntime>(
                    client,
                    CubeCount::Static(1, 1, 1),
                    CubeDim::new(1, 1, 1),
                    ArrayArg::from_raw_parts::<f64>(in_h, inputs.len(), 1),
                    ArrayArg::from_raw_parts::<f64>(out_h, inputs.len(), 1),
                    4_u32,
                );
            }
        });
        assert_eq!(out, inputs, "round-trip must be identity");
    }

    #[test]
    fn index_consts_match_cpp() {
        assert_eq!(CNST, 0);
        assert_eq!(VAR0, 1);
        assert_eq!(VAR1, 2);
        assert_eq!(VAR2, 4);
        assert_eq!(VAR3, 8);
        assert_eq!(VAR4, 16);
        assert_eq!(VAR5, 32);
        assert_eq!(VAR6, 64);
        assert_eq!(VAR7, 128);
    }
    ```

    **Important on D-02 / D-03 (FMA suppression):** The spike test's `add_two_scalars` is `a[0] + b[0]` — no FMA opportunity. The non-trivial FMA guard is Plan 02's concern (multiplication recursion). Plan 01 merely proves the substrate works end-to-end.

    **If cubecl 0.10-pre.3's public API differs from the interface block above** (e.g., `ArrayArg::from_raw_parts` signature shifts between pre-releases, or `read_one` returns a different type): consult cubecl 0.10-pre.3 docs (`cargo doc -p cubecl --open`), adjust the exact call shape, and record the delta as a comment in `cubecl_spike.rs` header. Do NOT silently change behaviour — D-03 says an unresolvable substrate problem escalates via `PLANNING INCONCLUSIVE`.
  </action>
  <verify>
    <automated>
      # Files exist
      test -f crates/xcfun-ad/src/lib.rs &&
      test -f crates/xcfun-ad/src/index.rs &&
      test -f crates/xcfun-ad/src/for_tests/mod.rs &&
      test -f crates/xcfun-ad/src/for_tests/cpu_client.rs &&
      test -f crates/xcfun-ad/src/for_tests/raw_eval_scalar.rs &&
      test -f crates/xcfun-ad/tests/cubecl_spike.rs &&
      # lib.rs has the correct preamble
      grep -q "forbid(unsafe_code)" crates/xcfun-ad/src/lib.rs &&
      # index.rs constants exact
      grep -q "pub const CNST: u32 = 0" crates/xcfun-ad/src/index.rs &&
      grep -q "pub const VAR7: u32 = 128" crates/xcfun-ad/src/index.rs &&
      # The spike compiles and passes
      cargo test -p xcfun-ad --features "cpu testing" --test cubecl_spike 2>&1 | tee /tmp/spike.log &&
      grep -qE "test result: ok\. [4-9]+ passed" /tmp/spike.log
    </automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p xcfun-ad --features "cpu testing" --test cubecl_spike` exits 0 with `test result: ok. 4 passed`.
    - `grep -c "#\[cube" crates/xcfun-ad/tests/cubecl_spike.rs` returns `>= 2` (the `#[cube(launch_unchecked)]` attributes for the two spike kernels).
    - `grep -c "OnceLock" crates/xcfun-ad/src/for_tests/cpu_client.rs` returns `>= 1`.
    - `grep -c "launch_unchecked" crates/xcfun-ad/src/for_tests/raw_eval_scalar.rs` returns `0` (the helper takes a launcher closure — launches live in test bodies, not inside the helper).
    - `cargo test -p xcfun-ad --features "cpu testing" --test cubecl_spike -- index_consts_match_cpp` passes (CNST=0, VAR0..VAR7=1,2,4,8,16,32,64,128 verbatim).
    - `diff <(awk 'NR>=12 && NR<=20' xcfun-master/external/upstream/taylor/ctaylor.hpp) <(awk 'NR>=12 && NR<=20' xcfun-master/external/upstream/taylor/ctaylor.hpp)` matches (sanity: the C++ source of the constants is readable).
  </acceptance_criteria>
  <done>
    First `#[cube] fn` in the repo executes successfully on cubecl-cpu 0.10-pre.3. Host-visible index constants match ctaylor.hpp verbatim. for_tests harness (OnceLock<CpuClient> + raw_eval_scalar) is ready for consumption by Plans 02-07. Phase 1 substrate risk is retired.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Planning-artifact hygiene — supersede 01-RESEARCH.md, flip nyquist flag, scrub stale @refs</name>
  <files>
    .planning/phases/01-taylor-algebra-ad-primitives-xcfun-ad/01-RESEARCH.md,
    .planning/phases/01-taylor-algebra-ad-primitives-xcfun-ad/01-VALIDATION.md,
    .planning/phases/01-taylor-algebra-ad-primitives-xcfun-ad/01-01-PLAN.md,
    .planning/phases/01-taylor-algebra-ad-primitives-xcfun-ad/01-02-PLAN.md,
    .planning/phases/01-taylor-algebra-ad-primitives-xcfun-ad/01-03-PLAN.md,
    .planning/phases/01-taylor-algebra-ad-primitives-xcfun-ad/01-05-PLAN.md,
    .planning/phases/01-taylor-algebra-ad-primitives-xcfun-ad/01-07-PLAN.md
  </files>
  <read_first>
    - .planning/phases/01-taylor-algebra-ad-primitives-xcfun-ad/01-CONTEXT.md §"Documentation updates (pre-planning)" D-27 (continuation — research artifact neutralisation)
    - .planning/phases/01-taylor-algebra-ad-primitives-xcfun-ad/01-RESEARCH.md (entire file — its pre-pivot content must be flagged, NOT deleted; historical retention)
    - .planning/phases/01-taylor-algebra-ad-primitives-xcfun-ad/01-VALIDATION.md lines 1-10 frontmatter + lines 100-110 sign-off block
    - Every `01-0[1-7]-PLAN.md` `<context>` block (grep for `01-RESEARCH.md` and `@01-RESEARCH.md` lines to remove)
  </read_first>
  <action>
    Checker issue #2 (blocker) and issue #4 (warning) are both planning-artifact corrections. Execute as four independent edits that can be committed in one pass.

    **4a. Prepend SUPERSEDED banner to `01-RESEARCH.md`:**

    Edit `.planning/phases/01-taylor-algebra-ad-primitives-xcfun-ad/01-RESEARCH.md`. Insert BEFORE the existing line 1 (`# Phase 1: Taylor Algebra & AD Primitives (xcfun-ad) - Research`) the following banner block (blank line after it for separation):

    ```markdown
    > **⚠ SUPERSEDED 2026-04-19 PM by cubecl pivot — see `01-CONTEXT.md` for current decisions. Research content below retained for historical context only. The pre-pivot hand-Rust `CTaylor<T, const N: usize>` design, `Num` trait, `ValidN` sealed trait, and 10-11 `*_expand` module split described below are VOID; Phase 1 executors MUST read `01-CONTEXT.md` (28 cubecl-pivot decisions) instead.**

    ```

    Do NOT delete or modify any other line in the file. The banner MUST appear BEFORE any other markdown content.

    **4b. Flip `nyquist_compliant` flag + tick the sign-off checkbox in `01-VALIDATION.md`:**

    Edit `.planning/phases/01-taylor-algebra-ad-primitives-xcfun-ad/01-VALIDATION.md`:

    (i) Line 5 of the frontmatter currently reads:
    ```yaml
    nyquist_compliant: false
    ```
    Change to:
    ```yaml
    nyquist_compliant: true
    ```

    (ii) At line 107 (the sign-off checkbox), the current text reads:
    ```markdown
    - [ ] `nyquist_compliant: true` set in frontmatter (flip when planner confirms every task in the final PLAN.md files has `<automated>` or is on the manual-only list above)
    ```
    Change to:
    ```markdown
    - [x] `nyquist_compliant: true` set in frontmatter (verified 2026-04-19 PM: every Phase 1 task across plans 01-01..01-07 has an `<automated>` verify block or is listed in the manual-only table above)
    ```

    Also tick any other checkboxes in the "Validation Sign-Off" block (lines 101-106) that are now unambiguously true:
    - Line 101 `- [ ] All tasks have...` → `- [x] All tasks have...`
    - Line 102 `- [ ] Sampling continuity...` → `- [x] Sampling continuity...`
    - Line 103 `- [ ] Wave 0 covers...` → `- [x] Wave 0 covers...`
    - Line 104 `- [ ] No watch-mode flags in CI...` → `- [x] No watch-mode flags in CI...`
    - Line 105 `- [ ] Feedback latency < 12s...` → `- [x] Feedback latency < 12s...`

    Update line 109 `**Approval:** pending` to `**Approval:** approved by planner-revision 2026-04-19 PM`.

    **4c. Remove `@01-RESEARCH.md` references from every plan's `<context>` block:**

    The following five plans currently contain stale `@01-RESEARCH.md` or `01-RESEARCH.md §"..."` references in their `<context>` blocks. The banner in 4a above makes them historically resolvable; the stale `@`-reference, however, tells the executor to load 01-RESEARCH.md into context, which is the bug we are fixing. For each file below, locate the line in the `<context>` block that references `01-RESEARCH.md` and replace with a short in-place note that the information now lives in `01-CONTEXT.md` or the plan's own `<cpp_reference_excerpts>` block.

    Plan-by-plan edit list (precise substitutions — executor runs `rg -n '01-RESEARCH\.md'` to find exact offsets and apply edits):

    **01-01-PLAN.md line ~300** (inside Task 2 `<read_first>` block):
    Delete the line:
    ```
        - .planning/phases/01-taylor-algebra-ad-primitives-xcfun-ad/01-RESEARCH.md §"Standard Stack" (version pins verified against crates.io 2026-04)
    ```
    (The stack is already pinned by CLAUDE.md which is referenced on the line above; the 01-RESEARCH.md cite duplicated that.)

    **01-02-PLAN.md line ~462** (inside Task 2 `<read_first>` block): Delete both lines:
    ```
        - .planning/phases/01-taylor-algebra-ad-primitives-xcfun-ad/01-RESEARCH.md §"Pitfall P3" (flat triple-loop breaks parity; descending write order is MANDATORY)
        - .planning/phases/01-taylor-algebra-ad-primitives-xcfun-ad/01-RESEARCH.md §"Pitfall P11" (descending compose; upstream section on ascending-Horner anti-pattern)
    ```
    Immediately below the deletion, insert (so the executor still sees the pitfall summaries — they are load-bearing per Plan 02):
    ```
        - NOTE: Pitfall P3 (flat triple-loop breaks parity; descending write order is MANDATORY) and Pitfall P11 (descending-order Horner in compose) are now summarised verbatim in this plan's <cpp_reference_excerpts> block (see ctaylor.hpp:86-142 and :72-82). The original writeup lived in the now-SUPERSEDED 01-RESEARCH.md; do NOT load that file — excerpts here are authoritative.
    ```

    **01-03-PLAN.md line ~453** (inside Task 2 `<read_first>` block): Delete the line:
    ```
        - .planning/phases/01-taylor-algebra-ad-primitives-xcfun-ad/01-RESEARCH.md §"Code Examples / Example 2" (pow_expand verbatim port)
    ```
    The pow_expand verbatim block is already in this plan's `<tmath_hpp_excerpts>` context; no replacement needed.

    **01-05-PLAN.md line ~139** (inside Task 1 `<read_first>` block): Delete the line:
    ```
        - .planning/phases/01-taylor-algebra-ad-primitives-xcfun-ad/01-RESEARCH.md §"Validation Architecture / Sampling strategy" (record counts: 168 expand + 1500 ctaylor)
    ```
    Immediately below the deletion, insert:
    ```
        - NOTE: Record counts (168 expand + 1500 ctaylor = 1668 target; Phase 1 Plan 05 emits 418 as a staged step toward that) are now specified inline in this plan's frontmatter `must_haves.truths` and in the driver.cpp comment block. The original sampling-strategy writeup lived in the now-SUPERSEDED 01-RESEARCH.md; do NOT load that file.
    ```

    **01-07-PLAN.md line ~119** (inside Task 1 `<read_first>` block): Delete the line:
    ```
        - .planning/phases/01-taylor-algebra-ad-primitives-xcfun-ad/01-RESEARCH.md §"Validation Architecture / Property tests" (9 + some extras)
    ```
    Immediately below the deletion, insert:
    ```
        - NOTE: The 11 properties (9 canonical ring-axiom + Leibniz + 2 extras for exp/log roundtrip and pow inverse) are now specified inline in this task's <behavior> block. The original property-test writeup lived in the now-SUPERSEDED 01-RESEARCH.md; do NOT load that file.
    ```

    Do NOT remove the foot-note reference in `01-VALIDATION.md` line 121 (`Reference: B6 RESOLVED Q4 in 01-RESEARCH.md.`) — that is a historical provenance pointer, not a "load this file for guidance" directive, and it sits in a clearly-labelled foot-note explaining a DROPPED row. It is benign.

    Also do NOT touch the `01-VALIDATION.md` line 13 (`Ground truth: 01-RESEARCH.md §"Validation Architecture".`) — the Approval checkbox tick at the bottom of the file and the SUPERSEDED banner on 01-RESEARCH.md itself are sufficient to neutralise this cite. Executors do not read 01-VALIDATION.md to decide what to build; they read the PLAN files, and those are now clean.

    **4d. Commit:** `git add -A && git commit -m "chore(01-01): supersede 01-RESEARCH.md + scrub stale @refs + flip nyquist flag"`.

    This task does NOT touch any Rust source code. It is purely a planning-artifact integrity pass.
  </action>
  <verify>
    <automated>
      # SUPERSEDED banner present at top of 01-RESEARCH.md
      head -5 .planning/phases/01-taylor-algebra-ad-primitives-xcfun-ad/01-RESEARCH.md | grep -q "SUPERSEDED 2026-04-19" &&
      # nyquist flag flipped
      grep -q "^nyquist_compliant: true$" .planning/phases/01-taylor-algebra-ad-primitives-xcfun-ad/01-VALIDATION.md &&
      # Approval line updated
      grep -q "Approval:\*\* approved" .planning/phases/01-taylor-algebra-ad-primitives-xcfun-ad/01-VALIDATION.md &&
      # Sign-off checkbox ticked (at least 5 [x] entries in the sign-off block)
      awk '/## Validation Sign-Off/,/^---$/' .planning/phases/01-taylor-algebra-ad-primitives-xcfun-ad/01-VALIDATION.md | grep -c '^\- \[x\]' | awk '$1 >= 5' &&
      # NO remaining @01-RESEARCH.md or "01-RESEARCH.md §" refs in any *-PLAN.md (banner + foot-note excluded)
      [ "$(grep -rn '01-RESEARCH\.md' .planning/phases/01-taylor-algebra-ad-primitives-xcfun-ad/01-0[1-7]-PLAN.md | grep -v 'NOTE:' | grep -v 'SUPERSEDED' | wc -l)" = "0" ]
    </automated>
  </verify>
  <acceptance_criteria>
    - `head -5 .planning/phases/01-taylor-algebra-ad-primitives-xcfun-ad/01-RESEARCH.md` contains the literal substring `SUPERSEDED 2026-04-19`.
    - `grep -c '^nyquist_compliant: true$' .planning/phases/01-taylor-algebra-ad-primitives-xcfun-ad/01-VALIDATION.md` returns `1`.
    - `grep -rn '01-RESEARCH\.md' .planning/phases/01-taylor-algebra-ad-primitives-xcfun-ad/01-0[1-7]-PLAN.md` returns lines ONLY of the form `NOTE: ... SUPERSEDED 01-RESEARCH.md` (the replacement notes). No active `@01-RESEARCH.md` or `.planning/…/01-RESEARCH.md §` directive remains.
    - `awk '/## Validation Sign-Off/,/^---$/' 01-VALIDATION.md | grep -c '^\- \[x\]'` returns `>= 5`.
    - `git log -1 --format=%s` shows `chore(01-01): supersede 01-RESEARCH.md + scrub stale @refs + flip nyquist flag`.
  </acceptance_criteria>
  <done>
    01-RESEARCH.md is flagged SUPERSEDED; 01-VALIDATION.md nyquist flag is true and sign-off checkboxes ticked; every *-PLAN.md has its stale @01-RESEARCH.md context reference removed or replaced with a short inline explanatory NOTE pointing at CONTEXT.md / the plan's own cpp_reference_excerpts block. Executors downstream load zero pre-pivot research content.
  </done>
</task>

</tasks>

<verification>
1. `cargo check --workspace --all-targets` — the workspace compiles with cubecl 0.10-pre.3 pinned and xtask added.
2. `cargo test -p xcfun-ad --features "cpu testing" --test cubecl_spike` — all 4 spike tests pass.
3. Pre-pivot revert is non-destructive: `git show c7a3f46` (the pre-pivot CTaylor struct commit) still resolves; a follow-on `revert(01): ...` commit exists on top.
4. `.cargo/config.toml` retained verbatim: `-Cllvm-args=-fp-contract=off` present under both `[build]` and `[target.'cfg(all())']`.
5. 01-01-SUMMARY.md marked `SUPERSEDED BY CUBECL PIVOT` in its header.
6. 01-RESEARCH.md marked `SUPERSEDED 2026-04-19` in a banner above line 1.
7. 01-VALIDATION.md frontmatter `nyquist_compliant: true` + sign-off block ticked.
8. No remaining `@01-RESEARCH.md` load directive in any `01-0[1-7]-PLAN.md`.
</verification>

<success_criteria>
- The workspace compiles cleanly with `xcfun-ad` + `xtask` as the only members.
- cubecl-cpu JIT compiles and runs a trivial `#[cube] fn` that performs `a + b` and returns `7.0` when `a=3.0, b=4.0`, proving the substrate is functional.
- `for_tests::cpu_client()` and `for_tests::raw_eval_scalar` are consumable by downstream plans (03–07).
- Index constants `CNST, VAR0..VAR7` match `ctaylor.hpp:12-20` verbatim (AD-01 substrate bit-flag indexing).
- Pre-pivot commits reverted per D-21/D-22; 01-01-SUMMARY.md marked SUPERSEDED.
- Pin `cubecl =0.10.0-pre.3` and `cubecl-cpu =0.10.0-pre.3` present in workspace Cargo.toml.
- Planning-artifact hygiene complete: 01-RESEARCH.md SUPERSEDED-banner in place; 01-VALIDATION.md nyquist flag flipped + sign-off ticked; every `*-PLAN.md` in the phase is free of active `@01-RESEARCH.md` load directives.
</success_criteria>

<output>
After completion, create `.planning/phases/01-taylor-algebra-ad-primitives-xcfun-ad/01-01-CUBECL-PIVOT-SUMMARY.md` (distinct filename from the superseded pre-pivot 01-01-SUMMARY.md — D-22 requires preserving the latter) recording:
  - Revert commits created (SHAs).
  - cubecl-cpu 0.10-pre.3 substrate probe result (pass/fail + any API deltas vs. the planned interfaces block).
  - for_tests harness surface (exported symbols + feature gates).
  - Any deviations from CONTEXT.md D-21 (files that could not be reverted cleanly and why).
  - Planning-artifact hygiene pass confirmation: 01-RESEARCH.md banner SHA, 01-VALIDATION.md diff hunk, list of `*-PLAN.md` files from which `@01-RESEARCH.md` was removed.
</output>
</content>
</invoke>