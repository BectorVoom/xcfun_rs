---
phase: 07-python-bindings-release
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - crates/xcfun-kernels/src/functionals/mgga/shared/br_like.rs
  - validation/mpmath_fixtures/
  - .planning/phases/06-gpu-backends-batch-lifecycle-xcfun-kernels-xcfun-gpu/06-HUMAN-UAT.md
autonomous: false
requirements: []
must_haves:
  truths:
    - "BR_Q_PREFACTOR_F64 in br_like.rs equals 0.699_291_115_553_117_4 (mpmath@200 truth)"
    - "MPMATH ground-truth fixtures regenerated for the 26 mpmath-spec functionals on restored xcfun-master HEAD a89b783"
    - "Plan 06-N1 11 inherited Phase-3 D-19 forwards verify GREEN at strict 1e-12 on order-3 sweep"
    - "Plan 06-N3 18 small-magnitude residual forwards verify GREEN at strict 1e-13 on order-3 sweep"
    - "06-HUMAN-UAT.md items 3, 4, 5, 6 marked CLEARED with commit refs"
  artifacts:
    - path: "crates/xcfun-kernels/src/functionals/mgga/shared/br_like.rs"
      provides: "BR_Q_PREFACTOR_F64 constant set to 0.699_291_115_553_117_4"
      contains: "0.699_291_115_553_117_4"
    - path: ".planning/phases/06-gpu-backends-batch-lifecycle-xcfun-kernels-xcfun-gpu/06-HUMAN-UAT.md"
      provides: "Status block updated: items 3, 4, 5, 6 set to passed"
    - path: "validation/mpmath_fixtures/"
      provides: "Regenerated mpmath ground-truth fixtures for 26 functionals (with .sha256 stamps)"
  key_links:
    - from: "crates/xcfun-kernels/src/functionals/mgga/shared/br_like.rs"
      to: "BRX/BRC/BRXC tier-1 self-tests"
      via: "BR_Q_PREFACTOR_F64 constant feeds br_like::z exponent prefactor"
      pattern: "0\\.699_291_115_553_117_4"
---

<objective>
Clear the four Phase-6 HUMAN-UAT items that D-14 declares blocking for v0.1.0:
(3) MPMATH ground-truth fixture regeneration (~6h offline), (4) Plan 06-N1
inherited-Phase-3 11-functional auto-tightening verify, (5) Plan 06-N3
18-functional auto-tightening verify, (6) BR_Q_PREFACTOR_F64 typo fix.
This plan gates every later wave: no Python work, no publish ceremony, no
v0.1.0 tag until all four items are signed off in 06-HUMAN-UAT.md.

Purpose: lock the algebraic substrate before the public release ceremony.
A botched v0.1.0 cannot be re-published (PyPI / crates.io yank irreversibility);
the four UAT items are the last numerical-correctness gates before the tag.

Output: corrected BR_Q_PREFACTOR_F64 constant + regenerated mpmath fixtures
+ verified order-3 strict-1e-12 sweep evidence + 06-HUMAN-UAT.md status
flipped from `pending` to `passed` for items 3/4/5/6.
</objective>

<execution_context>
@/home/user/Documents/workspace/xcfun_rs/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/xcfun_rs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-python-bindings-release/07-CONTEXT.md
@.planning/phases/06-gpu-backends-batch-lifecycle-xcfun-kernels-xcfun-gpu/06-HUMAN-UAT.md
@crates/xcfun-kernels/src/functionals/mgga/shared/br_like.rs

<interfaces>
<!-- BR_Q_PREFACTOR_F64 typo (line 37): the current value is wrong. -->
<!-- The correct mathematical value is 1/((2/3)·π^(2/3)). -->
<!-- mpmath@200 verification: -->
<!--   from mpmath import mp, mpf, pi -->
<!--   mp.dps = 200 -->
<!--   v = mpf(1) / (mpf(2)/3 * pi**(mpf(2)/3)) -->
<!--   # nearest f64 ≈ 0.699_291_115_553_117_4 -->

Current (WRONG, line 37):
```rust
const BR_Q_PREFACTOR_F64: f64 = 0.699_390_040_064_282_6_f64;
```

Required (per D-14 #6):
```rust
const BR_Q_PREFACTOR_F64: f64 = 0.699_291_115_553_117_4_f64;
```

The validation harness existing entry points:
- `cargo run -p validation --release -- --backend cpu --order 3 --filter '<list>'`
  produces `validation/report.html` + `validation/report.jsonl` (gitignored).
  Exit 0 = strict 1e-12 GREEN; exit 2 = failures.
- `cargo run --release -p xtask --bin regen-mpmath-fixtures` populates
  `validation/mpmath_fixtures/<name>.jsonl` + `.sha256` stamps for the
  Phase 6 Plan 06-N2 26-functional manual lane.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 0.1: Fix BR_Q_PREFACTOR_F64 typo (D-14 #6)</name>
  <files>crates/xcfun-kernels/src/functionals/mgga/shared/br_like.rs</files>
  <read_first>
    - crates/xcfun-kernels/src/functionals/mgga/shared/br_like.rs (lines 30-50 — see the constant in context; do NOT touch any other constant)
    - .planning/phases/07-python-bindings-release/07-CONTEXT.md (D-14 item #6 — verbatim correct value `0.699_291_115_553_117_4`)
    - .planning/phases/06-gpu-backends-batch-lifecycle-xcfun-kernels-xcfun-gpu/06-HUMAN-UAT.md (test #6 expected text)
  </read_first>
  <behavior>
    - Test 1: BRX/BRC/BRXC tier-1 self-tests still pass after the constant change
      (mathematical correctness — the new value is closer to the true `1/((2/3)·π^(2/3))`)
    - Test 2: a unit assertion in br_like.rs's `#[cfg(test)]` block confirms
      `BR_Q_PREFACTOR_F64 == 0.699_291_115_553_117_4_f64` (locks the typo from regressing)
  </behavior>
  <action>
    Open `crates/xcfun-kernels/src/functionals/mgga/shared/br_like.rs` line 37.
    Replace the literal `0.699_390_040_064_282_6_f64` with `0.699_291_115_553_117_4_f64`
    EXACTLY as written (preserve the trailing `_f64` suffix). This is a single-character-class
    edit; do NOT touch the doc comment, the const name, or any other constant in the file.
    Per D-14 #6 the new value is the f64-nearest of `1/((2/3)·π^(2/3))` verified at mpmath@200.
    Then add (or extend) a `#[cfg(test)] mod tests` block at the bottom of the file with a
    single regression assertion:
    ```rust
    #[test]
    fn br_q_prefactor_locked() {
        assert_eq!(super::BR_Q_PREFACTOR_F64, 0.699_291_115_553_117_4_f64);
    }
    ```
    Commit message: `fix(06-N4/07-00): correct BR_Q_PREFACTOR_F64 to mpmath@200 truth (1/((2/3)·π^(2/3)))`.
  </action>
  <verify>
    <automated>grep -F '0.699_291_115_553_117_4_f64' crates/xcfun-kernels/src/functionals/mgga/shared/br_like.rs &amp;&amp; ! grep -F '0.699_390_040_064_282_6' crates/xcfun-kernels/src/functionals/mgga/shared/br_like.rs &amp;&amp; cargo nextest run -p xcfun-kernels br_q_prefactor_locked</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F '0.699_291_115_553_117_4_f64' crates/xcfun-kernels/src/functionals/mgga/shared/br_like.rs` returns exactly one match
    - `grep -F '0.699_390_040_064_282_6' crates/xcfun-kernels/src/functionals/mgga/shared/br_like.rs` returns zero matches (old typo eliminated)
    - `cargo nextest run -p xcfun-kernels br_q_prefactor_locked` exits 0
    - `cargo nextest run -p xcfun-kernels --filter-expr 'binary(/^brx?c?$/) or test(brx) or test(brc) or test(brxc)'` exits 0 (BR family tier-1 still passes)
    - One git commit landed touching ONLY `crates/xcfun-kernels/src/functionals/mgga/shared/br_like.rs`
  </acceptance_criteria>
  <done>BR_Q_PREFACTOR_F64 set to 0.699_291_115_553_117_4_f64; locked by a regression test; BR family tier-1 still GREEN.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 0.2: MPMATH ground-truth fixture regeneration (~6h offline) (D-14 #3)</name>
  <files>validation/mpmath_fixtures/</files>
  <read_first>
    - .planning/phases/06-gpu-backends-batch-lifecycle-xcfun-kernels-xcfun-gpu/06-HUMAN-UAT.md (test #3 — exact command + expected outcome)
    - xtask/src/bin/regen_mpmath_fixtures.rs (existing driver — confirms the binary is present)
    - .planning/phases/07-python-bindings-release/07-CONTEXT.md (D-14 — "26 functionals; ~6h offline; smoke 5x5 GREEN for TW/PBELOCC/BLOCX")
  </read_first>
  <what-built>
    Task 0.1 corrected the BR_Q_PREFACTOR_F64 typo. The mpmath sidecar driver `xtask regen-mpmath-fixtures`
    is the existing one-shot binary that re-generates the 200-digit-precision JSONL ground-truth
    fixtures for the 26 mpmath-only-spec functionals (Plan 06-N2 manual lane). This step is OFFLINE
    work that takes ~6 hours wall-clock — Claude cannot run it autonomously inside a task; the human
    operator launches it on their workstation, lets it complete overnight, then commits the result.
  </what-built>
  <how-to-verify>
    1. From repo root, run:
       ```
       cargo run --release -p xtask --bin regen-mpmath-fixtures
       ```
       Expect this to take ~6 hours. Plan to run overnight or in the background.
    2. After completion, verify the fixtures + sha256 stamps:
       ```
       ls validation/mpmath_fixtures/ | grep -E '\.(jsonl|sha256)$' | wc -l
       ```
       Expect: 52 (26 functionals × 2 files each — one .jsonl, one .sha256).
    3. Re-run the strict-1e-13 mpmath-reference sweep on the 13 non-SCAN/non-BR functionals
       (per HUMAN-UAT test #3 expected line):
       ```
       cargo run -p validation --release -- --reference mpmath --order 3 --filter '^(TW|VWK|PBELOCC|BLOCX|TPSSC|TPSSLOCC|REVTPSSC|TPSSX|REVTPSSX|VWN3C|VWN5C|PW92C|PZ81C)$'
       ```
       Expect exit code 0 (strict 1e-13 GREEN).
    4. `git add validation/mpmath_fixtures/` and commit with message:
       `docs(06-N2/07-00): regenerate mpmath ground-truth fixtures on restored xcfun-master HEAD a89b783`.
    5. In `.planning/phases/06-gpu-backends-batch-lifecycle-xcfun-kernels-xcfun-gpu/06-HUMAN-UAT.md`,
       update the §3 `result:` line from `[pending …]` to `[passed: regen completed YYYY-MM-DD; sweep
       GREEN at 1e-13 across 13 functionals; commit <hash>]`. Also update the `## Summary` block to
       increment `passed` and decrement `pending`.
  </how-to-verify>
  <resume-signal>Type "approved" once mpmath regeneration AND the --reference mpmath sweep are committed and 06-HUMAN-UAT.md item #3 status is updated. If the sweep failed at any functional, paste the failing record and the operator decides whether to widen the per-functional tolerance (per Phase 6 caveats footnote) or to investigate.</resume-signal>
</task>

<task type="auto">
  <name>Task 0.3: Plan 06-N1 + 06-N3 auto-tightening verification on restored xcfun-master HEAD a89b783 (D-14 #4 + #5)</name>
  <files>.planning/phases/06-gpu-backends-batch-lifecycle-xcfun-kernels-xcfun-gpu/06-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/06-gpu-backends-batch-lifecycle-xcfun-kernels-xcfun-gpu/06-HUMAN-UAT.md (tests #4 + #5 — exact filter lists + expected outcomes)
    - .planning/STATE.md (footnote `^p6caveats` for the 11+18 functional names)
    - validation/src/driver.rs (entrypoint — confirms `--filter` regex semantics)
  </read_first>
  <action>
    Run the order-3 strict-1e-12 sweep over the union of the 11 Plan 06-N1 inherited
    Phase-3 D-19 forwards (PBEINTC, BECKESRX, P86C, P86CORRC, PW91C, SPBEC, APBEC,
    B97C, B97_1C, B97_2C, PW91K) AND the 18 Plan 06-N3 small-magnitude AD-residual
    forwards (M05X, M05C, M05X2C, M06X, M06C, M06LX, M06LC, M06HFX, M06HFC, M06X2C,
    B97X, B97_1X, B97_2X, LYPC, VWN_PBEC, PW92C, PBEC, OPTX) — 29 functionals total.

    Exact command (regex composed from the 29 names; case-sensitive against the
    FunctionalId enum spelling):
    ```
    cargo run -p validation --release -- \
      --backend cpu --order 3 \
      --filter '^(PBEINTC|BECKESRX|P86C|P86CORRC|PW91C|SPBEC|APBEC|B97C|B97_1C|B97_2C|PW91K|M05X|M05C|M05X2C|M06X|M06C|M06LX|M06LC|M06HFX|M06HFC|M06X2C|B97X|B97_1X|B97_2X|LYPC|VWN_PBEC|PW92C|PBEC|OPTX)$' \
      2>&amp;1 | tee /tmp/07-00-task-0.3.log
    ```
    Exit code 0 = all 29 GREEN at strict 1e-12 (D-14 #4 expects 1e-12; D-14 #5 expects
    1e-13 but the validation harness's strict gate is 1e-12 by default; if the harness's
    `--strict 1e-13` flag exists, prefer it for the 18 Plan 06-N3 functionals — verify by
    `cargo run -p validation --release -- --help` first; otherwise document the 1e-12 result).

    If exit 0: edit `06-HUMAN-UAT.md`. Flip §4 and §5 `result:` lines to:
    `[passed: order-3 sweep GREEN at strict 1e-12; commit <hash>; log /tmp/07-00-task-0.3.log
    (preserved in commit message body)]`. Also update the `## Summary` `passed` and `pending` counts.

    If exit 2 (any failure): record the failing tuples in the `result:` line, leave the
    test as `pending`, and surface the failure in the summary block. The operator decides
    whether to widen tolerances (out of scope for this task) or to forward the residual
    to v0.2 (which means D-14 must be amended — escalate to checkpoint).

    Commit message: `docs(06-N1/06-N3/07-00): verify auto-tightening on restored xcfun-master a89b783`.
  </action>
  <verify>
    <automated>grep -E '^### (4|5)\. ' .planning/phases/06-gpu-backends-batch-lifecycle-xcfun-kernels-xcfun-gpu/06-HUMAN-UAT.md -A 3 | grep -E 'result: \[passed' | wc -l | xargs -I{} test {} -eq 2</automated>
  </verify>
  <acceptance_criteria>
    - The exact filter regex above appears verbatim in the commit body or in `/tmp/07-00-task-0.3.log` (preserved as evidence)
    - `06-HUMAN-UAT.md` §4 `result:` line begins with `[passed:` (or contains `[failed:` if the operator chose to escalate)
    - `06-HUMAN-UAT.md` §5 `result:` line begins with `[passed:` (or `[failed:`)
    - `06-HUMAN-UAT.md` `## Summary` `passed:` count increased by 2 (3 if combined with item 3 from Task 0.2)
    - `git log --oneline -1` shows the verification commit
  </acceptance_criteria>
  <done>Plans 06-N1 and 06-N3 marked closed in 06-HUMAN-UAT.md (or escalated as a fail with operator decision recorded).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 0.4: Confirm 4 blocking HUMAN-UAT items closed; gate Wave 1</name>
  <what-built>
    Tasks 0.1 + 0.2 + 0.3 cleared HUMAN-UAT items 3, 4, 5, 6. This checkpoint exists so the
    operator confirms the three commits landed cleanly before any Python work begins.
    Items 1 + 2 (ROCm + Wgpu hardware sweeps) remain `pending` per D-14 SKIP — they are
    explicitly v0.2 deferrals; nothing to do here.
  </what-built>
  <how-to-verify>
    1. `cat .planning/phases/06-gpu-backends-batch-lifecycle-xcfun-kernels-xcfun-gpu/06-HUMAN-UAT.md`
       and confirm:
       - §3 result begins with `[passed:`
       - §4 result begins with `[passed:` (or operator-acknowledged `[failed:`)
       - §5 result begins with `[passed:` (or operator-acknowledged `[failed:`)
       - §6 result begins with `[passed:`
       - §1 result remains `[pending — requires AMD/ROCm GPU on cloud-CI runner]` (D-14 SKIP)
       - §2 result remains `[pending — requires SHADER_F64-capable Vulkan adapter]` (D-14 SKIP)
       - `## Summary` shows `passed: 4` and `pending: 2`.
    2. `git log --oneline -5` shows three new commits (Task 0.1 typo fix, Task 0.2 mpmath regen,
       Task 0.3 verify) since Phase 6 sign-off `ac3c4bb`.
    3. `cargo build --workspace` exits 0 (typo fix didn't break anything else).
  </how-to-verify>
  <resume-signal>Type "approved" if 06-HUMAN-UAT.md shows passed:4 / pending:2 and `cargo build --workspace` is GREEN. Type "block" if any of items 3/4/5/6 are still pending — that blocks the v0.1.0 release ceremony per D-14 and the operator must rerun the failing item before any further plan executes.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local dev → vendored xcfun-master HEAD a89b783 | Task 0.3 reads the C++ reference at the pinned content hash; no network |
| local dev → mpmath@200 sidecar | Task 0.2 spawns Python; no network input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-00-01 | Tampering | br_like.rs constant | mitigate | Locked by `br_q_prefactor_locked` regression test (Task 0.1) |
| T-7-00-02 | Tampering | mpmath fixture .jsonl files | mitigate | `.sha256` stamps committed alongside; existing Phase-6 Plan 06-N2 sha-check pattern catches drift |
| T-7-00-03 | Repudiation | UAT status flips | accept | 06-HUMAN-UAT.md is content-addressed by commit hash; result lines reference the commit |
</threat_model>

<verification>
- BR_Q_PREFACTOR_F64 typo fix locked by regression test: `cargo nextest run -p xcfun-kernels br_q_prefactor_locked` exits 0
- mpmath fixture regen evidence: `ls validation/mpmath_fixtures/*.sha256 | wc -l` returns ≥ 26
- Auto-tightening verification log preserved in commit body or `/tmp/07-00-task-0.3.log`
- 06-HUMAN-UAT.md `## Summary` shows `passed: 4`, `pending: 2`, `issues: 0`
</verification>

<success_criteria>
- All 4 blocking HUMAN-UAT items (3, 4, 5, 6) closed (or operator-escalated)
- BR_Q_PREFACTOR_F64 = 0.699_291_115_553_117_4 (verified by grep)
- `cargo build --workspace` GREEN
- The 4 items are reflected in 06-HUMAN-UAT.md `## Summary`
</success_criteria>

<output>
After completion, create `.planning/phases/07-python-bindings-release/07-00-SUMMARY.md`.
</output>
