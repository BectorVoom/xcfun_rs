---
phase: 07-python-bindings-release
plan: 10
type: execute
wave: 10
depends_on: [09]
files_modified:
  - CHANGELOG.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: []
must_haves:
  truths:
    - "CHANGELOG.md exists at the repo root in Keep-a-Changelog 1.1.0 format with `[Unreleased]` and `[0.1.0]` sections"
    - "[0.1.0] section enumerates the surface delivered in Phases 1-7 (78 functionals + 50+ aliases + 31 Vars × 3 modes + xcfun-rs facade + xcfun-capi C ABI + xcfun-py Python bindings)"
    - "[0.1.0] notes mention the 2 deferred HUMAN-UAT items (ROCm/Wgpu hardware sweeps → v0.2)"
    - "STATE.md, ROADMAP.md, REQUIREMENTS.md all updated to reflect Phase 7 completion + PY-01..06 marked Complete"
    - "Tag `v0.1.0` is created (annotated, signed if maintainer setup permits) and pushed — triggers `.github/workflows/release.yml`"
  artifacts:
    - path: "CHANGELOG.md"
      provides: "Keep-a-Changelog 1.1.0 format root CHANGELOG with [Unreleased] + [0.1.0] sections"
      contains: "## [0.1.0]"
    - path: ".planning/STATE.md"
      provides: "Phase 7 progress + Phase 7 sign-off entry"
      contains: "Phase 7"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 7 marked [x] Complete with the plan list (07-00..07-10)"
      contains: "Complete"
    - path: ".planning/REQUIREMENTS.md"
      provides: "PY-01..06 traceability rows updated to Complete; the [ ] checkboxes flipped to [x]"
      contains: "PY-01 | Phase 7 | Complete"
  key_links:
    - from: "git tag v0.1.0"
      to: ".github/workflows/release.yml"
      via: "push origin v0.1.0 triggers `on: push: tags: - 'v*'`"
      pattern: "v0\\.1\\.0"
---

<objective>
Capstone plan: write `CHANGELOG.md` per D-13 in Keep-a-Changelog 1.1.0 format,
update `.planning/{STATE,ROADMAP,REQUIREMENTS}.md` to reflect Phase 7 sign-off, and
tag `v0.1.0` to trigger the release pipeline.

Per RESEARCH §10 lines 1736-1786 the CHANGELOG body is already drafted; this plan
finalizes it with the actual sign-off date and any last-minute notes from the prior
nine plans.

The tag push is a CHECKPOINT (autonomous: false) — operator confirms ALL prior
plans are GREEN, the dry-run pipeline (workflow_dispatch on a feature branch) is
GREEN, the xtask release-publish dry-run is GREEN, and the local maintainer's
crates.io credentials + PyPI Trusted Publisher are configured before pushing
the tag. After tag push, the operator runs `xtask release-publish --execute`
locally for the 7 Rust crates, then waits for `.github/workflows/release.yml`
to complete the wheel publish + GH release.

Output: CHANGELOG.md committed; planning artifacts updated; v0.1.0 tag pushed.
</objective>

<execution_context>
@/home/user/Documents/workspace/xcfun_rs/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/Documents/workspace/xcfun_rs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-python-bindings-release/07-CONTEXT.md
@.planning/phases/07-python-bindings-release/07-RESEARCH.md
@CLAUDE.md

<interfaces>
RESEARCH §10 lines 1738-1782 — the CHANGELOG outline draft.

Phase 7 deliverables enumerated by plan:
- 07-00: 4 HUMAN-UAT items cleared + BR_Q_PREFACTOR_F64 typo fix
- 07-01: xcfun-py crate rename + workspace member + dep wiring
- 07-02: pyproject.toml + Python source layout + #[pymodule] skeleton + 11 free fns
- 07-03: XcfunError + abi3 §5 PyException workaround
- 07-04: Functional class + Mode/Vars IntEnum + 9 method delegates
- 07-05: NumPy strict zero-copy eval_vec + cross-language parity gate
- 07-06: README.md
- 07-07: release.yml sdist + 3 wheel jobs + pytest-from-wheel
- 07-08: xtask release-publish driver
- 07-09: release.yml publish-pypi + release-artifacts + github-release
- 07-10: this plan — CHANGELOG + tag

Plan 07-10 is autonomous: false because the tag push is the irreversible action; it
gates on operator approval after pre-tag dry-run.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 10.1: Author root CHANGELOG.md (Keep-a-Changelog 1.1.0)</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - .planning/phases/07-python-bindings-release/07-RESEARCH.md (§10 lines 1738-1782 — verbatim CHANGELOG outline)
    - .planning/phases/07-python-bindings-release/07-CONTEXT.md (D-13 framing)
    - .planning/STATE.md (date for the [0.1.0] header — substitute `<YYYY-MM-DD>` with operator-supplied date at tag time; for the planning commit use today's date)
    - .planning/ROADMAP.md (the 5 success criteria of Phase 7 are the headline deliverables)
    - All 11 plan SUMMARYs from earlier waves (07-00 through 07-09) — these would be authored AFTER each plan executes; for the planning step here, treat them as forward-references and use the plan objectives as a stand-in)
  </read_first>
  <behavior>
    - Test 1: CHANGELOG.md is valid Markdown
    - Test 2: Contains the `[Unreleased]`, `[0.1.0]`, and `### Added` / `### Notes` Keep-a-Changelog sections
    - Test 3: Includes the 2 v0.2 deferrals + the cubecl pre-release framing rationale
  </behavior>
  <action>
    Create `/home/user/Documents/workspace/xcfun_rs/CHANGELOG.md` with the EXACT content
    below (transcribed from RESEARCH §10 lines 1740-1783, with `<YYYY-MM-DD>` for the
    operator to fill in at tag time):

    ```markdown
    # Changelog

    All notable changes to this project will be documented in this file.

    The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
    and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

    ## [Unreleased]

    (empty after the v0.1.0 cut)

    ## [0.1.0] - <YYYY-MM-DD>

    ### Added

    - Initial public release of the Rust-from-scratch reimplementation of xcfun.
    - **78 functionals** across LDA / GGA / metaGGA tiers (Phases 2-4):
      - LDA tier: SLATERX, VWN3C, VWN5C, PW92C, PZ81C, LDAERFX, LDAERFC, LDAERFC_JT,
        TFK, TW, VWK (11 functionals).
      - GGA tier: PBE family (12), Becke family (4), LYP, OPTX (2), PW86/PW91 (4),
        P86 (2), APBE (2), B97 family (6), KTX, BTK, BR family (3), CSC (~36 functionals).
      - metaGGA tier: TPSS family (5), SCAN family (10), M05 family (4), M06 family (8),
        BLOCX (~28 functionals).
    - **50+ aliases** (b3lyp, camb3lyp, bp86, blyp, bhandh, etc.) with multiplicative
      weight composition (Phase 4 ALIAS-01..06).
    - **4 parameters:** XC_EXX (default 0.0), XC_RANGESEP_MU (0.4), XC_CAM_ALPHA (0.19),
      XC_CAM_BETA (0.46) (Phase 4 ALIAS-02..05).
    - **31 `Vars` × 3 evaluation modes** (Phase 2-4):
      - `PartialDerivatives` orders 0..=4 across LDA / GGA / metaGGA.
      - `Potential` (3-direction divergence construction) on LDA + GGA.
      - `Contracted` orders 0..=4 across LDA + GGA + metaGGA exemplars.
    - **`xcfun-rs` Rust facade** — `Functional` struct + 11 free fns; `Send + Sync`;
      zero-allocation per-point `eval` (Phase 5 RS-01..10).
    - **`xcfun-capi` C ABI** — drop-in replacement for `xcfun-master/api/xcfun.h`;
      cbindgen-generated header byte-matched to upstream; cdylib + staticlib + rlib
      triple-crate (Phase 5 CAPI-01..07).
    - **`xcfun-py` Python bindings** via PyO3 0.28.3 + rust-numpy 0.28.0 (Phase 7 PY-01..06):
      - `xcfun_rs.Functional` Python class with `set` / `get` / `is_gga` / `is_metagga`
        / `eval_setup` / `user_eval_setup` / `input_length` / `output_length` / `eval`
        / `eval_vec` / `configure`.
      - 11 module-level free fns: `version`, `splash`, `authors`, `is_compatible_library`,
        `self_test`, `which_vars`, `which_mode`, `enumerate_parameters`,
        `enumerate_aliases`, `describe_short`, `describe_long`.
      - `XcfunError` exception class with `.code: int` (Phase 5 D-08-A C-ABI mapping)
        and `.kind: str` (Rust variant name) attributes; abi3-py310-compatible
        workaround (per RESEARCH §5 CRITICAL FINDING).
      - Strict zero-copy `eval_vec` NumPy contract: `np.float64` C-contiguous input
        only; `TypeError` on layout violation; freshly-allocated 2-D `f64` output.
    - **CPU/GPU `Batch` lifecycle** for `nr_points >= 64` via `cubecl =0.10.0-pre.3`
      (Phase 6 RS-08 + GPU-01..08).
    - **ROCm/HIP primary GPU backend**; CUDA / Metal opt-in feature flags; Wgpu
      portable fallback at relaxed 1e-9 tolerance with `Dependency::ERF` auto-CPU
      fallback.
    - **`xtask release-publish`** topological cargo publish driver (Phase 7 D-15).

    ### Distribution

    - Wheel matrix: `manylinux_2_28_x86_64`, `macosx_11_0_arm64`, `win_amd64`. One
      wheel per platform covers Python 3.10 / 3.11 / 3.12 / 3.13 via abi3-py310.
    - GitHub Release artifacts: pre-built `libxcfun_capi.so` (Linux x86_64),
      `libxcfun_capi.dylib` (macOS arm64), `xcfun_capi.dll` (Windows x86_64), all
      built with `RUSTFLAGS=""` + `-fno-fast-math` discipline; cbindgen-generated
      `xcfun.h` attached.
    - PyPI distribution name: `xcfun_rs` (underscore, PEP 503-normalized).
    - Crates published to crates.io (in topological dep order):
      `xcfun-ad`, `xcfun-core`, `xcfun-kernels`, `xcfun-eval`, `xcfun-gpu`, `xcfun-rs`,
      `xcfun-capi`.

    ### Notes

    - This is a `0.x` release — the API may evolve before `1.0`. cubecl is at
      `=0.10.0-pre.3` (a pre-release dependency); a stable 1.0 SLA on top of an
      unstable dep would be contradictory.
    - **2 Phase-6 HUMAN-UAT items deferred to v0.2:**
      - **ROCm tier-3 1e-13 hardware sweep** — no AMD/ROCm GPU on dev / cloud-CI runner.
      - **Wgpu tier-3 1e-9 hardware sweep (excluding ERF)** — no SHADER_F64-capable
        Vulkan adapter.
    - Architecturally complete, validated on CPU at 1e-12; the GPU paths compile
      against ROCm / CUDA / Wgpu cubecl runtimes per design but the hardware-tier-3
      sweeps are pending v0.2 hardware-CI cycle.

    ### Architecture

    - 7 published Rust crates: `xcfun-ad` (cubecl-native AD; `CTaylor<F, N>` +
      `*_expand` + composed ops), `xcfun-core` (types + registry), `xcfun-kernels`
      (per-functional `#[cube]` bodies + DensVarsDev + dispatch_kernel), `xcfun-eval`
      (cubecl launcher), `xcfun-gpu` (Backend enum + Batch lifecycle + auto_backend),
      `xcfun-rs` (Rust facade), `xcfun-capi` (C ABI).
    - 1 PyPI distribution: `xcfun_rs` (Python bindings via `xcfun-py`).
    - License: [MPL-2.0](https://www.mozilla.org/MPL/2.0/) (inherited from xcfun).

    [Unreleased]: https://github.com/<owner>/xcfun_rs/compare/v0.1.0...HEAD
    [0.1.0]:      https://github.com/<owner>/xcfun_rs/releases/tag/v0.1.0
    ```

    Note: `<YYYY-MM-DD>` and `<owner>` are placeholders — operator fills in at tag
    time (or via a sed step in `xtask release-publish`). The placeholders are NOT
    a planning gap; they are intentionally surfaced for last-minute fill-in.

    Commit message: `docs(07-10): CHANGELOG.md v0.1.0 release notes (Keep-a-Changelog 1.1.0; D-13 framing)`.
  </action>
  <verify>
    <automated>test -f CHANGELOG.md &amp;&amp; grep -F '## [Unreleased]' CHANGELOG.md &amp;&amp; grep -F '## [0.1.0]' CHANGELOG.md &amp;&amp; grep -F '### Added' CHANGELOG.md &amp;&amp; grep -F '### Notes' CHANGELOG.md &amp;&amp; grep -F 'cubecl =0.10.0-pre.3' CHANGELOG.md &amp;&amp; grep -F 'PY-01..06' CHANGELOG.md &amp;&amp; grep -F 'xcfun_rs.Functional' CHANGELOG.md &amp;&amp; grep -F 'manylinux_2_28' CHANGELOG.md &amp;&amp; grep -F 'abi3-py310' CHANGELOG.md &amp;&amp; grep -F 'ROCm tier-3 1e-13 hardware sweep' CHANGELOG.md</automated>
  </verify>
  <acceptance_criteria>
    - test -f CHANGELOG.md exits 0
    - grep -F '## [Unreleased]' CHANGELOG.md returns 1 line
    - grep -F '## [0.1.0]' CHANGELOG.md returns 1 line
    - grep -F '### Added' CHANGELOG.md returns 1 line
    - grep -F '### Notes' CHANGELOG.md returns 1 line
    - grep -F 'Keep a Changelog' CHANGELOG.md returns 1 line
    - grep -F 'cubecl =0.10.0-pre.3' CHANGELOG.md returns 1 line (D-13 framing)
    - grep -F 'PY-01..06' CHANGELOG.md returns 1 line
    - grep -F 'manylinux_2_28' CHANGELOG.md returns 1 line
    - grep -F 'abi3-py310' CHANGELOG.md returns 1 line
    - grep -F 'ROCm tier-3 1e-13 hardware sweep' CHANGELOG.md returns 1 line (v0.2 deferral lock)
    - grep -F '78 functionals' CHANGELOG.md returns ≥ 1 line
    - grep -F 'MPL-2.0' CHANGELOG.md returns ≥ 1 line
    - wc -l CHANGELOG.md returns ≥ 60 (substantive content)
  </acceptance_criteria>
  <done>CHANGELOG.md committed; placeholders for `<YYYY-MM-DD>` and `<owner>` retained for operator fill-in.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 10.2: Update STATE.md / ROADMAP.md / REQUIREMENTS.md for Phase 7 sign-off</name>
  <files>.planning/STATE.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/STATE.md (current state — Phase 6 complete; this plan adds Phase 7 entry)
    - .planning/ROADMAP.md (Phase 7 section + Plans line + Progress Table)
    - .planning/REQUIREMENTS.md (PY-01..06 rows in the body + the Traceability table at the bottom)
  </read_first>
  <behavior>
    - Test 1: STATE.md `progress.completed_phases` increments from 6 to 7
    - Test 2: STATE.md `progress.percent` updates accordingly (7/8 = ~88%)
    - Test 3: ROADMAP.md Phase 7 entry shows `[x]` AND the plan list lists 07-00..07-10
    - Test 4: REQUIREMENTS.md PY-01..06 rows show `[x]` (not `[ ]`)
    - Test 5: REQUIREMENTS.md Traceability table PY-01..06 status flipped from Pending to Complete
  </behavior>
  <action>
    Step 1: Edit `.planning/STATE.md`. Update the front-matter:
    - `progress.completed_phases: 7` (was 6)
    - `progress.percent: 88` (was 86; 7/8 = 87.5% → round to 88)
    - `last_updated: "<YYYY-MM-DD>T..."` — operator-supplied date

    Update the body:
    - Top-of-file dated entry: prepend a new paragraph dated today (operator fills in
      today's date) summarizing Phase 7 sign-off: "Phase 7 sign-off — 11/11 plans
      shipped (07-00 UAT clearance + BR typo fix; 07-01 xcfun-py rename; 07-02
      #[pymodule] + 11 free fns; 07-03 XcfunError abi3 §5 workaround; 07-04 Functional
      class; 07-05 NumPy strict zero-copy + parity; 07-06 README; 07-07 wheel matrix
      CI; 07-08 xtask release-publish; 07-09 publish-pypi + release-artifacts; 07-10
      CHANGELOG + tag). PY-01..06 marked Complete. v0.1.0 tag pushed; release.yml ran;
      crates.io publish via xtask release-publish --execute completed."
    - Add to `## Current Position` section: "Phase: 07 (python-bindings-release) —
      **COMPLETE (<YYYY-MM-DD>)**".
    - Update `**Progress:** [████████] 88% (7/8 phases)`.

    Step 2: Edit `.planning/ROADMAP.md`. In the `## Phases` list (line ~22-29), flip:
    ```
    - [ ] **Phase 7: Python Bindings (`xcfun-py`) + Release** - PyO3 0.28 + rust-numpy 0.28 wheel passing `pytest`, crates published, release ceremony
    ```
    to:
    ```
    - [x] **Phase 7: Python Bindings (`xcfun-py`) + Release** - **Complete (<YYYY-MM-DD>)** — PyO3 0.28.3 + rust-numpy 0.28.0 wheel for {linux x86_64, macos arm64, win amd64}; PY-01..06 Complete; 7 Rust crates published to crates.io via xtask release-publish; v0.1.0 tag triggered release.yml.
    ```

    In the Phase 7 detail block (line ~192-203), update `**Plans**: TBD` to the actual
    11-plan list:
    ```
    **Plans**: 11 plans across 5 waves (granularity standard)
      - [x] 07-00-PLAN.md — Wave 0: clear 4 blocking HUMAN-UAT items + BR_Q_PREFACTOR_F64 typo fix (D-14)
      - [x] 07-01-PLAN.md — Wave 0: xcfun-python → xcfun-py rename + workspace member promotion (D-01, D-03)
      - [x] 07-02-PLAN.md — Wave 1: pyproject.toml + #[pymodule] _native skeleton + 11 free fns (PY-01, PY-04)
      - [x] 07-03-PLAN.md — Wave 2: XcfunError + abi3 §5 PyException workaround (PY-05; D-09, D-10)
      - [x] 07-04-PLAN.md — Wave 2: Functional #[pyclass] + Mode/Vars IntEnum + 9 method delegates (PY-02; D-05, D-06, D-12)
      - [x] 07-05-PLAN.md — Wave 2: NumPy strict zero-copy eval_vec + cross-language parity (PY-03; D-07, D-08)
      - [x] 07-06-PLAN.md — Wave 2: crates/xcfun-py/README.md (D-03, D-04, D-13)
      - [x] 07-07-PLAN.md — Wave 3: release.yml sdist + 3 wheel matrix + pytest-from-wheel (PY-06)
      - [x] 07-08-PLAN.md — Wave 4: xtask release-publish topological driver (D-15)
      - [x] 07-09-PLAN.md — Wave 4: release.yml publish-pypi (OIDC) + release-artifacts + github-release (D-15, D-16)
      - [x] 07-10-PLAN.md — Wave 4: CHANGELOG.md + tag v0.1.0 (D-13)
    ```

    In the `## Progress Table` (line ~207-216), flip the Phase 7 row:
    ```
    | 7. Python Bindings + Release | 11/11 | Complete | <YYYY-MM-DD> |
    ```

    Step 3: Edit `.planning/REQUIREMENTS.md`:
    - Body section "### Python Bindings": flip each `- [ ] **PY-XX**:` to `- [x] **PY-XX**:`
      for PY-01 through PY-06. Append `(Plan 07-XX; ...)` to each line referencing the
      plan that closed it (07-02 for PY-01 + PY-04; 07-04 for PY-02; 07-05 for PY-03;
      07-03 for PY-05; 07-07 for PY-06).
    - Traceability table at the bottom: change PY-01..06 status from `Pending` to
      `Complete`.

    Commit message: `docs(07-10): mark Phase 7 + PY-01..06 Complete in STATE / ROADMAP / REQUIREMENTS`.
  </action>
  <verify>
    <automated>grep -F 'completed_phases: 7' .planning/STATE.md &amp;&amp; grep -F '7. Python Bindings + Release | 11/11 | Complete' .planning/ROADMAP.md &amp;&amp; grep -F '[x] **Phase 7: Python Bindings' .planning/ROADMAP.md &amp;&amp; test "$(grep -cE '^\s*-\s*\[x\]\s*\*\*PY-[0-9]+\*\*' .planning/REQUIREMENTS.md)" -ge 6 &amp;&amp; test "$(grep -cE '\| PY-0[1-6] \| Phase 7 \| Complete' .planning/REQUIREMENTS.md)" -eq 6</automated>
  </verify>
  <acceptance_criteria>
    - grep -F 'completed_phases: 7' .planning/STATE.md returns 1 line
    - grep -F 'percent: 88' .planning/STATE.md returns 1 line
    - grep -F 'Phase 7' .planning/STATE.md returns ≥ 2 lines (front-matter + content)
    - grep -F '[x] **Phase 7: Python Bindings' .planning/ROADMAP.md returns 1 line (with `[x]`, NOT `[ ]`)
    - grep -F '7. Python Bindings + Release | 11/11 | Complete' .planning/ROADMAP.md returns 1 line (Progress Table row)
    - grep -cE '^\s*-\s*\[x\]\s*07-[0-9]+-PLAN\.md' .planning/ROADMAP.md returns ≥ 11 (all 11 Phase 7 plans listed [x])
    - grep -cE '^\s*-\s*\[x\]\s*\*\*PY-[0-9]+\*\*' .planning/REQUIREMENTS.md returns ≥ 6 (PY-01..06 all `[x]`)
    - grep -cE '\| PY-0[1-6] \| Phase 7 \| Complete' .planning/REQUIREMENTS.md returns 6 (Traceability rows)
    - grep -cE '^\s*-\s*\[ \]\s*\*\*PY-' .planning/REQUIREMENTS.md returns 0 (no PY-XX still pending)
  </acceptance_criteria>
  <done>STATE / ROADMAP / REQUIREMENTS reflect Phase 7 completion; PY-01..06 marked Complete with plan-ID provenance.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 10.3: Operator pre-tag dry-run + push v0.1.0 tag + run xtask release-publish --execute</name>
  <files>(no files modified by this task — it's the tag push + the local execute step)</files>
  <read_first>
    - CHANGELOG.md (the operator fills in `<YYYY-MM-DD>` and `<owner>` placeholders pre-tag)
    - .github/workflows/release.yml (per-job permissions + tag-push gate)
    - xtask/src/bin/release_publish.rs (the local cargo publish driver)
    - .planning/phases/07-python-bindings-release/07-CONTEXT.md (D-13/D-14/D-15/D-16 — release ceremony intent)
  </read_first>
  <what-built>
    Plans 07-00 through 07-09 + Plan 07-10 Tasks 10.1 + 10.2 deliver every artifact
    needed for the v0.1.0 release. The remaining steps are operator-driven and
    irreversible — pushing the v0.1.0 tag is what publishes to crates.io + PyPI +
    GitHub Releases. Yanks are non-recoverable; this checkpoint exists so the operator
    confirms every gate is green before pulling the trigger.
  </what-built>
  <how-to-verify>
    Pre-tag verification (operator-driven; ALL must pass before pushing tag):

    1. **Local pytest GREEN:**
       ```
       cd crates/xcfun-py
       maturin develop --release
       pytest tests/ -q
       ```
       Exit 0.

    2. **xtask release-publish dry-run GREEN:**
       ```
       cargo run -p xtask --bin release-publish -- --dry-run
       ```
       Output prints 7 crates in topological order; per-crate `cargo publish --dry-run`
       exits 0 for each.

    3. **CI dry-run GREEN (workflow_dispatch on a feature branch):**
       Push a feature branch, manually trigger `release.yml` via GitHub UI:
       - sdist + linux + macos + windows jobs all GREEN
       - publish-pypi + release-artifacts + github-release all SKIPPED (the
         `if: tags/v*` gate filters them out for branch pushes)

    4. **CHANGELOG placeholders filled:**
       - Replace `<YYYY-MM-DD>` with today's date in both `CHANGELOG.md` and the
         release-notes header.
       - Replace `<owner>` with the actual GitHub owner in `CHANGELOG.md`,
         `crates/xcfun-py/README.md`, and `crates/xcfun-py/pyproject.toml`.
       - Commit: `docs(07-10): finalize CHANGELOG + README placeholder fill-in for v0.1.0`.

    5. **Workspace at v0.1.0:**
       ```
       grep '^version = "0.1.0"' Cargo.toml
       ```
       Must return 1 line. (Already pinned in workspace `Cargo.toml`; no edit needed
       at this step.)

    6. **PyPI Trusted Publisher configured:**
       Visit https://pypi.org/manage/account/publishing/. Confirm the pending entry
       matches:
       - Project name: `xcfun_rs`
       - Owner: `<github-owner>`
       - Repository: `<github-repo>`
       - Workflow filename: `release.yml`
       - Environment: `pypi`
       (One-time setup; persists across releases. If not present, follow RESEARCH §7.3
       lines 1641-1652 to add it.)

    7. **Maintainer's `~/.cargo/credentials.toml` has crates.io token** (for the local
       `xtask release-publish --execute` step).

    Tag + execute (irreversible from here):

    8. **Create + push the tag:**
       ```
       git tag -a v0.1.0 -m "v0.1.0 — initial public release"
       git push origin v0.1.0
       ```
       This triggers `.github/workflows/release.yml` end-to-end. Watch the GH Actions
       tab — sdist + 3 wheel jobs + 3 release-ceremony jobs (~15-25 min wall-clock).

    9. **Run xtask release-publish --execute LOCALLY** (D-15 — driven from maintainer
       machine, NOT from CI):
       ```
       cargo run -p xtask --bin release-publish -- --execute
       ```
       Exit 0; 7 Rust crates published to crates.io in topological order with
       index-propagation polling between each. (~5-10 min wall-clock.)

    10. **Verify:**
       - `pip install xcfun_rs` from a clean Python venv. Then
         `python -c "import xcfun_rs; print(xcfun_rs.version())"` prints `0.1.0`.
       - `cargo install xcfun-rs` from a clean machine. Or `cargo new test_install &&
         cd test_install && cargo add xcfun-rs && cargo build`.
       - GitHub Release page for v0.1.0 (https://github.com/<owner>/xcfun_rs/releases/tag/v0.1.0)
         shows the source tarball + 3 platform-specific `libxcfun_capi.{so,dylib,dll}`
         + `xcfun.h`.

    11. **Author SUMMARY:**
       Write `.planning/phases/07-python-bindings-release/07-10-SUMMARY.md` recording
       the actual tag commit hash, the GH Actions run URL, the PyPI page URL, and the
       git tag itself. Update `.planning/STATE.md` `last_updated:` to the actual
       sign-off date.
  </how-to-verify>
  <resume-signal>
    Type "approved" if ALL 11 verification steps above are GREEN and all 3 publish
    surfaces (crates.io, PyPI, GitHub Releases) show v0.1.0 artifacts.

    Type "block" if ANY of the 11 steps fails. The recovery path depends on which step:
    - Steps 1-7 (pre-tag): fix the failing artifact and retry; tag is NOT yet pushed.
    - Step 8 (tag pushed but release.yml fails): the tag itself is fine; rerun the
      failing job. If publish-pypi fails on `skip-existing: true`, the wheel is already
      uploaded and the next attempt is a no-op. If release-artifacts fails on a single
      platform, attach the artifact manually via the GH UI.
    - Step 9 (xtask release-publish --execute fails mid-stream): use `--from <crate>`
      to resume. Already-published crates are skipped via `is_published_at_version`
      (idempotent).
    - Step 10 (post-publish smoke fail): document in `.planning/phases/07-python-
      bindings-release/07-HUMAN-UAT.md` (newly created — Phase 7 follow-ups). The
      v0.1.0 tag stands; v0.1.1 patch ships the fix per Pitfall 5 (yanks are
      irreversible).
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Maintainer's machine → crates.io | Local-driven xtask release-publish; uses ~/.cargo/credentials.toml |
| GitHub Actions runner → PyPI | OIDC short-lived token (no long-lived secret) |
| GitHub Actions runner → GitHub Release | contents: write JOB-level permission |
| Tag push → release pipeline | refs/tags/v* gate; only authenticated push triggers publish |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-10-01 (T-7-V14) | Tampering | Botched v0.1.0 with PyPI yank-irreversibility | mitigate | Mandatory checkpoint Task 10.3 with 7 pre-tag dry-run gates; xtask --dry-run + workflow_dispatch dry-run + local pytest before tag push |
| T-7-10-02 (T-7-V14) | Repudiation | Tag pushed by unauthorized actor | mitigate | GH branch protection rules (operator-managed; outside workflow scope); annotated/signed tag if maintainer setup permits |
| T-7-10-03 | Information disclosure | CHANGELOG / README placeholders revealing pre-tag email or org | accept | `<owner>` placeholder is intentional; operator fills in once at tag time |
| T-7-10-04 (T-7-V14) | Tampering | xtask release-publish run with stale Cargo.lock | accept | cargo metadata reads working tree; cargo publish itself catches mismatch via dry-run |
</threat_model>

<verification>
- CHANGELOG.md committed in Keep-a-Changelog 1.1.0 format
- STATE / ROADMAP / REQUIREMENTS reflect Phase 7 + PY-01..06 Complete
- Pre-tag dry-run GREEN on all 7 verification gates
- Tag push triggers release.yml; release-artifacts + publish-pypi + github-release all SUCCEED
- xtask release-publish --execute publishes 7 Rust crates
- pip install xcfun_rs from a clean venv works post-publish
</verification>

<success_criteria>
- v0.1.0 published on PyPI (https://pypi.org/project/xcfun_rs/0.1.0/)
- 7 Rust crates published on crates.io (xcfun-ad, xcfun-core, xcfun-kernels, xcfun-eval, xcfun-gpu, xcfun-rs, xcfun-capi all at 0.1.0)
- GitHub Release v0.1.0 attached with source tarball + libxcfun_capi.{so,dylib,dll} + xcfun.h
- 5/5 ROADMAP § Phase 7 success criteria GREEN end-to-end
- 07-10-SUMMARY.md authored with actual tag commit hash + GH Actions URL + PyPI URL
</success_criteria>

<output>
After tag push + xtask release-publish --execute completes, create `.planning/phases/07-python-bindings-release/07-10-SUMMARY.md` recording:
- v0.1.0 tag commit hash
- GH Actions release.yml run URL
- PyPI page URL
- crates.io publish timestamps for each of the 7 Rust crates
- Any operator notes on hiccups (e.g., a `--from <crate>` resume after index propagation timeout)
- Update .planning/STATE.md `last_updated` to the actual tag date
</output>
