---
phase: 03-gga-tier-mode-potential
plan: 03
type: execute
wave: 3
depends_on: [03-00, 03-01, 03-02]
files_modified:
  - crates/xcfun-eval/src/functionals/gga/shared/pw91_like.rs
  - crates/xcfun-eval/src/functionals/gga/shared/optx.rs
  - crates/xcfun-eval/src/functionals/gga/mod.rs
  - crates/xcfun-eval/src/functionals/gga/optx/mod.rs
  - crates/xcfun-eval/src/functionals/gga/optx/optx.rs
  - crates/xcfun-eval/src/functionals/gga/optx/optxcorr.rs
  - crates/xcfun-eval/src/functionals/gga/pw91/mod.rs
  - crates/xcfun-eval/src/functionals/gga/pw91/pw86x.rs
  - crates/xcfun-eval/src/functionals/gga/pw91/pw91x.rs
  - crates/xcfun-eval/src/functionals/gga/pw91/pw91c.rs
  - crates/xcfun-eval/src/functionals/gga/pw91/pw91k.rs
  - crates/xcfun-eval/src/functionals/gga/p86/mod.rs
  - crates/xcfun-eval/src/functionals/gga/p86/p86c.rs
  - crates/xcfun-eval/src/functionals/gga/p86/p86corrc.rs
  - crates/xcfun-eval/src/functionals/gga/apbe/mod.rs
  - crates/xcfun-eval/src/functionals/gga/apbe/apbex.rs
  - crates/xcfun-eval/src/functionals/gga/apbe/apbec.rs
  - crates/xcfun-eval/src/dispatch.rs
  - validation/build.rs
autonomous: true
requirements: [GGA-05, GGA-06, GGA-07, GGA-08, MODE-01]
tags: [xcfun-eval, gga-kernels, optx, pw91, p86, apbe, wave2]

must_haves:
  truths:
    - "10 new `#[cube] fn <name>_kernel<F>` bodies — 2 OPTX (GGA-05) + 4 PW86/PW91 (GGA-06) + 2 P86 (GGA-07) + 2 APBE (GGA-08) — compile and dispatch correctly via `dispatch_kernel` extensions (FunctionalId ids 17, 18, 1, 26, 27, 77, 56, 57, 67, 68)."
    - "Shared-helper full bodies ship: `pw91_like::{pw91k_prefactor, pw91xk_enhancement}`, `optx::{g_xa2, optx_enhancement}` — each composed via `ctaylor_sqrtx_asinh_sqrtx` (D-06) or `ctaylor_pow` as appropriate."
    - "`cargo xtask validate --backend cpu --order 2 --filter 'optx|pw91|p86|apbe'` GREEN at 1e-12 for all 10 functionals."
    - "`supports(id)` bitmap bumps from 28 (Wave 2) to 38."
    - "PW91C is verified as THE LONGEST GGA body (~120 Rust LOC mirror of `pw91c.cpp:39-87`) and passes tier-2 at 1e-12."
    - "P86C/P86CORRC `pz81eps` re-export from Phase-2 `lda/pz81c.rs` verified reachable from `gga/p86/` (Pitfall G7 analog)."
  artifacts:
    - path: "crates/xcfun-eval/src/functionals/gga/optx/"
      provides: "2 kernel files (optx.rs + optxcorr.rs) per GGA-05"
    - path: "crates/xcfun-eval/src/functionals/gga/pw91/"
      provides: "4 kernel files (pw86x.rs, pw91x.rs, pw91c.rs, pw91k.rs) per GGA-06"
    - path: "crates/xcfun-eval/src/functionals/gga/p86/"
      provides: "2 kernel files (p86c.rs, p86corrc.rs) per GGA-07"
    - path: "crates/xcfun-eval/src/functionals/gga/apbe/"
      provides: "2 kernel files (apbex.rs, apbec.rs) per GGA-08"
    - path: "crates/xcfun-eval/src/dispatch.rs"
      provides: "10 new comptime arms + supports() bitmap includes ids {1, 17, 18, 26, 27, 56, 57, 67, 68, 77}"
  key_links:
    - from: "crates/xcfun-eval/src/functionals/gga/pw91/pw91x.rs::pw91x_kernel"
      to: "xcfun_ad::math::ctaylor_sqrtx_asinh_sqrtx (D-06)"
      via: "composition via pw91_like::pw91xk_enhancement"
      pattern: "ctaylor_sqrtx_asinh_sqrtx"
    - from: "crates/xcfun-eval/src/functionals/gga/p86/p86c.rs::p86c_kernel"
      to: "crate::functionals::lda::pz81c::pz81eps"
      via: "cross-tier import of Phase-2 LSDA baseline helper"
      pattern: "pz81eps"
    - from: "crates/xcfun-eval/src/functionals/gga/apbe/apbec.rs::apbec_kernel"
      to: "crate::functionals::gga::shared::pbec_eps::a_expm1_inner"
      via: "function call inside #[cube] body"
      pattern: "pbec_eps::a_expm1_inner"
---

<objective>
Ship Wave 3 of Phase 3: 10 GGA kernels across 4 families — OPTX (2), PW86/PW91 (4),
P86 (2), APBE (2) — per D-03. PW91X + PW91K exercise the `ctaylor_sqrtx_asinh_sqrtx`
primitive (D-06) at strict 1e-12 for the FIRST TIME in a kernel body (Becke also
uses it in Wave 2 but PW91 composes through the `pw91xk_enhancement` shared helper).

Purpose: PW91C is flagged in RESEARCH as "the longest GGA body" (~120 LOC); it
stress-tests operation-order preservation. P86C verifies Phase-2 `pz81eps`
cross-tier import. APBEC re-uses shared `pbec_eps` from Wave 2. 10 more kernels
means Waves 1+2+3 = 27 of 36 GGA kernels shipped; Wave 4 closes with B97+KTX+BTK
(8 more).

Output: 10 Rust kernel files + 10 dispatch arms + tier-2 GREEN for all 10 at
orders 0, 1, 2 under strict 1e-12. c_stubs.cpp shrinks by another 10 (50 → 40).
</objective>

<execution_context>
@/home/chemtech/workspace/xcfun_rs/.claude/get-shit-done/workflows/execute-plan.md
@/home/chemtech/workspace/xcfun_rs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-gga-tier-mode-potential/03-CONTEXT.md
@.planning/phases/03-gga-tier-mode-potential/03-RESEARCH.md
@.planning/phases/03-gga-tier-mode-potential/03-PATTERNS.md
@.planning/phases/03-gga-tier-mode-potential/03-VALIDATION.md
@.planning/phases/03-gga-tier-mode-potential/03-01-PLAN.md
@.planning/phases/03-gga-tier-mode-potential/03-02-PLAN.md
@crates/xcfun-eval/src/functionals/lda/slaterx.rs
@crates/xcfun-eval/src/functionals/lda/pw92c.rs
@crates/xcfun-eval/src/functionals/lda/pz81c.rs
@crates/xcfun-eval/src/functionals/lda/tw.rs
@crates/xcfun-eval/src/dispatch.rs
@validation/build.rs

<interfaces>
From `crates/xcfun-eval/src/functionals/lda/pz81c.rs` (cross-tier import target for P86):
```rust
pub #[cube] fn pz81eps<F: Float>(d: &DensVarsDev<F>, out: &mut Array<F>, #[comptime] n: u32)
```
Verify this is exported at `crate::functionals::lda::pz81c::pz81eps` (or re-exported at module root). If `pub` not set, EXECUTOR extends to make it accessible from gga/p86/.

From `crates/xcfun-eval/src/functionals/lda/tw.rs` (LDA analog for PW91K):
```rust
//! XC_TW — Thomas-Fermi kinetic with Weizsäcker correction. LDA-10 (kinetic-GGA).
```

From `xcfun-master/src/functionals/pw91c.cpp:39-87` (PW91C body — the longest in Phase 3):
```cpp
template <class num>
static num pw91c(const densvars<num> &d) {
  const double alpha = 0.09;
  const double beta_ = 0.0667263212;     // β from pw91c
  const double Cc0 = 0.004235;
  // ... T² term, expm1, log(1 + ...), compound terms ...
  return ...;
}
```

From `xcfun-master/src/functionals/pw9xx.hpp:67-87` (pw91xk_enhancement target):
```cpp
// PW91 exchange enhancement factor
template <class T>
static T pw91xk_enhancement(T s2, const double a1, const double a2, const double a3,
                             const double a4, const double a5, const double b) {
  // (1 + a1·s²·sqrtx_asinh_sqrtx(a2·s²) + (a3 - a4·exp(-100·s²))·s²)
  //   / (1 + a1·s²·sqrtx_asinh_sqrtx(a2·s²) + b·s⁴)
  // PW91X:  a1=0.19645, a2=7.7956, a3=0.2743, a4=0.1508, a5=0.004, b=a5
  // PW91K:  different parameter set (kinetic variant)
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port OPTX family × 2 + flesh out `shared/optx.rs` full bodies</name>
  <read_first>
    - `/home/chemtech/workspace/xcfun_rs/xcfun-master/src/functionals/optx.cpp` (full file, ~30 lines)
    - `/home/chemtech/workspace/xcfun_rs/xcfun-master/src/functionals/optxcorr.cpp` (full file, ~35 lines)
    - `/home/chemtech/workspace/xcfun_rs/crates/xcfun-eval/src/functionals/gga/shared/optx.rs` (from plan 03-01 — skeletons; full bodies land here)
    - `/home/chemtech/workspace/xcfun_rs/crates/xcfun-eval/src/functionals/gga/shared/constants.rs` — confirm OPTX_A1_F64, OPTX_A2_F64, OPTX_GAMMA_F64 present
    - `.planning/phases/03-gga-tier-mode-potential/03-RESEARCH.md` §File-by-File OPTX (lines 765-772)
    - `.planning/phases/03-gga-tier-mode-potential/03-PATTERNS.md` Area E OPTX entry
  </read_first>
  <files>
    crates/xcfun-eval/src/functionals/gga/shared/optx.rs (modify — full bodies),
    crates/xcfun-eval/src/functionals/gga/optx/mod.rs (NEW),
    crates/xcfun-eval/src/functionals/gga/optx/optx.rs (NEW),
    crates/xcfun-eval/src/functionals/gga/optx/optxcorr.rs (NEW),
    crates/xcfun-eval/src/functionals/gga/mod.rs (modify — add `pub mod optx;`)
  </files>
  <behavior>
    - Test 1: `shared/optx.rs::g_xa2` FULL BODY implements `γ · grad² · ρ^(-8/3)` via `ctaylor_pow(ρ, -8/3)` + `ctaylor_scalar_mul(γ)` + `ctaylor_mul(grad², ...)`.
    - Test 2: `shared/optx.rs::optx_enhancement` FULL BODY implements the OPTX enhancement factor verbatim from `optx.cpp:22-24`.
    - Test 3: `gga/optx/optx.rs::optx_kernel` + `gga/optx/optxcorr.rs::optxcorr_kernel` compile + tier-2 GREEN at 1e-12 (both IDs lack upstream test_in per §File-by-File so only tier-2 parity gates).
  </behavior>
  <action>
    **Step A — `shared/optx.rs` full bodies:**

    **`g_xa2<F>(rho, grad2, out, n)`** — Port `optx.cpp:20`: `g_xa(a, gaa) = γ · gaa · a^(-8/3)`. Because GGA is spin-resolved here (single-spin form), argument `grad2 = gaa` (per α-channel), `rho = a`.
    ```rust
    use xcfun_ad::ctaylor_rec::mul::ctaylor_mul;
    use xcfun_ad::ctaylor::ctaylor_scalar_mul;
    use xcfun_ad::math::ctaylor_pow;
    // ... body:
    let mut rho_m83 = Array::<F>::new(size);
    ctaylor_pow::<F>(rho, F::cast_from(-8.0_f64 / 3.0_f64), &mut rho_m83, n);
    let mut prod = Array::<F>::new(size);
    ctaylor_mul::<F>(grad2, &rho_m83, &mut prod, n);
    ctaylor_scalar_mul::<F>(&prod, F::cast_from(OPTX_GAMMA_F64), out, n);
    ```

    **`optx_enhancement<F>(gx, a1, a2, out, n)`** — Port `optx.cpp:22-24`:
    ```cpp
    num term = g_xa2 / (1 + g_xa2);
    return a1 * e_x_LSDA_a + a2 * term * term * a_43;   // (approximate — read upstream)
    ```
    Actually read the C++ body carefully — OPTX's enhancement uses `(g_x/(1+g_x))² · (LSDA_term)`. Executor ports operation-by-operation; ~30 Rust LOC.

    **Step B — `gga/optx/mod.rs` + `optx.rs` + `optxcorr.rs`.**

    | FunctionalId | File | C++ src (lines) | Shared helpers | Key formula |
    |--------------|------|-----------------|---------------|-------------|
    | XC_OPTX (17) | `gga/optx/optx.rs` | `optx.cpp:18-26` | `optx::{g_xa2, optx_enhancement}` | OPTX = 1.05151·LSDA_a + 1.43169·(g_xa²/(1+g_xa²))²·a_43 + β-twin |
    | XC_OPTXCORR (18) | `gga/optx/optxcorr.rs` | `optxcorr.cpp:18-33` | `optx::{g_xa2, optx_enhancement}` | OPTXCORR = just the gradient-correction term: 1.43169·(g_xa²/(1+g_xa²))²·a_43 + β-twin (no LSDA part) |

    **Step C — commit per-family** (recommended 1 commit for shared bodies + 1 commit per functional = 3 total for OPTX).
  </action>
  <verify>
    <automated>cargo build -p xcfun-eval --features testing &amp;&amp; rg -c "^pub fn (optx|optxcorr)_kernel" crates/xcfun-eval/src/functionals/gga/optx/ &amp;&amp; cargo xtask check-no-mul-add</automated>
  </verify>
  <acceptance_criteria>
    - `ls crates/xcfun-eval/src/functionals/gga/optx/` prints 3 entries (`mod.rs optx.rs optxcorr.rs`).
    - `rg -c "^pub fn (optx|optxcorr)_kernel" crates/xcfun-eval/src/functionals/gga/optx/` = 2.
    - `rg "F::new\(0\.006" crates/xcfun-eval/src/functionals/gga/` prints ZERO — confirms OPTX_GAMMA_F64 is used via `F::cast_from` (SP-2).
    - `shared/optx.rs` has no `_ = (...)` silencer lines — full bodies ported.
    - `cargo build -p xcfun-eval --features testing` exits 0.
  </acceptance_criteria>
  <done>
    OPTX family compiles. Ready for tier-2 (Task 4).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Port PW86/PW91 family × 4 kernels + flesh out `shared/pw91_like.rs` full bodies (pw91k_prefactor + pw91xk_enhancement)</name>
  <read_first>
    - `/home/chemtech/workspace/xcfun_rs/xcfun-master/src/functionals/pw86x.cpp` (full file, ~35 lines)
    - `/home/chemtech/workspace/xcfun_rs/xcfun-master/src/functionals/pw91x.cpp` (full file, ~30 lines)
    - `/home/chemtech/workspace/xcfun_rs/xcfun-master/src/functionals/pw91c.cpp` (full file — 87 lines; **the longest body**)
    - `/home/chemtech/workspace/xcfun_rs/xcfun-master/src/functionals/pw91k.cpp` (full file, ~30 lines)
    - `/home/chemtech/workspace/xcfun_rs/xcfun-master/src/functionals/pw9xx.hpp` (full file, ~90 lines) — `pw91k_prefactor` + `pw91xk_enhancement` port targets
    - `/home/chemtech/workspace/xcfun_rs/crates/xcfun-eval/src/functionals/gga/shared/pw91_like.rs` (from plan 03-01 + 03-02 — `s2`, `chi2`, `prefactor` already full; `pw91k_prefactor` + `pw91xk_enhancement` are skeletons — land full bodies here)
    - `/home/chemtech/workspace/xcfun_rs/crates/xcfun-eval/src/functionals/lda/pw92eps.rs` — PW91C LSDA baseline uses PW92 (confirm export)
    - `.planning/phases/03-gga-tier-mode-potential/03-RESEARCH.md` §File-by-File PW86/PW91 (lines 774-783) + Pitfall G2 (sqrtx_asinh_sqrtx Padé branch precision)
  </read_first>
  <files>
    crates/xcfun-eval/src/functionals/gga/shared/pw91_like.rs (modify — full bodies),
    crates/xcfun-eval/src/functionals/gga/pw91/mod.rs (NEW),
    crates/xcfun-eval/src/functionals/gga/pw91/pw86x.rs (NEW),
    crates/xcfun-eval/src/functionals/gga/pw91/pw91x.rs (NEW),
    crates/xcfun-eval/src/functionals/gga/pw91/pw91c.rs (NEW),
    crates/xcfun-eval/src/functionals/gga/pw91/pw91k.rs (NEW),
    crates/xcfun-eval/src/functionals/gga/mod.rs (modify — add `pub mod pw91;`)
  </files>
  <behavior>
    - Test 1: `shared/pw91_like.rs::pw91k_prefactor` FULL BODY implements `(3/10)·(3π²)^(2/3) · ρ^(5/3)` (note: PW91K is the kinetic-GGA variant, prefactor differs from PW91X).
    - Test 2: `shared/pw91_like.rs::pw91xk_enhancement` FULL BODY implements the polynomial `(1 + a1·s²·sqrtx_asinh_sqrtx(a2·s²) + (a3 - a4·exp(-100·s²))·s²) / (1 + a1·s²·sqrtx_asinh_sqrtx(a2·s²) + b·s⁴)` per `pw9xx.hpp:67-87` — exercises `ctaylor_sqrtx_asinh_sqrtx` (D-06) for the first time in a shared helper.
    - Test 3: All 4 PW91 kernels compile + PW91X + PW91C have upstream test_in (tier-1 GREEN at strict 1e-12).
    - Test 4: PW91C specifically does NOT simplify any compound term algebraically — operation order preserved per ACC-03.
    - Test 5: Tier-2 `cargo xtask validate --backend cpu --order 2 --filter 'pw91x|pw91c|pw91k|pw86x'` reports 0 failures at 1e-12.
  </behavior>
  <action>
    **Step A — Flesh out `shared/pw91_like.rs::pw91k_prefactor`:**
    ```rust
    #[cube]
    pub fn pw91k_prefactor<F: Float>(rho: &Array<F>, out: &mut Array<F>, #[comptime] n: u32) {
        // (3/10)·(3π²)^(2/3) · ρ^(5/3)  (pw9xx.hpp:58-63)
        let size = comptime!((1_u32 << n) as usize);
        let mut rho_53 = Array::<F>::new(size);
        ctaylor_pow::<F>(rho, F::cast_from(5.0_f64 / 3.0_f64), &mut rho_53, n);
        // C_TF = (3/10)·(3π²)^(2/3) = 2.8712337090699442
        const C_TF_F64: f64 = 2.871_233_709_069_944_2_f64;
        ctaylor_scalar_mul::<F>(&rho_53, F::cast_from(C_TF_F64), out, n);
    }
    ```
    Use `LYP_CF_F64` constant if it's the same numerical value (verify: yes, C_F in LYP = C_TF in PW91K) — consolidate under a single constant.

    **Step B — Flesh out `shared/pw91_like.rs::pw91xk_enhancement`:** Port `pw9xx.hpp:67-87` VERBATIM. ~50 Rust LOC.
    ```rust
    /// Port of pw9xx.hpp:67-87 — PW91/PW91K enhancement polynomial.
    /// CRITICAL per ACC-03: operation order preserved. NO algebraic simplification.
    /// Branch domain: uses `ctaylor_sqrtx_asinh_sqrtx` from D-06 (plan 03-00).
    #[cube]
    pub fn pw91xk_enhancement<F: Float>(
        s2: &Array<F>,
        a1: F, a2: F, a3: F, a4: F, a5: F, b: F,
        out: &mut Array<F>, #[comptime] n: u32,
    ) {
        // Compute each C++ sub-expression as a named Rust intermediate.
        // Detailed port: executor reads 20 lines of C++ and writes ~50 Rust LOC
        // using the standard pattern: one let mut tmp = Array::new(size) per C++ intermediate.
        // Use ctaylor_sqrtx_asinh_sqrtx(a2·s², ...) for the sqrt-asinh-sqrt term.
        // Use ctaylor_mul, ctaylor_scalar_mul, ctaylor_add, ctaylor_sub, ctaylor_exp, ctaylor_reciprocal.
    }
    ```

    **Step C — 4 PW91-family kernel files.**

    | FunctionalId | File | C++ src | Shared helpers | Key formula |
    |--------------|------|---------|---------------|-------------|
    | XC_PW86X (1) | `gga/pw91/pw86x.rs` | `pw86x.cpp:17-32` | `pw91_like::{chi2, prefactor}`, `ctaylor_pow` (heavy — degree-4 polynomial in chi²) | PW86 enhancement = polynomial in chi²: `(1 + 1.296·chi² + 14·(chi²)² + 0.2·(chi²)³)^(1/15)` per pw86x.cpp |
    | XC_PW91X (26) | `gga/pw91/pw91x.rs` | `pw91x.cpp:18-24` | `pw91_like::{prefactor, pw91xk_enhancement, s2}` + PW91X parameter tuple `(0.19645, 7.7956, 0.2743, 0.1508, 0.004, 0.004)` | prefactor(a) · pw91xk_enhancement(S²(a,gaa), ...params) + β-twin |
    | XC_PW91C (77) | `gga/pw91/pw91c.rs` | `pw91c.cpp:39-87` | `lda::pw92eps`, `pbec_eps::a_expm1_inner`, `ctaylor_log`, `ctaylor_pow`, `ctaylor_sqrt`, `ctaylor_exp`, `ctaylor_cbrt`, `ctaylor_scalar_mul`, `ctaylor_powi_4` | THE LONGEST — ~100 Rust LOC. Uses `pw92_eps` as LSDA baseline + PW91 gradient correction H₀+H₁ with 4 compound terms. **Per ACC-03 + Pitfall G6 — preserve every operator precedence.** |
    | XC_PW91K (27) | `gga/pw91/pw91k.rs` | `pw91k.cpp:21-28` | `pw91_like::{pw91k_prefactor, pw91xk_enhancement, s2}` + PW91K parameter tuple (different from PW91X — read pw91k.cpp) | PW91K (kinetic-GGA variant): pw91k_prefactor(a) · pw91xk_enhancement(...params_k) + β-twin |

    **Step D — commit strategy.** Given PW91C is the longest (~100 LOC), recommend separate commit just for PW91C. Other 3 + shared helpers = another 2 commits.

    **EXECUTOR NOTE — G2 pitfall:** `ctaylor_sqrtx_asinh_sqrtx` has two branches (|x|<0.5 Padé, |x|≥0.5 unstable). Plan 03-00 Task 2 deferred Padé to fixture-gate fallback. At tier-2 Wave-3, if PW91X fails at low-gradient stratum (|S²|<0.5 region), escalate to extend Wave-0 primitive with Padé branch per 03-00-Task-2 action note. Do NOT widen tolerance; recurse back to Wave-0.
  </action>
  <verify>
    <automated>cargo build -p xcfun-eval --features testing &amp;&amp; rg -c "^pub fn (pw86x|pw91x|pw91c|pw91k)_kernel" crates/xcfun-eval/src/functionals/gga/pw91/ &amp;&amp; cargo xtask check-no-mul-add</automated>
  </verify>
  <acceptance_criteria>
    - `ls crates/xcfun-eval/src/functionals/gga/pw91/` prints 5 entries (`mod.rs pw86x.rs pw91x.rs pw91c.rs pw91k.rs`).
    - `rg -c "^pub fn (pw86x|pw91x|pw91c|pw91k)_kernel" crates/xcfun-eval/src/functionals/gga/pw91/` = 4.
    - `wc -l crates/xcfun-eval/src/functionals/gga/pw91/pw91c.rs` ≥ 80 (PW91C is the longest body).
    - `rg "ctaylor_sqrtx_asinh_sqrtx" crates/xcfun-eval/src/functionals/gga/shared/pw91_like.rs` matches ≥ 1.
    - `rg "_ = \(" crates/xcfun-eval/src/functionals/gga/shared/pw91_like.rs` prints ZERO (no skeleton placeholders remain).
  </acceptance_criteria>
  <done>
    PW86/PW91 family compiles. `pw91xk_enhancement` shared helper exercises D-06 sqrtx_asinh_sqrtx — first validation in production kernel path.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Port P86 × 2 + APBE × 2 kernels</name>
  <read_first>
    - `/home/chemtech/workspace/xcfun_rs/xcfun-master/src/functionals/p86c.cpp` (full file — both P86C and P86CORRC live here)
    - `/home/chemtech/workspace/xcfun_rs/xcfun-master/src/functionals/apbex.cpp` (full file, ~40 lines)
    - `/home/chemtech/workspace/xcfun_rs/xcfun-master/src/functionals/apbec.cpp` (full file, ~40 lines)
    - `/home/chemtech/workspace/xcfun_rs/crates/xcfun-eval/src/functionals/lda/pz81c.rs` (full file) — confirm `pz81eps` is `pub` and importable
    - `/home/chemtech/workspace/xcfun_rs/crates/xcfun-eval/src/functionals/lda/mod.rs` — may need `pub use lda::pz81c::pz81eps;` at xcfun-eval crate root
    - `.planning/phases/03-gga-tier-mode-potential/03-RESEARCH.md` §File-by-File P86 + APBE (lines 785-800)
  </read_first>
  <files>
    crates/xcfun-eval/src/functionals/lda/pz81c.rs (modify — ensure `pub fn pz81eps` exported),
    crates/xcfun-eval/src/functionals/lda/mod.rs (possibly modify — re-export pz81eps),
    crates/xcfun-eval/src/functionals/gga/p86/mod.rs (NEW),
    crates/xcfun-eval/src/functionals/gga/p86/p86c.rs (NEW),
    crates/xcfun-eval/src/functionals/gga/p86/p86corrc.rs (NEW),
    crates/xcfun-eval/src/functionals/gga/apbe/mod.rs (NEW),
    crates/xcfun-eval/src/functionals/gga/apbe/apbex.rs (NEW),
    crates/xcfun-eval/src/functionals/gga/apbe/apbec.rs (NEW),
    crates/xcfun-eval/src/functionals/gga/mod.rs (modify — add pub mod p86; pub mod apbe;)
  </files>
  <behavior>
    - Test 1: `pz81eps` is callable from `gga/p86/p86c.rs` (Pitfall G7-analog for P86).
    - Test 2: P86C + P86CORRC compile + P86C has upstream test_in (tier-1 GREEN).
    - Test 3: APBEX + APBEC compile + both use `ctaylor_expm1` via `pbec_eps::a_expm1_inner` (APBEC) + `pbex::enhancement` with MU_APBE_F64 (APBEX).
    - Test 4: All 4 kernels pass tier-2 at 1e-12 (Task 4 runs the filter).
  </behavior>
  <action>
    **Step A — `pz81eps` export audit.** Check that `crates/xcfun-eval/src/functionals/lda/pz81c.rs` has `pub fn pz81eps<F: Float>(...)`. If not, extend visibility.

    **Step B — P86 family per-file table:**

    | FunctionalId | File | C++ src (lines) | Shared helpers | Key formula |
    |--------------|------|-----------------|---------------|-------------|
    | XC_P86C (56) | `gga/p86/p86c.rs` | `p86c.cpp:45-48, 54-110` | `lda::pz81c::pz81eps` (LSDA baseline), `ctaylor_exp`, `ctaylor_sqrt`, `ctaylor_pow`, `ctaylor_cbrt`, P86_F_F64 | P86 correlation = pz81eps(d) · (1 + P86-GGA correction). Uses `f64::EPSILON` fudge at `gnn=0` → `F::cast_from(f64::EPSILON)`. |
    | XC_P86CORRC (57) | `gga/p86/p86corrc.rs` | `p86c.cpp:50-52, 112-119` | Same | P86 gradient correction alone (no LSDA part). |

    **Step C — APBE family per-file table:**

    | FunctionalId | File | C++ src (lines) | Shared helpers | Key formula |
    |--------------|------|-----------------|---------------|-------------|
    | XC_APBEX (68) | `gga/apbe/apbex.rs` | `apbex.cpp:18-38` | `pw91_like::s2`, `pbex::enhancement` with MU_APBE_F64 (0.26), R_PBE_F64 (0.804) | APBEX = PBEX with μ=0.26 instead of MU_PBE (0.2195). Identical shape. |
    | XC_APBEC (67) | `gga/apbe/apbec.rs` | `apbec.cpp:19-38` | `lda::pw92eps::pw92_eps`, `pbec_eps::{phi, a_expm1_inner, h_gga}` | APBEC = PBEC with APBE-specific β parameter (read apbec.cpp for exact β). Uses D-05 ctaylor_expm1 via pbec_eps helper. |

    **Step D — commit.**
  </action>
  <verify>
    <automated>cargo build -p xcfun-eval --features testing &amp;&amp; rg -c "^pub fn (p86c|p86corrc|apbex|apbec)_kernel" crates/xcfun-eval/src/functionals/gga/ &amp;&amp; rg "pz81eps" crates/xcfun-eval/src/functionals/gga/p86/</automated>
  </verify>
  <acceptance_criteria>
    - `ls crates/xcfun-eval/src/functionals/gga/p86/` = 3 entries; `ls crates/xcfun-eval/src/functionals/gga/apbe/` = 3 entries.
    - `rg -c "^pub fn (p86c|p86corrc|apbex|apbec)_kernel" crates/xcfun-eval/src/functionals/gga/` = 4.
    - `rg "pz81eps" crates/xcfun-eval/src/functionals/gga/p86/` ≥ 2 matches (both p86c.rs and p86corrc.rs).
    - `rg "pbec_eps::a_expm1_inner" crates/xcfun-eval/src/functionals/gga/apbe/apbec.rs` ≥ 1 match.
    - `cargo build -p xcfun-eval --features testing` exits 0.
  </acceptance_criteria>
  <done>
    P86 + APBE families compile. Cross-tier import of pz81eps verified live.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Dispatch wiring + validation/build.rs extension + tier-2 for all 10 Wave-3 kernels</name>
  <read_first>
    - `/home/chemtech/workspace/xcfun_rs/crates/xcfun-eval/src/dispatch.rs` (after plan 03-02 edits)
    - `/home/chemtech/workspace/xcfun_rs/validation/build.rs` (after plan 03-02 edits)
    - `.planning/phases/03-gga-tier-mode-potential/03-PATTERNS.md` F1 + J1
  </read_first>
  <files>
    crates/xcfun-eval/src/dispatch.rs (modify — 10 new comptime arms + supports bitmap bump),
    validation/build.rs (modify — 10 new cc::Build::file entries; or 9 if P86C + P86CORRC share p86c.cpp),
    crates/xcfun-eval/src/functional.rs (modify if needed — run_launch match arms)
  </files>
  <behavior>
    - Test 1: `rg -c "comptime!\(id == (1|17|18|26|27|56|57|67|68|77)\)" crates/xcfun-eval/src/dispatch.rs` = 10.
    - Test 2: `supports(id)` bitmap includes all 10 new ids.
    - Test 3: `cargo xtask validate --backend cpu --order 2 --filter 'optx|pw91|p86|apbe'` — 0 failures at 1e-12 on 10k-point stratified grid.
    - Test 4: `cargo xtask regen-registry --check` exits 0 — c_stubs.cpp shrinks by 10 (50 → 40).
  </behavior>
  <action>
    **Step A — Extend dispatch.rs with 10 new arms** (same pattern as plan 03-02 Task 3):
    ```rust
    } else if comptime!(id == 1) {
        crate::functionals::gga::pw91::pw86x::pw86x_kernel::<F>(d, out, n);
    } else if comptime!(id == 17) {
        crate::functionals::gga::optx::optx::optx_kernel::<F>(d, out, n);
    } else if comptime!(id == 18) {
        crate::functionals::gga::optx::optxcorr::optxcorr_kernel::<F>(d, out, n);
    } else if comptime!(id == 26) {
        crate::functionals::gga::pw91::pw91x::pw91x_kernel::<F>(d, out, n);
    } else if comptime!(id == 27) {
        crate::functionals::gga::pw91::pw91k::pw91k_kernel::<F>(d, out, n);
    } else if comptime!(id == 56) {
        crate::functionals::gga::p86::p86c::p86c_kernel::<F>(d, out, n);
    } else if comptime!(id == 57) {
        crate::functionals::gga::p86::p86corrc::p86corrc_kernel::<F>(d, out, n);
    } else if comptime!(id == 67) {
        crate::functionals::gga::apbe::apbec::apbec_kernel::<F>(d, out, n);
    } else if comptime!(id == 68) {
        crate::functionals::gga::apbe::apbex::apbex_kernel::<F>(d, out, n);
    } else if comptime!(id == 77) {
        crate::functionals::gga::pw91::pw91c::pw91c_kernel::<F>(d, out, n);
    }
    ```

    **Step B — Extend `supports(id)` bitmap** to include ids {1, 17, 18, 26, 27, 56, 57, 67, 68, 77} on top of the Wave-2 bitmap.

    **Step C — Extend validation/build.rs:**
    ```rust
    // Wave 3 additions (plan 03-03, D-22):
    for f in &[
        "optx", "optxcorr",            // GGA-05 (2)
        "pw86x", "pw91x", "pw91c", "pw91k",  // GGA-06 (4)
        "p86c",                         // GGA-07 (2 functionals in same .cpp)
        "apbex", "apbec",               // GGA-08 (2)
    ] {
        build.file(format!("{}/src/functionals/{}.cpp", xcfun_root, f));
    }
    ```

    9 new `.cpp` files added (P86C + P86CORRC share `p86c.cpp`).

    **Step D — Regen registry + tier-2 per family.**
    ```bash
    cargo run -p xtask --bin regen_registry
    cargo xtask regen-registry --check
    cargo xtask validate --backend cpu --order 2 --filter 'optx'
    cargo xtask validate --backend cpu --order 2 --filter 'pw91'
    cargo xtask validate --backend cpu --order 2 --filter 'pw86'
    cargo xtask validate --backend cpu --order 2 --filter 'p86'
    cargo xtask validate --backend cpu --order 2 --filter 'apbe'
    ```
    All must report 0 failures at 1e-12. If PW91X fails at |S²|<0.5 gradient stratum (Pitfall G2), recurse back to plan 03-00 Task 2 to port the Padé branch — do NOT widen tolerance.

    **Step E — commit:**
    ```
    feat(03-03-4): dispatch + validation wiring for 10 Wave-3 GGAs

    - crates/xcfun-eval/src/dispatch.rs — 10 new comptime arms (ids 1, 17,
      18, 26, 27, 56, 57, 67, 68, 77); supports() grows from 28 to 38.
    - validation/build.rs — 9 new cc::Build::file entries (P86C + P86CORRC
      share p86c.cpp so 9 files cover 10 functionals); c_stubs.cpp shrinks
      from 50 to 40.
    - Tier-2: --filter {optx, pw91x, pw91c, pw91k, pw86x, p86c, p86corrc,
      apbex, apbec, optxcorr} all GREEN at 1e-12 strict.

    Co-Authored-By: Claude Haiku 4.5 <noreply@anthropic.com>
    ```
  </action>
  <verify>
    <automated>cargo build -p xcfun-eval --features testing &amp;&amp; cargo xtask regen-registry --check &amp;&amp; cargo xtask validate --backend cpu --order 2 --filter 'optx' &amp;&amp; cargo xtask validate --backend cpu --order 2 --filter 'pw91' &amp;&amp; cargo xtask validate --backend cpu --order 2 --filter 'p86' &amp;&amp; cargo xtask validate --backend cpu --order 2 --filter 'apbe'</automated>
  </verify>
  <acceptance_criteria>
    - `rg -c "comptime!\(id == (1|17|18|26|27|56|57|67|68|77)\)" crates/xcfun-eval/src/dispatch.rs` = 10.
    - All 4 per-family `cargo xtask validate` filters report 0 failures at 1e-12 strict.
    - `cargo xtask regen-registry --check` exits 0.
    - c_stubs.cpp line-count decreases by a measurable amount (10 stubs removed).
    - No PLANNING INCONCLUSIVE escalation needed (if one is — document in SUMMARY).
  </acceptance_criteria>
  <done>
    Wave 3 complete. 10 more GGA kernels GREEN at 1e-12. Phase 3 now has 27 of 36 GGAs shipped. Plan 03-04 wraps with 8 more (B97 + KTX + BTK).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `pw91xk_enhancement` → `ctaylor_sqrtx_asinh_sqrtx` | First heavy production use of D-06 primitive; Padé-branch domain (|S²|<0.5) is the failure risk |
| `gga/p86/p86c.rs` → `lda::pz81c::pz81eps` | Cross-tier import — confirm pub-visibility + no circular dep |
| `gga/apbe/apbec.rs` → `gga/shared/pbec_eps::a_expm1_inner` | Intra-GGA helper reuse; D-05 expm1 proven in plan 03-02 for PBEC already |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Denial-of-Service | PW91C body (~100 LOC) | mitigate | Operation-order preserved per ACC-03; every `ctaylor_log`/`ctaylor_reciprocal` domain precondition documented in the C++ original. No `assert!` in kernel. |
| T-03-02 | Tampering (drift) | `pz81eps` cross-tier import | mitigate | P86C tier-2 test verifies pz81eps produces the identical numerical result at strict 1e-12 vs the C++ reference. |
| T-03-04 | Denial-of-Service (compile blow-up) | dispatch_kernel 38-arm if-chain + run_launch growth | mitigate | Wall-clock compile budget tracked per Pitfall G10. Wave-3 adds 10 × 5 orders × 9 vars = 450 new monomorphisations to `run_launch`. If > 45 s, split `run_launch` per-family. |
| T-03-05 | Tampering (unsafe-code) | xcfun-eval crate boundary | accept | `#[forbid(unsafe_code)]` preserved. |
</threat_model>

<verification>
Plan-level verification:
- `cargo build -p xcfun-eval --features testing` — exits 0, wall-clock tracked (Pitfall G10).
- `cargo xtask regen-registry --check` — exits 0.
- `cargo xtask validate --backend cpu --order 2 --filter 'optx'` — 0 failures at 1e-12.
- `cargo xtask validate --backend cpu --order 2 --filter 'pw91'` — 0 failures at 1e-12 (includes pw86x, pw91x, pw91c, pw91k).
- `cargo xtask validate --backend cpu --order 2 --filter 'p86'` — 0 failures at 1e-12.
- `cargo xtask validate --backend cpu --order 2 --filter 'apbe'` — 0 failures at 1e-12.
- `cargo xtask check-no-mul-add` — exits 0.
- `cargo xtask check-no-fma` — exits 0.
</verification>

<success_criteria>
1. 10 new `#[cube] fn <name>_kernel<F>` bodies in `crates/xcfun-eval/src/functionals/gga/{optx,pw91,p86,apbe}/`.
2. Shared helper full bodies: `optx::{g_xa2, optx_enhancement}`, `pw91_like::{pw91k_prefactor, pw91xk_enhancement}` — no more skeletons.
3. `dispatch_kernel` has 10 new arms; `supports(id)` bitmap has 38 ids total (11 LDA + 17 Wave-2 + 10 Wave-3).
4. Tier-2 per-family verification GREEN at 1e-12 strict for all 10 functionals.
5. c_stubs.cpp reduced by ~10 entries (50 → 40).
6. Cross-tier import of `pz81eps` from Phase-2 `lda/pz81c.rs` proven correct (Pitfall G7 analog).
</success_criteria>

<output>
After completion, create `.planning/phases/03-gga-tier-mode-potential/03-03-SUMMARY.md` with:
- Kernels shipped (10 — per family)
- PW91C size (LOC) confirming it IS the longest GGA body + tier-2 result
- Whether D-06 Padé branch was needed (did Wave-0 Task 2 get retroactively extended?)
- `pz81eps` cross-tier visibility status (was `pub use lda::pz81c::pz81eps` added?)
- Compile-time wall-clock for `cargo build -p xcfun-eval --features testing` (Pitfall G10 tracker — compare to 03-02 baseline)
- c_stubs.cpp line-count delta
- Forward risks for plan 03-04 (Wave 4 B97 family) — particularly: will B97C coefficient tables produce polynomial conditioning issues at order 4 per Pitfall G6?
- Commit hashes
</output>
